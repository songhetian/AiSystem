# 开发文档

## 目录

1. [环境模式说明](#环境模式说明)
2. [开发环境搭建](#开发环境搭建)
3. [测试数据服务器](#测试数据服务器)
4. [项目结构](#项目结构)
5. [开发流程](#开发流程)
6. [代码规范](#代码规范)
7. [调试指南](#调试指南)
8. [常见问题](#常见问题)

---

## 环境模式说明

### 🔧 开发环境 (Development)

**用途**：日常开发、功能调试、热更新开发
**特点**：

- 包含 Mock 测试数据服务器
- 支持代码热更新
- 详细的调试日志
- API 文档可访问
- 直接访问前后端服务

### 🧪 仿生产环境 (Production-like)

**用途**：集成测试、部署验证、性能测试
**特点**：

- 不包含 Mock 服务器
- 通过 Nginx 统一入口
- 模拟生产环境配置
- 关闭调试功能

---

## 开发环境搭建

### 环境要求

- **Node.js**: >= 18.0.0
- **npm**: >= 9.0.0
- **Docker**: >= 20.10.0
- **Docker Compose**: >= 2.0.0
- **Git**: >= 2.30.0

### 🚀 开发环境启动（推荐日常开发）

```bash
# 1. 克隆项目
git clone <repository-url>
cd aisystem

# 2. 切换到开发环境配置
npm run env:dev

# 3. 安装所有依赖
npm run install:all

# 4. 启动基础服务（MySQL、Redis、MinIO、Qdrant）
npm run docker:base

# 5. 等待服务启动完成（约 30 秒）
npm run docker:ps

# 6. 执行数据库迁移
npm run db:migrate:deploy

# 7. 导入种子数据
npm run db:seed:local

# 8. 🔧 启动开发环境（包含 Mock 服务器）
npm run docker:up

# 9. 访问系统
# 前端（热更新）：http://localhost:8000
# 后端 API：http://localhost:3000/api
# API 文档：http://localhost:3000/api/api-docs
# Mock 服务器：http://localhost:3888
# 统一入口（Nginx）：http://localhost
```

### 🎯 仿生产环境启动（集成测试用）

```bash
# 1. 切换到生产环境配置
npm run env:prod

# 2. 修改 .env 中的密码（测试环境可以使用简单密码）
vim .env

# 3. 🧪 启动仿生产环境（不包含 Mock 服务器）
docker compose up -d --build

# 4. 执行数据库迁移
npm run db:migrate:deploy

# 5. 导入种子数据
npm run db:seed:local

# 6. 访问系统
# 统一入口：http://localhost
# 注意：仿生产环境不提供直接访问前后端的端口
```

### 📊 测试数据服务器启动

```bash
# 方式一：单独启动 Mock 服务器
npm run docker:mock

# 方式二：开发环境自动包含 Mock 服务器
npm run docker:up

# 访问 Mock API：
# 京东数据：http://localhost:3888/api/jd/*
# 拼多多数据：http://localhost:3888/api/pdd/*
# 淘宝数据：http://localhost:3888/api/taobao/*
```

---

## 测试数据服务器

### Mock 服务器说明

Mock 服务器模拟真实的电商平台 API，提供测试数据用于开发和测试。

**🔧 开发环境**：自动启动 Mock 服务器
**🧪 仿生产环境**：不启动 Mock 服务器（模拟真实生产环境）

### Mock API 接口

#### 京东 (JD) 接口

```bash
# 订单数据
curl http://localhost:3888/api/jd/orders

# 客服对话
curl http://localhost:3888/api/jd/chats

# 商品评价
curl http://localhost:3888/api/jd/evaluations

# 业务数据
curl http://localhost:3888/api/jd/business-data
```

#### 拼多多 (PDD) 接口

```bash
# 订单数据
curl http://localhost:3888/api/pdd/orders

# 客服对话
curl http://localhost:3888/api/pdd/chats

# 商品评价
curl http://localhost:3888/api/pdd/evaluations

# 业务数据
curl http://localhost:3888/api/pdd/business-data
```

#### 淘宝 (Taobao) 接口

```bash
# 订单数据
curl http://localhost:3888/api/taobao/orders

# 客服对话
curl http://localhost:3888/api/taobao/chats

# 商品评价
curl http://localhost:3888/api/taobao/evaluations

# 业务数据
curl http://localhost:3888/api/taobao/business-data
```

### Mock 数据特点

- **真实场景**：退款纠纷、发货延迟、商品瑕疵等
- **质检要点**：响应时效、服务态度、专业能力、合规性
- **平台差异**：不同平台的订单字段和业务逻辑
- **数据量**：每个平台 100+ 条测试数据

### 生成新的测试数据

```bash
# 进入开发工具目录
cd dev-tools

# 安装依赖
npm install

# 生成新的测试数据
npm run generate

# 查看生成的数据
ls -la data-templates/
```

---

## 常用开发指令对比

### 🔧 开发环境指令

| 指令                     | 说明                     | 包含服务                       |
| ------------------------ | ------------------------ | ------------------------------ |
| `npm run docker:up`      | 启动完整开发环境         | 所有服务 + Mock 服务器         |
| `npm run docker:app:dev` | 启动应用服务（开发模式） | Backend + Frontend（无 Nginx） |
| `npm run docker:mock`    | 单独启动 Mock 服务器     | 仅 Mock 服务器                 |
| `npm run docker:base`    | 启动基础服务             | MySQL + Redis + MinIO + Qdrant |

**访问地址（开发环境）**：

- 前端（热更新）：http://localhost:8000
- 后端 API：http://localhost:3000/api
- API 文档：http://localhost:3000/api/api-docs
- Mock 服务器：http://localhost:3888
- 统一入口：http://localhost

### 🧪 仿生产环境指令

| 指令                           | 说明                     | 包含服务                   |
| ------------------------------ | ------------------------ | -------------------------- |
| `docker compose up -d --build` | 启动仿生产环境           | 所有服务（无 Mock）        |
| `npm run docker:app`           | 启动应用服务（生产模式） | Backend + Frontend + Nginx |

**访问地址（仿生产环境）**：

- 统一入口：http://localhost
- 注意：不提供直接访问前后端的端口

### 环境切换指令

```bash
# 切换到开发环境配置
npm run env:dev

# 切换到生产环境配置
npm run env:prod

# 检查当前环境配置
npm run env:check
```

---

## 项目结构

```
aisystem/
├── frontend/                 # 前端项目 (UmiJS + React)
│   ├── src/
│   │   ├── pages/           # 页面组件
│   │   │   ├── auth/        # 认证相关页面
│   │   │   ├── system/      # 系统管理页面
│   │   │   ├── organization/ # 组织架构页面
│   │   │   └── dashboard/   # 仪表盘页面
│   │   ├── components/      # 公共组件
│   │   │   ├── layout/      # 布局组件
│   │   │   ├── form/        # 表单组件
│   │   │   └── table/       # 表格组件
│   │   ├── api/             # API 接口定义
│   │   ├── models/          # 状态管理 (Zustand)
│   │   ├── utils/           # 工具函数
│   │   └── constants/       # 常量定义
│   ├── config/              # 配置文件
│   ├── public/              # 静态资源
│   └── Dockerfile           # 前端容器配置
├── backend/                  # 后端项目 (NestJS)
│   ├── src/
│   │   ├── modules/         # 业务模块
│   │   │   ├── auth/        # 认证授权模块
│   │   │   ├── user/        # 用户管理模块
│   │   │   ├── role/        # 角色管理模块
│   │   │   ├── menu/        # 菜单管理模块
│   │   │   ├── organization/ # 组织架构模块
│   │   │   └── file/        # 文件管理模块
│   │   ├── common/          # 公共模块
│   │   │   ├── decorators/  # 装饰器（防抖、限流等）
│   │   │   ├── guards/      # 守卫（认证、权限等）
│   │   │   ├── filters/     # 异常过滤器
│   │   │   ├── dto/         # 数据传输对象
│   │   │   └── utils/       # 工具函数
│   │   └── app.module.ts    # 应用主模块
│   ├── prisma/              # 数据库相关
│   │   ├── schema.prisma    # 数据库模型定义
│   │   ├── migrations/      # 数据库迁移文件
│   │   └── seed.ts          # 种子数据脚本
│   ├── storage/             # 文件存储目录
│   └── Dockerfile           # 后端容器配置
├── dev-tools/               # 开发工具
│   ├── mock-server.ts       # Mock API 服务器
│   ├── generate-data.ts     # 测试数据生成
│   └── data-templates/      # 测试数据模板
│       ├── jd/              # 京东测试数据
│       ├── pdd/             # 拼多多测试数据
│       └── taobao/          # 淘宝测试数据
├── infra/                   # 基础设施配置
│   ├── nginx/               # Nginx 配置
│   ├── mysql/               # MySQL 配置
│   └── redis/               # Redis 配置
├── scripts/                 # 脚本工具
├── .env.development         # 开发环境配置
├── .env.production          # 生产环境配置
├── docker-compose.yml       # Docker 编排配置
└── package.json             # 根目录脚本
```

---

## 开发流程

### 1. 功能开发流程

```bash
# 1. 创建功能分支
git checkout -b feature/user-management

# 2. 启动开发环境
npm run env:dev
npm run docker:up

# 3. 后端开发
cd backend

# 3.1 创建数据库模型（如需要）
# 编辑 prisma/schema.prisma
npx prisma migrate dev --name add_user_table

# 3.2 创建业务模块
nest g module modules/user
nest g controller modules/user
nest g service modules/user

# 3.3 编写业务逻辑
# 实现 Controller、Service、DTO 等

# 4. 前端开发
cd ../frontend

# 4.1 创建页面组件
# 在 src/pages/ 下创建相应页面

# 4.2 创建 API 接口
# 在 src/api/ 下定义接口

# 4.3 实现页面逻辑
# 使用 React Query + Zustand 管理状态

# 5. 测试验证
npm run docker:logs:backend  # 查看后端日志
npm run docker:logs:frontend # 查看前端日志

# 6. 提交代码
git add .
git commit -m "feat: add user management module"
git push origin feature/user-management
```

### 2. 数据库开发流程

```bash
# 1. 修改数据库模型
# 编辑 backend/prisma/schema.prisma

# 2. 生成迁移文件
npm run db:migrate:dev

# 3. 应用迁移（如果使用 Docker）
npm run db:migrate:deploy

# 4. 更新种子数据（如需要）
# 编辑 backend/prisma/seed.ts
npm run db:seed:local

# 5. 查看数据库
npm run db:studio
```

### 3. API 开发流程

```bash
# 1. 定义 DTO（数据传输对象）
# backend/src/modules/user/dto/create-user.dto.ts

# 2. 实现 Controller
# backend/src/modules/user/user.controller.ts
# 使用装饰器：@ApiOperation, @ApiResponse 等

# 3. 实现 Service
# backend/src/modules/user/user.service.ts
# 使用 Prisma 进行数据库操作

# 4. 添加权限控制
# 使用 @UseGuards(JwtAuthGuard, RoleGuard)

# 5. 测试 API
# 访问 http://localhost:3000/api/api-docs
# 使用 Swagger 文档测试接口
```

---

## 代码规范

### 1. TypeScript 规范

```typescript
// ✅ 好的示例
interface CreateUserDto {
  username: string;
  email: string;
  roleIds: number[];
}

class UserService {
  async createUser(dto: CreateUserDto): Promise<User> {
    // 实现逻辑
  }
}

// ❌ 避免的写法
function createUser(data: any): any {
  // 缺少类型定义
}
```

### 2. 命名规范

```typescript
// 文件命名：kebab-case
user-management.controller.ts
create-user.dto.ts

// 类命名：PascalCase
class UserManagementController {}
class CreateUserDto {}

// 方法/变量命名：camelCase
const userName = 'admin';
async getUserById(id: number) {}

// 常量命名：UPPER_SNAKE_CASE
const MAX_FILE_SIZE = 1024 * 1024;
```

### 3. Git 提交规范

```bash
# 提交格式
<type>(<scope>): <subject>

# 类型说明
feat: 新功能
fix: 修复 bug
docs: 文档更新
style: 代码格式调整
refactor: 代码重构
test: 测试相关
chore: 构建/工具链相关

# 示例
feat(user): add user management module
fix(auth): resolve login token expiration issue
docs(readme): update installation guide
```

---

## 调试指南

### 1. 后端调试

```bash
# 查看后端日志
npm run docker:logs:backend

# 进入后端容器
docker compose exec backend sh

# 查看环境变量
docker compose exec backend env

# 测试数据库连接
docker compose exec backend npx prisma db pull
```

### 2. 前端调试

```bash
# 查看前端日志
npm run docker:logs:frontend

# 进入前端容器
docker compose exec frontend sh

# 查看构建状态
docker compose exec frontend npm run build
```

### 3. Mock 服务器调试

```bash
# 查看 Mock 服务器日志
npm run docker:logs:mock

# 测试 Mock API
curl http://localhost:3888/api/jd/orders
curl http://localhost:3888/api/pdd/chats
curl http://localhost:3888/api/taobao/evaluations

# 进入 Mock 服务器容器
docker compose exec mock-service sh
```

### 4. 数据库调试

```bash
# 启动 Prisma Studio
npm run db:studio

# 查看数据库状态
npm run db:migrate:status

# 进入 MySQL 容器
docker compose exec mysql mysql -u aisystem -p aisystem

# 查看表结构
DESCRIBE users;
SHOW TABLES;
```

---

## 常见问题

### 1. 端口冲突

**问题**：端口被占用，容器启动失败

**解决方案**：

```bash
# 查看端口占用
lsof -i :3306  # macOS/Linux
netstat -ano | findstr :3306  # Windows

# 修改 .env 中的端口配置
MYSQL_PORT=3307
REDIS_PORT=6380
MOCK_SERVICE_PORT=3889
```

### 2. Mock 服务器无法访问

**问题**：http://localhost:3888 无法访问

**解决方案**：

```bash
# 1. 确认使用开发环境配置
npm run env:dev

# 2. 启动包含 Mock 服务器的开发环境
npm run docker:up

# 3. 检查 Mock 服务器状态
npm run docker:logs:mock

# 4. 单独启动 Mock 服务器
npm run docker:mock
```

### 3. 开发环境和仿生产环境混淆

**问题**：不知道当前运行的是哪种环境

**解决方案**：

```bash
# 检查当前环境配置
npm run env:check

# 查看当前运行的容器
docker compose ps

# 开发环境会显示 mock-service 容器
# 仿生产环境不会显示 mock-service 容器
```

### 4. 数据库连接失败

**问题**：Backend 无法连接 MySQL

**解决方案**：

```bash
# 1. 检查 MySQL 是否启动
npm run docker:ps

# 2. 查看 MySQL 日志
npm run docker:logs:mysql

# 3. 等待 MySQL 初始化完成（首次启动需要约 30 秒）

# 4. 检查数据库配置
npm run env:check
```

### 5. 热更新不生效

**问题**：修改代码后页面不自动刷新

**解决方案**：

```bash
# 1. 确保使用开发模式
npm run docker:up
# 或
npm run docker:app:dev

# 2. 检查前端容器日志
npm run docker:logs:frontend

# 3. 重启前端容器
docker compose restart frontend
```

### 6. API 文档无法访问

**问题**：http://localhost:3000/api/api-docs 无法访问

**解决方案**：

```bash
# 1. 确认使用开发环境配置
npm run env:dev

# 2. 检查 .env 中的配置
API_DOCS_ENABLED=true
DEBUG_MODE=true

# 3. 重启后端服务
docker compose restart backend
```

---

## 开发技巧

### 1. 快速重启服务

```bash
# 重启单个服务
docker compose restart backend
docker compose restart frontend
docker compose restart mock-service

# 重启所有服务
npm run docker:restart
```

### 2. 查看实时日志

```bash
# 查看所有日志
npm run docker:logs

# 查看特定服务日志
npm run docker:logs:backend
npm run docker:logs:frontend
npm run docker:logs:mock
npm run docker:logs:mysql
```

### 3. 数据库快速操作

```bash
# 可视化管理
npm run db:studio

# 快速备份
npm run db:backup

# 快速恢复
npm run db:restore
```

### 4. 环境快速切换

```bash
# 切换到开发环境
npm run env:dev
npm run docker:up

# 切换到仿生产环境
npm run env:prod
docker compose up -d --build

# 检查环境配置
npm run env:check
```

### 5. Mock 数据管理

```bash
# 生成新的测试数据
cd dev-tools
npm run generate

# 查看生成的数据文件
ls -la data-templates/jd/
ls -la data-templates/pdd/
ls -la data-templates/taobao/

# 重启 Mock 服务器加载新数据
docker compose restart mock-service
```

---

---

## 新功能开发指南

### 数据大屏实时功能

**功能位置**：`/service/dashboard`

**后端文件**：
- `backend/src/modules/service/services/dashboard-realtime.service.ts` - 实时推送服务
- `backend/src/common/gateways/realtime.gateway.ts` - WebSocket网关
- `backend/src/modules/service/controllers/service.controller.ts` - API控制器

**前端文件**：
- `frontend/src/pages/service/dashboard/index.tsx` - 大屏组件
- `frontend/src/hooks/useWebSocket.ts` - WebSocket Hook
- `frontend/src/hooks/useRealtimeDashboard.ts` - 实时大屏Hook

**开发流程**：
```bash
# 1. 启动开发环境
npm run docker:up

# 2. 访问大屏
# http://localhost:8000/service/dashboard

# 3. 查看实时日志
npm run docker:logs:backend

# 4. 测试 WebSocket 连接
# 打开浏览器控制台,查看 WebSocket 连接状态
```

**调试技巧**：
```typescript
// 前端调试 WebSocket
const { socket, isConnected } = useWebSocket();
console.log('WebSocket状态:', isConnected);
console.log('Socket实例:', socket);

// 后端调试定时任务
@Cron('*/30 * * * * *')
async pushDashboardMetrics() {
  console.log('推送大屏数据:', new Date());
  // ...
}
```

### 前端性能优化组件

**组件位置**：
- `frontend/src/components/VirtualList/index.tsx` - 虚拟滚动
- `frontend/src/components/LazyImage/index.tsx` - 图片懒加载
- `frontend/src/hooks/useDebounceSearch.ts` - 防抖搜索
- `frontend/src/utils/storage.ts` - 本地存储
- `frontend/src/utils/queryClient.ts` - React Query配置
- `frontend/src/utils/performance.ts` - 性能监控

**使用示例**：

#### 1. 虚拟滚动
```typescript
import { VirtualList } from '@/components/VirtualList';

<VirtualList
  data={largeDataArray}
  itemHeight={60}
  containerHeight={500}
  renderItem={(item, index) => (
    <div>{item.name}</div>
  )}
/>
```

#### 2. 图片懒加载
```typescript
import { LazyImage } from '@/components/LazyImage';

<LazyImage
  src="/path/to/image.jpg"
  alt="描述"
  placeholder={<Skeleton.Image />}
/>
```

#### 3. 防抖搜索
```typescript
import { useDebounceSearch } from '@/hooks/useDebounceSearch';

const { searchTerm, setSearchTerm, data, isLoading } = useDebounceSearch({
  searchFn: (query) => api.search(query),
  delay: 300,
  minLength: 2,
});
```

#### 4. 性能监控
```typescript
import { performanceMonitor, initPerformanceMonitoring } from '@/utils/performance';

// 初始化监控
initPerformanceMonitoring();

// 测量性能
await performanceMonitor.measure('任务名称', async () => {
  // 执行任务
});

// 查看统计
const avg = performanceMonitor.getAverageDuration('任务名称');
console.log('平均耗时:', avg);
```

**性能优化示例页面**：
- 访问 `http://localhost:8000/examples/performance-demo`
- 查看所有性能优化组件的使用示例

### 质检Prompt功能

**功能位置**：
- 全局配置：`/service/quality-prompts/global`
- 部门配置：`/service/quality-prompts/department`
- 模板库：`/service/quality-prompts/templates`

**后端文件**：
- `backend/src/modules/service/controllers/quality-prompt.controller.ts` - 控制器
- `backend/src/modules/service/services/quality-prompt.service.ts` - 核心服务
- `backend/src/modules/service/services/conflict-validator.service.ts` - 冲突检测
- `backend/src/modules/service/services/version-manager.service.ts` - 版本管理
- `backend/src/modules/service/services/template-library.service.ts` - 模板库

**前端文件**：
- `frontend/src/pages/service/quality-prompts/` - 页面组件
- `frontend/src/api/quality-prompt/` - API接口

**开发流程**：
```bash
# 1. 查看数据模型
# backend/prisma/schema.prisma
# 搜索 service_quality_prompt

# 2. 测试API
# http://localhost:3000/api/api-docs
# 搜索 quality-prompt

# 3. 查看前端页面
# http://localhost:8000/service/quality-prompts/global
```

---

## 性能监控

### 前端性能指标

```bash
# 查看性能报告
# 打开浏览器控制台
performanceMonitor.exportReport()

# 查看页面性能
# 控制台会自动输出:
# - DNS 查询时间
# - TCP 连接时间
# - 请求响应时间
# - DOM 解析时间
# - 页面加载时间
# - FCP (首次内容绘制)
# - LCP (最大内容绘制)
# - FID (首次输入延迟)
```

### 后端性能监控

```bash
# 查看定时任务执行时间
# 后端日志会输出:
# ⏱️ 推送大屏数据: XXXms

# 查看数据库查询性能
# 使用 @QueryOptimize 装饰器
# 慢查询会自动记录到日志
```

---

**最后更新时间**：2026-04-21
