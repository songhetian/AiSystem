# 前端优化快速应用指南

本指南帮助你在 5 分钟内将优化应用到任何列表页面。

---

## 🚀 快速开始（5 分钟）

### 步骤 1: 导入必要的依赖 (30 秒)

在页面顶部添加导入：

```typescript
import { useDebounce, useFormDraft, useKeyboardShortcuts } from "@/hooks";
import { GlobalLoading } from "@/components/common";
import {
  confirmBatchAction,
  handleExportWithProgress,
  resetColumnConfig,
} from "@/utils/ui-helpers";
import { useRef } from "react"; // 如果还没导入
```

---

### 步骤 2: 添加搜索防抖 (1 分钟)

**找到搜索相关的 state**:

```typescript
const [searchText, setSearchText] = useState("");
```

**添加防抖**:

```typescript
const [searchText, setSearchText] = useState("");
const debouncedSearchText = useDebounce(searchText, 500); // 👈 添加这行
```

**更新过滤逻辑**:

```typescript
// 将所有 searchText 改为 debouncedSearchText
const filteredData = data.filter(
  (item) => item.name?.includes(debouncedSearchText), // 👈 使用 debouncedSearchText
);
```

---

### 步骤 3: 添加表单草稿保存 (1 分钟)

**找到表单相关代码**:

```typescript
const [form] = Form.useForm();
```

**添加草稿保存**:

```typescript
const [form] = Form.useForm();
const { clearDraft } = useFormDraft(form, "your-page-name-form", 30000); // 👈 添加这行
```

**在提交成功后清除草稿**:

```typescript
const createMutation = useMutation({
  mutationFn: api.create,
  onSuccess: async () => {
    clearDraft(); // 👈 添加这行
    message.success("创建成功");
    // ... 其他逻辑
  },
});
```

---

### 步骤 4: 添加快捷键支持 (2 分钟)

**添加搜索框引用**:

```typescript
const searchInputRef = useRef<any>(null);
```

**添加快捷键配置**:

```typescript
useKeyboardShortcuts({
  "Ctrl+n": () => {
    setOpen(true);
    setEditing(null);
    form.resetFields();
  },
  "Ctrl+f": () => searchInputRef.current?.focus(),
  "Ctrl+r": () => refresh(),
  Escape: () => {
    if (open) setOpen(false);
  },
});
```

**更新搜索框**:

```typescript
<Input.Search
  ref={searchInputRef} // 👈 添加这行
  placeholder="搜索... (Ctrl+F)" // 👈 更新提示
  // ... 其他属性
/>
```

---

### 步骤 5: 添加全局加载状态 (30 秒)

**包裹表格组件**:

```typescript
// 之前
<BaseTable
  dataSource={filteredData}
  loading={isLoading}
  // ...
/>

// 之后
<GlobalLoading loading={isLoading}>
  <BaseTable
    dataSource={filteredData}
    loading={isLoading}
    // ...
  />
</GlobalLoading>
```

---

## 🎯 可选优化（额外 5 分钟）

### 优化 1: 批量操作确认

**替换原有的 Modal.confirm**:

```typescript
// 之前
<Button
  onClick={() => {
    Modal.confirm({
      title: "确认批量删除？",
      content: `将删除 ${selectedIds.length} 条记录`,
      onOk: () => batchDeleteMutation.mutate(selectedIds),
    });
  }}
>
  批量删除
</Button>

// 之后
<Button
  onClick={() => {
    confirmBatchAction(
      selectedIds.length,
      "删除",
      async () => {
        await batchDeleteMutation.mutateAsync(selectedIds);
      },
      "记录"
    );
  }}
>
  批量删除
</Button>
```

---

### 优化 2: 导出进度提示

**替换原有的导出逻辑**:

```typescript
// 之前
<Button
  onClick={async () => {
    try {
      message.loading("正在导出...");
      const blob = await api.export();
      // 下载逻辑
      message.success("导出成功");
    } catch {
      message.error("导出失败");
    }
  }}
>
  导出
</Button>

// 之后
<Button
  onClick={async () => {
    await handleExportWithProgress(
      async () => {
        const blob = await api.export();
        // 下载逻辑
      },
      "数据列表.xlsx"
    );
  }}
>
  导出
</Button>
```

---

### 优化 3: 列配置重置按钮

**添加重置按钮**:

```typescript
<Space>
  <ColumnCustomizer
    columns={columns}
    onChange={(newColumns) => {
      setColumns(newColumns);
      saveColumnConfig("your-table-columns", newColumns);
    }}
    storageKey="your-table-columns"
  />
  <Button
    icon={<ReloadOutlined />}
    onClick={() => resetColumnConfig(
      "your-table-columns",
      defaultColumnConfig,
      setColumns
    )}
    title="重置列配置"
  >
    重置列
  </Button>
</Space>
```

---

### 优化 4: 操作反馈优化

**为所有 mutation 添加反馈**:

```typescript
const createMutation = useMutation({
  mutationFn: api.create,
  onSuccess: () => {
    message.success("创建成功"); // 👈 添加成功提示
    // ...
  },
  onError: () => {
    message.error("创建失败，请重试"); // 👈 添加错误提示
  },
});

const updateMutation = useMutation({
  mutationFn: api.update,
  onSuccess: () => {
    message.success("更新成功"); // 👈 添加成功提示
    // ...
  },
  onError: () => {
    message.error("更新失败，请重试"); // 👈 添加错误提示
  },
});

const deleteMutation = useMutation({
  mutationFn: api.delete,
  onSuccess: () => {
    message.success("删除成功"); // 👈 添加成功提示
    // ...
  },
  onError: () => {
    message.error("删除失败，请重试"); // 👈 添加错误提示
  },
});
```

---

## 📋 检查清单

应用优化后，检查以下项目：

### 必须项

- [ ] 导入了必要的 Hooks 和组件
- [ ] 搜索使用了防抖（`useDebounce`）
- [ ] 表单添加了草稿保存（`useFormDraft`）
- [ ] 添加了快捷键支持（`useKeyboardShortcuts`）
- [ ] 表格使用了全局加载（`GlobalLoading`）

### 推荐项

- [ ] 批量操作使用了确认对话框（`confirmBatchAction`）
- [ ] 导出使用了进度提示（`handleExportWithProgress`）
- [ ] 添加了列配置重置按钮
- [ ] 所有 mutation 都有成功/失败反馈

### 测试项

- [ ] 搜索输入时不会立即触发请求（等待 500ms）
- [ ] 表单填写后刷新页面，数据能恢复
- [ ] 快捷键能正常工作
- [ ] 批量操作有确认提示
- [ ] 所有操作都有明确的反馈

---

## 🔍 常见问题

### Q1: 搜索防抖不生效？

**A**: 确保在过滤逻辑中使用 `debouncedSearchText` 而不是 `searchText`。

### Q2: 表单草稿不保存？

**A**: 检查是否在提交成功后调用了 `clearDraft()`，以及表单的 key 是否唯一。

### Q3: 快捷键冲突？

**A**: 避免使用浏览器默认快捷键，如 Ctrl+S、Ctrl+P 等。

### Q4: 草稿保存太频繁？

**A**: 调整保存间隔，如改为 60000（60 秒）。

---

## 📊 优化效果对比

### 优化前

```typescript
// ❌ 每次输入都触发请求
const filteredData = data.filter(item =>
  item.name?.includes(searchText)
);

// ❌ 表单数据可能丢失
const [form] = Form.useForm();

// ❌ 只能用鼠标操作
<Button onClick={() => setOpen(true)}>新建</Button>

// ❌ 批量操作没有确认
<Button onClick={() => batchDelete()}>批量删除</Button>
```

### 优化后

```typescript
// ✅ 防抖后触发请求
const debouncedSearchText = useDebounce(searchText, 500);
const filteredData = data.filter((item) =>
  item.name?.includes(debouncedSearchText),
);

// ✅ 自动保存草稿
const { clearDraft } = useFormDraft(form, "form-key", 30000);

// ✅ 支持快捷键
useKeyboardShortcuts({
  "Ctrl+n": () => setOpen(true),
});

// ✅ 友好的确认提示
confirmBatchAction(
  count,
  "删除",
  async () => {
    await batchDelete();
  },
  "记录",
);
```

---

## 🎯 页面优化优先级

### 高优先级（立即优化）

1. 员工管理 ✅ 已完成
2. 审批流程列表
3. 考勤记录列表
4. 商品管理列表

### 中优先级（本周完成）

5. 部门管理
6. 岗位管理
7. 权限管理
8. 系统配置

### 低优先级（下周完成）

9. 其他列表页面
10. 详情页面优化

---

## 💡 最佳实践

### 1. 统一的 key 命名

```typescript
// 表单草稿 key
useFormDraft(form, "employee-form", 30000);
useFormDraft(form, "department-form", 30000);

// 列配置 key
saveColumnConfig("employee-table-columns", columns);
saveColumnConfig("department-table-columns", columns);

// 搜索历史 key
<SmartSearch storageKey="employee-search-history" />
<SmartSearch storageKey="department-search-history" />
```

### 2. 统一的快捷键

所有列表页面使用相同的快捷键：

- Ctrl+N: 新建
- Ctrl+F: 聚焦搜索
- Ctrl+R: 刷新
- Escape: 关闭弹窗

### 3. 统一的提示文案

```typescript
message.success("创建成功");
message.success("更新成功");
message.success("删除成功");
message.error("操作失败，请重试");
```

---

## 📚 相关文档

- `前端优化建议.md` - 完整的优化建议列表
- `前端优化实施报告.md` - 已完成的优化详情
- `测试工具文档.md` - 后端优化工具使用指南

---

**最后更新**: 2026-04-16
**适用范围**: 所有列表页面
**预计时间**: 5-10 分钟/页面
