# 雷犀AI客服系统 - 使用手册

> **版本**: 3.0  
> **更新时间**: 2026-04-25  
> **维护者**: 雷犀科技开发团队

## 📋 目录

- [快速开始](#-快速开始)
- [环境配置](#-环境配置)
- [命令说明](#-命令说明)
- [服务说明](#-服务说明)
- [开发指南](#-开发指南)
- [生产部署](#-生产部署)
- [常见问题](#-常见问题)

---

## 🚀 快速开始

### 前置要求

- Docker Desktop 已安装并运行
- 至少 4GB 可用内存
- 至少 10GB 可用磁盘空间

### 首次启动（完整流程）

```bash
# 1. 确保 Docker 正常运行
docker ps

# 2. 复制环境配置文件
cp .env.development .env

# 3. 构建 Docker 镜像（首次必须，约5-10分钟）
npm run rebuild:all

# 4. 启动开发环境（包含Mock服务）
npm run dev

# 5. 等待服务启动（约30秒），查看服务状态
npm run ps

# 6. 初始化数据库
npm run db:sync
npm run db:seed

# 7. 访问系统
# 前端: http://localhost:8000
# 后端API: http://localhost:3000
# API文档: http://localhost:3000/api-docs
# Mock服务: http://localhost:3888
```

### 测试账号

| 用户名 | 密码 | 角色 |
|--------|------|------|
| admin | admin123 | 管理员 |
| test | test123 | 普通用户 |

### 日常使用

```bash
# 启动开发环境（包含Mock服务）
npm run dev

# 查看服务状态
npm run ps

# 查看所有日志
npm run logs

# 停止服务
npm run stop

# 重启服务
npm run restart
```

---

## ⚙️ 环境配置

### 环境变量说明

系统通过 `.env` 文件配置所有服务参数。

#### 快速配置

```bash
# 开发环境（推荐）
cp .env.development .env

# 生产环境
cp .env.production .env
```

#### 重要配置项

| 配置项 | 说明 | 开发环境默认值 | 生产环境建议 |
|--------|------|----------------|--------------|
| `NODE_ENV` | 运行环境 | development | production |
| `PORT` | 后端端口 | 3000 | 3000 |
| `FRONTEND_PORT` | 前端端口 | 8000 | 8000 |
| `VITE_API_BASE_URL` | 前端API地址 | http://localhost:3000/api/v1 | 根据实际情况配置 |
| `MYSQL_PASSWORD` | 数据库密码 | aisystem123 | 强密码 |
| `REDIS_PASSWORD` | Redis密码 | redis123 | 强密码 |
| `JWT_SECRET` | JWT密钥 | 固定值 | 随机生成 |

#### 修改配置后

```bash
# 1. 编辑 .env 文件
vim .env

# 2. 重启相关服务
npm run restart

# 或只重启前端（如果只修改了前端配置）
docker compose restart frontend
```

**重要提示**：
- 修改 `VITE_API_BASE_URL` 后只需重启前端容器，**无需重新构建镜像**
- 系统使用运行时配置，环境变量在容器启动时动态生成

---

## 📖 命令说明

### 安装命令

| 命令 | 说明 | 使用场景 |
|------|------|----------|
| `npm run install:all` | 安装所有依赖（前端+后端+Mock） | 首次启动或依赖更新 |
| `npm run install:backend` | 仅安装后端依赖 | 后端依赖更新 |
| `npm run install:frontend` | 仅安装前端依赖 | 前端依赖更新 |
| `npm run install:mock` | 仅安装Mock服务依赖 | Mock服务依赖更新 |

### 启动命令

| 命令 | 说明 | 包含服务 |
|------|------|----------|
| `npm run dev` | 启动开发环境 | 所有服务 + Mock服务 |
| `npm run dev:base` | 仅启动基础服务 | MySQL、Redis、MinIO、Qdrant |
| `npm run dev:mock` | 仅启动Mock服务 | Mock服务 |
| `npm run prod` | 启动生产环境 | 所有服务 + Nginx（不含Mock） |

### 停止和重启命令

| 命令 | 说明 |
|------|------|
| `npm run stop` | 停止所有服务 |
| `npm run restart` | 重启所有服务 |

### 构建命令

| 命令 | 说明 | 使用场景 |
|------|------|----------|
| `npm run rebuild:frontend` | 重新构建前端镜像 | 前端Dockerfile或依赖变更 |
| `npm run rebuild:all` | 重新构建所有镜像 | 大版本更新或完全重置 |

### 日志命令

| 命令 | 说明 |
|------|------|
| `npm run logs` | 查看所有服务日志（实时） |
| `npm run logs:backend` | 查看后端日志（实时） |
| `npm run logs:frontend` | 查看前端日志（实时） |
| `npm run logs:mock` | 查看Mock服务日志（实时） |

### 数据库命令

| 命令 | 说明 | 使用场景 |
|------|------|----------|
| `npm run db:migrate` | 执行数据库迁移 | 生产环境应用变更 |
| `npm run db:sync` | 强制同步数据库 | 开发环境快速修复 |
| `npm run db:seed` | 导入种子数据 | 初始化测试数据 |
| `npm run db:studio` | 打开数据库管理工具 | 可视化管理数据 |

### 状态查看

| 命令 | 说明 |
|------|------|
| `npm run ps` | 查看所有服务运行状态 |

---

## 🔧 服务说明

### 服务列表

| 服务 | 容器名 | 端口 | 说明 | 启动模式 |
|------|--------|------|------|----------|
| MySQL | mysql-service | 3306 | 数据库 | 所有模式 |
| Redis | redis-service | 6379 | 缓存 | 所有模式 |
| MinIO | minio-service | 9000/9001 | 对象存储 | 所有模式 |
| Qdrant | qdrant-service | 6333/6334 | 向量数据库 | 所有模式 |
| Backend | backend-service | 3000 | 后端API | 所有模式 |
| Frontend | frontend-service | 8000 | 前端页面 | 所有模式 |
| Mock | leixin-mock-server | 3888 | Mock服务 | 仅开发模式 |
| Nginx | nginx-service | 80 | 反向代理 | 仅生产模式 |

### 访问地址

#### 开发环境（npm run dev）

- **前端**: http://localhost:8000
- **后端API**: http://localhost:3000
- **API文档**: http://localhost:3000/api-docs
- **Mock服务**: http://localhost:3888
- **MinIO控制台**: http://localhost:9001
  - 用户名: minioadmin
  - 密码: minioadmin123
- **Prisma Studio**: http://localhost:5555 (执行`npm run db:studio`后)

#### 生产环境（npm run prod）

- **系统入口**: http://localhost (通过Nginx统一入口)
- **MinIO控制台**: http://localhost:9001

### Mock服务说明

Mock服务模拟京东、拼多多、淘宝的真实接口，仅在开发环境使用。

**平台凭证**:

| 平台 | app_key | app_secret |
|------|---------|------------|
| 京东 | jd_leixin_2026 | jd_secret_8899 |
| 拼多多 | pdd_leixin_2026 | pdd_secret_7788 |
| 淘宝 | tb_leixin_2026 | tb_secret_6677 |

**接口示例**:
```bash
# 京东订单列表
curl http://localhost:3888/api/jd/orders

# 拼多多订单列表
curl -X POST http://localhost:3888/api/pdd/orders/search

# 淘宝订单列表
curl http://localhost:3888/api/taobao/trades/sold/get
```

详细文档: `dev-tools/快速开始.md`

---

## 💻 开发指南

### 开发模式选择

#### 模式1：完全Docker（推荐新手）

所有服务都在Docker中运行，环境一致。

```bash
# 启动所有服务
npm run dev

# 修改代码后，重启对应服务
docker compose restart backend   # 后端代码修改
docker compose restart frontend  # 前端代码修改

# 查看日志
npm run logs:backend
npm run logs:frontend
```

**优点**: 
- ✅ 环境完全一致
- ✅ 不需要本地安装Node.js
- ✅ 前端支持热更新（已配置volume挂载）

**缺点**: 
- ❌ 后端修改需要重启容器

#### 模式2：混合开发（推荐有经验开发者）

基础服务在Docker，前后端在本地运行。

```bash
# 1. 仅启动基础服务
npm run dev:base

# 2. 本地启动后端（终端1）
cd backend
npm install
npm run dev

# 3. 本地启动前端（终端2）
cd frontend
npm install
npm run dev

# 4. 可选：启动Mock服务
npm run dev:mock
```

**优点**: 
- ✅ 支持热更新
- ✅ 开发效率高
- ✅ 调试方便

**缺点**: 
- ❌ 需要本地安装Node.js 20+
- ❌ 需要配置本地环境变量

### 代码修改流程

#### 前端代码修改

```bash
# 方式1：Docker模式（已支持热更新）
# 直接修改 frontend/src 下的代码，保存后自动生效
# 如果未生效，重启前端容器：
docker compose restart frontend

# 方式2：本地开发模式
# 修改代码后自动热更新，无需任何操作
```

#### 后端代码修改

```bash
# 方式1：Docker模式
# 修改代码后重启容器：
docker compose restart backend

# 方式2：本地开发模式
# 修改代码后自动热更新，无需任何操作
```

#### 数据库模型修改

```bash
# 1. 修改 backend/prisma/schema.prisma

# 2. 生成Prisma客户端
cd backend
npx prisma generate

# 3. 同步到数据库（开发环境）
npm run db:sync

# 或执行迁移（生产环境）
npm run db:migrate
```

### 数据库管理

```bash
# 可视化管理工具
npm run db:studio

# 查看数据库状态
npm run ps | grep mysql

# 连接数据库（命令行）
mysql -h 127.0.0.1 -P 3306 -uaisystem -paisystem123

# 备份数据库
docker exec mysql-service mysqldump -uroot -proot123 aisystem > backup.sql

# 恢复数据库
docker exec -i mysql-service mysql -uroot -proot123 aisystem < backup.sql
```

### 调试技巧

#### 查看容器日志

```bash
# 实时查看所有日志
npm run logs

# 查看特定服务日志
npm run logs:backend
npm run logs:frontend
npm run logs:mock

# 查看最近100行日志
docker compose logs --tail=100 backend
```

#### 进入容器调试

```bash
# 进入后端容器
docker exec -it backend-service sh

# 进入前端容器
docker exec -it frontend-service sh

# 进入数据库容器
docker exec -it mysql-service bash
```

#### 检查网络连接

```bash
# 测试后端API
curl http://localhost:3000/api/v1/health

# 测试前端
curl http://localhost:8000

# 测试Mock服务
curl http://localhost:3888/health

# 容器内测试（服务间通信）
docker exec backend-service curl http://mysql-service:3306
```

---

## 🚀 生产部署

### 部署前准备

#### 1. 修改环境配置

```bash
# 复制生产配置
cp .env.production .env

# 编辑配置文件
vim .env
```

**必须修改的配置**（所有包含 `changeme` 的值）:

- `MYSQL_PASSWORD` - 数据库密码
- `MYSQL_ROOT_PASSWORD` - 数据库root密码
- `REDIS_PASSWORD` - Redis密码
- `MINIO_ROOT_PASSWORD` - MinIO密码
- `QDRANT_API_KEY` - Qdrant API密钥
- `JWT_SECRET` - JWT密钥
- `SMTP_PASSWORD` - 邮件服务密码（如使用）
- `SMS_API_SECRET` - 短信服务密钥（如使用）

#### 2. 生成强密码

```bash
# 生成32位随机密码
openssl rand -base64 32

# 生成64位JWT密钥
openssl rand -hex 32
```

#### 3. 配置API地址

```bash
# 编辑 .env
VITE_API_BASE_URL=/api/v1  # 使用相对路径，通过Nginx代理
```

### 部署步骤

```bash
# 1. 构建生产镜像
npm run rebuild:all

# 2. 启动生产环境（包含Nginx）
npm run prod

# 3. 等待服务启动（约30秒）
npm run ps

# 4. 执行数据库迁移
npm run db:migrate

# 5. 导入初始数据
npm run db:seed

# 6. 查看日志确认启动成功
npm run logs

# 7. 访问系统
# http://localhost (通过Nginx)
```

### 生产环境特点

- ✅ 使用Nginx作为统一入口（端口80）
- ✅ 不启动Mock服务
- ✅ 使用生产配置和强密码
- ✅ 禁用API文档（安全考虑）
- ✅ 启用日志记录和监控
- ✅ 数据持久化（使用Docker volumes）

### 生产环境维护

```bash
# 查看服务状态
npm run ps

# 查看日志
npm run logs

# 重启服务
npm run restart

# 备份数据库
docker exec mysql-service mysqldump -uroot -p<密码> aisystem > backup_$(date +%Y%m%d).sql

# 更新代码后重新部署
git pull
npm run rebuild:all
npm run prod
npm run db:migrate
```

---

## ❓ 常见问题

### Q1: 首次启动失败

**症状**: `npm run dev` 后服务无法启动

**解决**:
```bash
# 1. 检查Docker是否运行
docker ps

# 2. 检查端口是否被占用
lsof -i :3000
lsof -i :8000

# 3. 清理并重新构建
npm run stop
npm run rebuild:all
npm run dev
```

### Q2: 前端页面空白或404

**症状**: 访问 http://localhost:8000 显示空白页面

**解决**:
```bash
# 1. 查看前端日志
npm run logs:frontend

# 2. 检查前端容器状态
npm run ps | grep frontend

# 3. 重启前端服务
docker compose restart frontend

# 4. 如果仍然失败，重新构建前端
npm run rebuild:frontend
```

### Q3: API请求404或CORS错误

**症状**: 前端无法访问后端API

**解决**:
```bash
# 1. 检查后端是否正常运行
curl http://localhost:3000/api/v1/health

# 2. 检查环境变量配置
cat .env | grep VITE_API_BASE_URL

# 3. 检查前端配置是否生效
docker exec frontend-service cat /app/dist/config.js

# 4. 重启前端服务
docker compose restart frontend

# 5. 在浏览器控制台验证配置
# 打开 http://localhost:8000
# 控制台执行: console.log(window.APP_CONFIG)
```

### Q4: 数据库连接失败

**症状**: 后端日志显示数据库连接错误

**解决**:
```bash
# 1. 检查MySQL是否启动
npm run ps | grep mysql

# 2. 查看MySQL日志
docker compose logs mysql

# 3. 等待MySQL完全启动（约30秒）
docker compose logs -f mysql

# 4. 测试数据库连接
mysql -h 127.0.0.1 -P 3306 -uaisystem -paisystem123

# 5. 如果仍然失败，重启MySQL
docker compose restart mysql
```

### Q5: 修改环境变量不生效

**症状**: 修改 `.env` 文件后配置未更新

**解决**:
```bash
# 1. 重启相关服务
npm run restart

# 2. 如果是前端API地址配置
docker compose restart frontend

# 3. 验证配置是否生效
docker exec frontend-service cat /app/dist/config.js
docker exec backend-service env | grep DATABASE_URL
```

### Q6: Mock服务无法访问

**症状**: http://localhost:3888 无法访问

**解决**:
```bash
# 1. 确认使用开发模式启动
npm run dev

# 2. 检查Mock服务状态
npm run ps | grep mock

# 3. 查看Mock服务日志
npm run logs:mock

# 4. 单独启动Mock服务
npm run dev:mock

# 5. 测试Mock服务
curl http://localhost:3888/health
```

### Q7: 容器频繁重启

**症状**: 容器不断重启，无法稳定运行

**解决**:
```bash
# 1. 查看容器日志找出错误原因
npm run logs

# 2. 检查内存和磁盘空间
docker system df
df -h

# 3. 清理Docker缓存
docker system prune -a

# 4. 重新构建
npm run rebuild:all
npm run dev
```

### Q8: 如何完全重置环境

**警告**: 此操作会删除所有数据！

```bash
# 1. 停止并删除所有容器和数据卷
docker compose down -v

# 2. 删除所有镜像（可选）
docker rmi $(docker images -q aisystem-*)

# 3. 重新构建和启动
npm run rebuild:all
npm run dev

# 4. 重新初始化数据库
npm run db:sync
npm run db:seed
```

### Q9: 构建镜像失败

**症状**: `npm run rebuild:all` 失败，显示TypeScript编译错误

**常见错误**:
```
error TS2307: Cannot find module 'xxx' or its corresponding type declarations
```

**解决**:
```bash
# 1. 检查Docker磁盘空间
docker system df

# 2. 清理未使用的镜像和容器
docker system prune -a

# 3. 检查网络连接（npm镜像源）
ping registry.npmmirror.com

# 4. 使用无缓存构建
docker compose build --no-cache

# 5. 单独构建前端或后端
cd frontend && docker build -t aisystem-frontend-v8:latest . --no-cache
cd backend && docker build -t aisystem-backend:latest . --no-cache

# 6. 如果是TypeScript编译错误，检查代码
# 测试文件(.spec.ts)已自动排除，不会影响构建
```

**注意**: 
- 构建时会自动排除测试文件（`*.spec.ts`、`*.test.ts`）
- 如果遇到模块找不到的错误，检查import路径是否正确
- 确保所有依赖都在package.json中声明

### Q10: 数据库迁移冲突

**症状**: `npm run db:migrate` 失败

**解决**:
```bash
# 开发环境：使用强制同步
npm run db:sync

# 生产环境：手动解决冲突
# 1. 进入后端容器
docker exec -it backend-service sh

# 2. 查看迁移状态
npx prisma migrate status

# 3. 解决冲突后重新迁移
npx prisma migrate resolve --applied <migration_name>
npx prisma migrate deploy
```

---

## 📚 相关文档

- **API文档**: http://localhost:3000/api-docs (开发环境)
- **环境变量配置**: `.env.development` / `.env.production`
- **Mock服务文档**: `dev-tools/快速开始.md`
- **Docker配置**: `docker-compose.yml`
- **数据库Schema**: `backend/prisma/schema.prisma`

---

## 🔐 安全建议

### 开发环境

- ✅ 使用简单密码（如 aisystem123）
- ✅ 启用API文档
- ✅ 启用Mock服务
- ✅ 启用详细日志

### 生产环境

- ⚠️ **必须**修改所有默认密码
- ⚠️ **必须**使用强密码（至少16位，包含大小写字母、数字、特殊字符）
- ⚠️ **必须**禁用API文档
- ⚠️ **必须**使用HTTPS（配置SSL证书）
- ⚠️ **建议**定期备份数据库
- ⚠️ **建议**定期更换密码和密钥
- ⚠️ **建议**启用防火墙，只开放必要端口
- ⚠️ **建议**使用Nginx限流和访问控制

---

## 📞 技术支持

如遇到问题，请提供以下信息：

1. **错误信息**: 完整的错误日志或截图
2. **服务状态**: `npm run ps` 的输出
3. **服务日志**: `npm run logs` 的输出
4. **环境配置**: `cat .env | grep -v PASSWORD` 的输出（不要包含密码）
5. **操作步骤**: 详细描述问题复现步骤
6. **系统信息**: 操作系统、Docker版本

---

**文档版本**: 3.0  
**更新时间**: 2026-04-25  
**维护者**: 雷犀科技开发团队

---

## 📖 命令说明

### 安装命令

| 命令 | 说明 | 使用场景 |
|------|------|----------|
| `npm run install:all` | 安装所有依赖 | 首次启动或依赖更新 |
| `npm run install:backend` | 仅安装后端依赖 | 后端依赖更新 |
| `npm run install:frontend` | 仅安装前端依赖 | 前端依赖更新 |
| `npm run install:mock` | 仅安装测试工具依赖 | 测试工具依赖更新 |

### 核心命令

| 命令 | 说明 | 使用场景 |
|------|------|----------|
| `npm run dev` | 启动开发环境 | 日常开发（包含Mock服务） |
| `npm run dev:base` | 仅启动基础服务 | 本地开发前后端 |
| `npm run dev:mock` | 启动Mock服务 | 测试电商平台接口 |
| `npm run prod` | 启动生产环境 | 生产部署（含Nginx） |
| `npm run stop` | 停止基础服务 | 停止开发 |
| `npm run restart` | 重启所有服务 | 配置变更后 |
| `docker compose --profile dev down` | 停止开发环境(含Mock) | 彻底清理开发环境 |

### 日志命令

| 命令 | 说明 |
|------|------|
| `npm run logs` | 查看所有服务日志 |
| `npm run logs:backend` | 查看后端日志 |
| `npm run logs:frontend` | 查看前端日志 |
| `npm run logs:mock` | 查看Mock服务日志 |

### 数据库命令

| 命令 | 说明 |
|------|------|
| `npm run db:migrate` | 执行数据库迁移 (基于SQL记录) | 应用现有变更 |
| `npm run db:sync` | 强制同步数据库 (基于Schema) | 开发环境快速修复/冲突解决 |
| `npm run db:seed` | 导入种子数据 | 初始化测试数据 |
| `npm run db:studio` | 打开数据库管理工具 |

### 状态查看

| 命令 | 说明 |
|------|------|
| `npm run ps` | 查看服务运行状态 |

---

## 🔧 服务说明

### 服务列表

| 服务 | 容器名 | 端口 | 说明 |
|------|--------|------|------|
| MySQL | mysql-service | 3306 | 数据库 |
| Redis | redis-service | 6379 | 缓存 |
| MinIO | minio-service | 9000/9001 | 对象存储 |
| Qdrant | qdrant-service | 6333/6334 | 向量数据库 |
| Backend | backend-service | 3000 | 后端API |
| Frontend | frontend-service | 8000 | 前端页面 |
| Nginx | nginx-service | 80 | 反向代理（仅生产） |
| Mock | leixin-mock-server | 3888 | Mock服务（仅开发） |

### 访问地址

#### 开发环境

- **前端**: http://localhost:8000
- **后端API**: http://localhost:3000
- **API文档**: http://localhost:3000/api-docs
- **Mock服务**: http://localhost:3888
- **MinIO控制台**: http://localhost:9001
- **Prisma Studio**: http://localhost:5555 (执行`npm run db:studio`后)

#### 生产环境

- **系统入口**: http://localhost (通过Nginx)
- **MinIO控制台**: http://localhost:9001

### Mock服务说明

Mock服务模拟京东、拼多多、淘宝的真实接口，用于开发测试。

**平台凭证**:

| 平台 | app_key | app_secret |
|------|---------|------------|
| 京东 | jd_leixin_2026 | jd_secret_8899 |
| 拼多多 | pdd_leixin_2026 | pdd_secret_7788 |
| 淘宝 | tb_leixin_2026 | tb_secret_6677 |

**接口示例**:
```bash
# 京东订单列表
curl http://localhost:3888/api/jd/orders

# 拼多多订单列表
curl -X POST http://localhost:3888/api/pdd/orders/search

# 淘宝订单列表
curl http://localhost:3888/api/taobao/trades/sold/get
```

详细文档: `dev-tools/快速开始.md`

---

## 💻 开发指南

### 开发模式1：完全Docker（推荐新手）

```bash
# 启动所有服务
npm run dev

# 修改代码后，重启对应服务
docker compose restart backend
docker compose restart frontend

# 查看日志
npm run logs:backend
npm run logs:frontend
```

**优点**: 环境一致，不需要本地安装Node.js
**缺点**: 代码修改需要重启容器

### 开发模式2：混合开发（推荐）

```bash
# 1. 仅启动基础服务
npm run dev:base

# 2. 本地启动后端（终端1）
cd backend
npm install
npm run dev

# 3. 本地启动前端（终端2）
cd frontend
npm install
npm run dev

# 4. 启动Mock服务（可选）
npm run dev:mock
```

**优点**: 支持热更新，开发效率高
**缺点**: 需要本地安装Node.js

### 开发模式3：仅本地开发

```bash
# 1. 启动基础服务
npm run dev:base

# 2. 本地启动后端
cd backend
npm run dev

# 3. 本地启动前端
cd frontend
npm run dev
```

**优点**: 完全本地开发，调试方便
**缺点**: 需要配置本地环境

### 数据库管理

```bash
# 查看数据库（可视化工具）
npm run db:studio

# 执行迁移
npm run db:migrate

# 导入测试数据
npm run db:seed

# 连接数据库（命令行）
mysql -h 127.0.0.1 -P 3306 -uaisystem -p
# 密码: changeme_user (开发环境)
```

### 代码修改后

```bash
# 后端代码修改
docker compose restart backend
# 或本地开发时，自动热更新

# 前端代码修改
docker compose restart frontend
# 或本地开发时，自动热更新

# 数据库模型修改
cd backend
npx prisma generate
npm run db:migrate
```

---

## 🚀 生产部署

### 部署前准备

1. **修改环境配置**

```bash
# 复制生产配置
cp .env.production .env

# 修改所有包含 changeme 的密码
vim .env
```

必须修改的配置:
- `MYSQL_PASSWORD`
- `MYSQL_ROOT_PASSWORD`
- `REDIS_PASSWORD`
- `MINIO_ROOT_PASSWORD`
- `QDRANT_API_KEY`
- `JWT_SECRET`

2. **生成强密码**

```bash
# 生成32位随机密码
openssl rand -base64 32

# 生成JWT密钥
openssl rand -hex 32
```

### 部署步骤

```bash
# 1. 启动生产环境（包含Nginx）
npm run prod

# 2. 等待服务启动（约30秒）
npm run ps

# 3. 执行数据库迁移
npm run db:migrate

# 4. 导入种子数据
npm run db:seed

# 5. 查看日志
npm run logs

# 6. 访问系统
# http://localhost (通过Nginx)
```

### 生产环境特点

- ✅ 使用Nginx作为统一入口
- ✅ 不启动Mock服务
- ✅ 使用生产配置
- ✅ 强密码保护
- ✅ 禁用API文档

---

## ❓ 常见问题

### Q1: 前端镜像不存在

**错误**: `Error response from daemon: pull access denied for aisystem-frontend-v8, repository does not exist` 或 `no such image: aisystem-frontend-v8:latest`

**解决**:
```bash
# 构建前端 Docker 镜像
cd frontend
docker build -t aisystem-frontend-v8:latest .
cd ..

# 然后重新启动服务
npm run dev
```

### Q2: 端口被占用

**错误**: `Error: bind: address already in use`

**解决**:
```bash
# 查看端口占用
lsof -i :3000
lsof -i :8000

# 杀死占用进程
kill -9 <PID>

# 或修改 .env 中的端口
PORT=3001
FRONTEND_PORT=8001
```

### Q3: 数据库连接失败

**错误**: `Error: connect ECONNREFUSED`

**解决**:
```bash
# 1. 检查MySQL是否启动
npm run ps

# 2. 查看MySQL日志
docker compose logs mysql

# 3. 等待MySQL完全启动（约30秒）
docker compose logs -f mysql

# 4. 检查.env配置
cat .env | grep MYSQL
```

### Q4: 前端无法访问后端

**错误**: `Network Error` 或 `CORS Error`

**解决**:
```bash
# 1. 检查后端是否启动
curl http://localhost:3000/api/health

# 2. 检查CORS配置（.env）
CORS_ORIGINS=http://localhost:8000,http://localhost:3000

# 3. 重启后端
docker compose restart backend
```

### Q5: Mock服务无法访问

**错误**: `Connection refused`

**解决**:
```bash
# 1. 检查Mock服务是否启动
npm run ps | grep mock

# 2. 启动Mock服务
npm run dev:mock

# 3. 查看Mock服务日志
npm run logs:mock

# 4. 测试Mock服务
curl http://localhost:3888/health
```

### Q6: 数据丢失

**原因**: 删除了数据卷

**恢复**:
```bash
# 1. 重新启动服务
npm run dev

# 2. 执行数据库迁移
npm run db:migrate

# 3. 导入种子数据
npm run db:seed
```

### Q7: 容器无法启动

**解决**:
```bash
# 1. 查看容器状态
npm run ps

# 2. 查看容器日志
docker compose logs <service-name>

# 3. 重新构建容器
docker compose up -d --build

# 4. 清理并重启
docker compose down -v
npm run dev
```

### Q8: 如何重置环境

```bash
# 警告：会删除所有数据！

# 1. 停止并删除所有容器和数据卷
docker compose down -v

# 2. 重新启动
npm run dev

# 3. 执行迁移和种子数据
npm run db:sync
npm run db:seed
```

### Q9: 迁移历史损坏 (P3018 等错误)

**解决方法**: 建议清理损坏的迁移历史并建立新基线。

```bash
# 1. 删除旧迁移文件
rm -rf backend/prisma/migrations

# 2. 强制同步数据库
npm run db:sync

# 3. 如果需要重新开始追踪迁移
# docker exec backend-service npx prisma migrate dev --name init
```

---

## 📚 相关文档

- **环境变量配置**: `环境变量配置说明.md`
- **Mock服务**: `dev-tools/快速开始.md`
- **开发指南**: `开发.md`
- **部署指南**: `部署.md`

---

## 🔐 安全建议

### 开发环境

- ✅ 使用简单密码（changeme）
- ✅ 启用API文档
- ✅ 启用Mock服务
- ✅ 不使用Nginx

### 生产环境

- ⚠️ 必须修改所有密码
- ⚠️ 禁用API文档
- ⚠️ 不启动Mock服务
- ⚠️ 使用Nginx统一入口
- ⚠️ 定期备份数据库
- ⚠️ 定期更换密码

---

## 📞 技术支持

如遇到问题，请提供以下信息：

1. 错误信息截图
2. 服务运行状态: `npm run ps`
3. 服务日志: `npm run logs`
4. 环境配置: `cat .env | grep -v PASSWORD`
5. 操作步骤描述

---

**文档版本**: 2.1
**更新时间**: 2026-04-23
**维护者**: 雷犀科技开发团队

## 前端服务连接问题修复进展

### ✅ 已完成的修复
1. **Docker配置优化** - 修复了前端容器的端口配置和环境变量
2. **环境变量修复** - 将`import.meta.env`改为`process.env`以兼容Umi 4
3. **性能监控修复** - 修复了性能监控在开发环境的网络请求问题
4. **API配置修复** - 修正了前端API基础URL配置

### 🔍 当前问题诊断
**根本问题：React应用初始化失败，导致空白页面**

**症状：**
- HTML结构正常，包含`<div id="root"></div>`和JavaScript文件
- 所有路由返回404（`/login`、`/test`等）
- React应用完全未渲染到DOM中
- 前端服务正常运行，Webpack编译成功

**可能原因：**
1. JavaScript运行时错误阻止了应用启动
2. 某个组件或依赖在初始化时抛出异常
3. 环境变量或配置问题导致应用崩溃

### 🚀 下一步修复计划
1. **简化应用配置** - 移除复杂的组件和配置，使用最小化配置
2. **添加错误捕获** - 在关键位置添加try-catch和错误边界
3. **逐步排查** - 从最简单的页面开始，逐步添加功能
4. **浏览器调试** - 检查浏览器控制台的JavaScript错误信息

### 📋 临时解决方案
用户可以：
1. 直接访问后端API进行测试：`http://localhost:3000/api/v1/`
2. 等待前端修复完成后再进行前端功能测试
3. 使用API工具（如Postman）测试后端功能

### 🔧 技术细节
- 后端服务：✅ 正常运行（端口3000）
- 数据库：✅ 正常连接（MySQL端口3307）
- Redis：✅ 正常连接（端口6379）
- 前端服务：⚠️ 运行但应用未启动（端口8000）

**建议：** 在浏览器中打开 `http://localhost:8000` 并查看控制台错误信息，这将帮助进一步诊断问题。
