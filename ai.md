# 智能客服质检&数据分析平台需求文档

# 1. 文档概述

## 1.1 文档目的

本文档用于明确智能客服质检&数据分析平台的全部功能需求、非功能需求、交互需求、技术需求及落地规范，作为产品设计、开发、测试、上线的核心依据，确保平台满足业务场景需求，符合Ant Design Pro企业中台风格，实现敏感词管理、询单流失分析、高频问题统计、AI+Qdrant智能质检及标签管理等核心功能，且流程闭环、操作高效。

## 1.2 文档范围

本文档覆盖平台所有模块的详细需求，包括数据概览、敏感词管理、询单流失分析、高频问题统计、智能质检（AI+Qdrant）、标签管理、系统管理7大核心模块，同时明确UI风格、交互规范、技术架构、权限控制、操作日志等相关要求，不涉及第三方系统的内部实现细节（仅明确接口对接要求）。

## 1.3 目标用户

- 管理员：负责系统配置、权限分配、数据管理、全局设置，查看平台所有数据及操作日志。

- 质检人员：负责人工审核AI生成的质检结果、标签，处理质检异常，统计质检数据。

- 运营人员：查看数据概览、敏感词统计、询单流失分析、高频问题统计，用于业务优化、话术调整。

- 客服人员（可选）：查看自身相关的质检结果、敏感词命中记录，优化服务话术。

## 1.4 核心目标

- 实现客服对话的智能质检，结合AI与Qdrant向量库，提升质检效率，降低人工成本。

- 精准记录敏感词命中情况，实现敏感词的全流程管理与告警，规避业务风险。

- 全面分析询单流失原因，统计高频问题，为业务优化、话术完善提供数据支撑。

- 实现标签的AI自动生成与人工确认，支持标签管理的高效操作（一键去重等），提升数据分类与检索效率。

- 打造符合Ant Design Pro风格的企业级中台，保证操作流畅、界面统一、体验良好。

# 2. 总体需求

## 2.1 功能总体要求

平台需包含7大核心模块，各模块功能独立且联动，形成“数据采集→自动处理→人工确认→统计分析→数据应用”的完整闭环，具体核心功能如下：

- 数据概览：展示平台核心数据指标，支持多维度可视化查看。

- 敏感词管理：支持敏感词库配置、敏感词命中记录、敏感词统计与告警。

- 询单流失分析：统计询单流失数据，支持多维度筛选，分析流失原因。

- 高频问题统计：支持“整体维度/商品维度”切换，统计TOP高频问题，关联原始对话。

- 智能质检：基于AI+Qdrant向量库，实现对话自动质检、打分、生成标签，支持人工复核。

- 标签管理：支持AI自动生成标签、人工确认/驳回，提供标签分类、编辑、一键去重等功能。

- 系统管理：包含权限管理、操作日志、数据导出、系统配置等功能。

## 2.2 UI风格要求

严格遵循Ant Design Pro企业中台风格，具体要求如下：

- 布局：采用“左侧深色侧边栏+顶部导航栏+主体内容区”的经典中台布局，布局均匀、层次清晰。

- 色彩：主色调采用Ant Design Pro默认主色（#1890ff），辅助色搭配合理，文字与背景对比度符合 accessibility 要求，避免刺眼色彩，突出核心操作按钮。

- 组件：统一使用Ant Design Pro标准组件（表格、按钮、弹窗、筛选框、图表等），组件样式一致，交互逻辑统一，避免自定义组件与标准组件冲突。

- 字体：采用无衬线字体（默认微软雅黑/思源黑体），标题字体加粗，正文字体清晰，字号层级分明（一级标题、二级标题、正文、辅助文字区分明确）。

- 交互：操作反馈及时（如按钮点击、表单提交、筛选切换等），加载状态有提示，错误信息清晰易懂，分页、筛选、排序逻辑符合用户使用习惯。

## 2.3 性能需求

- 响应速度：页面加载时间≤1.5秒，筛选、排序、查询操作响应时间≤1秒，大数据量（万级以上）加载时需提供加载提示，且加载完成时间≤3秒。

- 并发能力：支持同时在线用户≥150人，同时操作（如质检审核、标签确认）≥120人，无卡顿、无数据错乱。

- 数据存储：支持百万级对话数据、十万级标签数据、万级敏感词数据存储，数据查询、检索高效，无明显延迟。

- 稳定性：系统连续运行≥72小时无崩溃、无异常，数据无丢失，AI质检、标签生成等核心功能无异常中断。

## 2.4 兼容性需求

- 浏览器兼容：支持Chrome（最新版、前两个版本）、Edge（最新版、前两个版本）、Firefox（最新版），不支持IE浏览器。

- 终端兼容：支持PC端（分辨率≥1366×768），适配笔记本、台式机，不支持移动端、平板端。

## 2.5 安全需求

- 权限控制：基于角色的权限管理（RBAC），不同角色仅能查看、操作对应权限范围内的功能，无越权访问。

- 数据安全：用户密码加密存储，对话数据、敏感词数据、质检数据等核心数据加密传输，防止数据泄露、篡改。

- 操作安全：所有关键操作（如敏感词删除、标签批量删除、权限修改）需二次确认，操作日志完整记录，可追溯、可审计。

- 防攻击：支持基础的防SQL注入、XSS攻击，避免恶意操作导致系统异常。

# 3. 详细功能需求

## 3.1 数据概览模块

### 3.1.1 核心指标展示

页面顶部以卡片形式展示核心数据指标，支持实时更新，具体指标如下：

- 今日会话总量：展示当日客服对话总条数，点击可跳转至智能质检会话列表（筛选当日会话）。

- 敏感词命中数：展示当日敏感词命中总次数，点击可跳转至敏感词命中记录列表（筛选当日记录）。

- 询单流失率：展示当日询单流失率（流失询单数/总询单数），保留2位小数，点击可跳转至询单流失分析页面。

- 质检不合格率：展示当日自动质检不合格会话占比（不合格会话数/总质检会话数），保留2位小数，点击可跳转至智能质检页面（筛选不合格会话）。

- 待确认标签数：展示当前待人工确认的标签总数，点击可跳转至标签管理页面（筛选待确认标签）。

### 3.1.2 数据可视化图表

核心指标下方展示多维度可视化图表，支持时间筛选（今日/近7天/近30天/自定义），图表采用Ant Design Charts组件，具体如下：

- 会话量趋势图：折线图，展示所选时间范围内每日会话总量变化趋势，支持hover查看具体数值。

- 敏感词命中趋势图：柱状图，展示所选时间范围内每日敏感词命中次数，支持按敏感词分级（红/黄/蓝）筛选展示。

- 询单流失原因分布：饼图，展示所选时间范围内各类流失原因的占比，点击饼图扇区可查看对应原因的详细流失列表。

- 质检结果分布：柱状图，展示所选时间范围内质检合格、不合格、待复核的会话数量分布。

- 高频问题TOP10：横向柱状图，展示所选时间范围内（整体/商品维度可切换）TOP10高频问题，点击可查看对应问题的原始对话。

### 3.1.3 交互要求

- 时间筛选：顶部提供时间筛选下拉框，默认展示近7天数据，支持自定义时间范围（开始日期、结束日期），筛选后图表、核心指标实时更新。

- 图表交互：支持图表缩放、hover显示详细数据，饼图、柱状图支持点击筛选对应数据，跳转至对应模块详情页。

- 刷新功能：提供手动刷新按钮，点击可刷新所有核心指标及图表数据，默认每5分钟自动刷新一次。

## 3.2 敏感词管理模块

### 3.2.1 敏感词库配置

支持敏感词的全生命周期管理，页面分为敏感词库列表和敏感词编辑弹窗，具体功能如下：

- 敏感词列表：展示所有敏感词，包含列：敏感词内容、敏感词分级（红/黄/蓝）、匹配规则、创建时间、创建人、状态（启用/禁用）、操作。

- 敏感词分级：
        

    - 红色：严重违规敏感词（如违法、辱骂、欺诈等），命中后立即告警，质检直接判定不合格。

    - 黄色：一般违规敏感词（如不当表述、违规承诺等），命中后记录，质检扣分。

    - 蓝色：提醒类敏感词（如表述不规范、语气生硬等），命中后记录，不扣分，仅提醒客服优化。

- 匹配规则：支持3种匹配方式，可在添加/编辑敏感词时选择：
        

    - 全词匹配：仅当对话中出现完整敏感词时，判定为命中（如敏感词“欺诈”，仅“欺诈”命中，“欺诈行为”不命中）。

    - 模糊匹配：对话中包含敏感词片段即判定为命中（如敏感词“欺诈”，“欺诈”“欺诈行为”“欺诈性”均命中）。

    - 正则匹配：支持自定义正则表达式，按正则规则匹配命中（如敏感词正则“[0-9]{11}”，匹配11位手机号）。

- 操作功能：
        

    - 新增：点击“新增敏感词”按钮，弹出编辑弹窗，填写敏感词内容、选择分级、匹配规则，提交后新增至敏感词库，创建人自动记录当前登录用户。

    - 编辑：点击列表“编辑”按钮，弹出编辑弹窗，可修改敏感词内容、分级、匹配规则，提交后更新，操作记录存入操作日志。

    - 删除：点击列表“删除”按钮，弹出二次确认弹窗，确认后删除敏感词，不可恢复，操作记录存入操作日志；支持批量删除（勾选多条敏感词，点击“批量删除”按钮）。

    - 启用/禁用：点击列表“启用/禁用”按钮，切换敏感词状态，禁用后敏感词不再参与命中检测，操作记录存入操作日志；支持批量启用/禁用。

    - 导入导出：支持Excel导入敏感词（模板可下载，包含敏感词内容、分级、匹配规则），支持Excel/PDF导出敏感词库列表。

### 3.2.2 敏感词命中记录

记录所有对话中命中敏感词的详细信息，支持多维度筛选、查看，具体功能如下：

- 命中记录列表：包含列：会话ID、敏感词内容、敏感词分级、命中时间、客服姓名、商品名称、命中内容（截取对话中包含敏感词的片段，高亮显示敏感词）、处理状态（未处理/已处理）、操作。

- 筛选功能：支持按时间范围（今日/近7天/近30天/自定义）、敏感词分级、客服、商品、处理状态筛选，支持关键词搜索（搜索会话ID、敏感词内容、客服姓名）。

- 操作功能：
        

    - 查看对话：点击“查看对话”按钮，弹出弹窗，展示该会话的完整对话内容（客服回复、用户提问），高亮显示所有命中的敏感词。

    - 标记处理：点击“标记处理”按钮，弹出处理备注弹窗，填写处理意见（如“已提醒客服优化”“已处罚客服”），提交后处理状态变为“已处理”，操作记录存入操作日志。

    - 导出：支持Excel/PDF导出命中记录列表，可导出筛选后的数据或全部数据。

### 3.2.3 敏感词统计与告警

- 统计功能：展示敏感词命中统计数据，包含：各分级敏感词命中次数、TOP10命中敏感词、各客服敏感词命中次数排行，支持时间范围筛选，以图表（柱状图、饼图）展示。

- 告警功能：
        

    - 实时告警：红色、黄色敏感词命中时，系统顶部弹出告警提示（包含敏感词内容、客服、时间），点击告警提示可跳转至命中记录详情。

    - 消息通知：告警信息同步推送至管理员、质检人员的系统消息中心，支持邮件通知（可在系统配置中开启/关闭）。

    - 告警记录：保留所有告警记录，包含告警时间、敏感词、分级、客服、处理状态，支持筛选、查看。

### 3.2.4 交互要求

- 列表操作：分页显示（默认每页20条，可切换10/20/50/100条），支持排序（按命中时间、命中次数等）。

- 弹窗交互：新增/编辑、查看对话、处理备注等弹窗，点击关闭按钮或空白区域可关闭，提交操作需校验必填项（如敏感词内容不能为空）。

- 导入校验：导入敏感词时，校验敏感词内容不能为空、分级和匹配规则必须选择，不符合规则的条目将提示错误，可下载错误报告。

## 3.3 询单流失分析模块

### 3.3.1 流失数据统计

展示询单流失的核心统计数据，支持多维度筛选，具体如下：

- 核心统计卡片：展示总询单数、流失询单数、流失率、挽回成功数、挽回率，支持时间范围筛选（今日/近7天/近30天/自定义），数据实时更新。

- 流失趋势图：折线图，展示所选时间范围内每日流失询单数、流失率变化趋势，支持hover查看具体数值。

- 商品流失排行：柱状图，展示所选时间范围内各商品的流失询单数排行，支持按商品类目筛选。

- 客服流失排行：柱状图，展示所选时间范围内各客服的询单流失率排行，支持按班组筛选。

### 3.3.2 流失列表管理

展示所有流失询单的详细信息，支持筛选、查看、编辑流失原因，具体功能如下：

- 流失列表：包含列：询单ID、用户姓名、用户手机号（脱敏显示）、商品名称、商品类目、询单时间、流失时间、客服姓名、班组、流失原因、挽回状态（未挽回/挽回中/已挽回）、操作。

- 筛选功能：支持按时间范围（询单时间/流失时间）、商品类目/商品、客服/班组、流失原因、挽回状态筛选，支持关键词搜索（询单ID、用户姓名、商品名称）。

- 流失原因：
        

    - AI自动识别：系统基于对话内容，自动识别流失原因（如“价格过高”“商品无货”“客服响应慢”“用户需求未满足”等）。

    - 人工修正：点击“编辑原因”按钮，可修改流失原因，选择系统预设原因或自定义原因，提交后更新，操作记录存入操作日志。

- 操作功能：
        

    - 查看对话：点击“查看对话”按钮，弹出弹窗，展示该询单的完整对话内容（用户提问、客服回复），辅助分析流失原因。

    - 编辑原因：点击“编辑原因”按钮，弹出编辑弹窗，修改流失原因，支持自定义输入（需校验非空）。

    - 标记挽回状态：点击“标记挽回”按钮，选择挽回状态（挽回中/已挽回），填写挽回备注（如“已联系用户，用户同意重新咨询”），提交后更新状态，操作记录存入操作日志。

    - 导出：支持Excel/PDF导出流失列表，可导出筛选后的数据或全部数据。

### 3.3.3 流失原因分析

对流失原因进行分类统计，为业务优化提供支撑，具体功能如下：

- 流失原因分布：饼图，展示所选时间范围内各类流失原因的占比，点击饼图扇区可查看对应原因的详细流失列表。

- 原因详情分析：点击某类流失原因，展示该原因的详细分析（如“价格过高”原因，可查看涉及的商品、客服、用户画像等）。

- 优化建议（可选）：基于流失原因统计，AI自动生成简单的优化建议（如“价格过高流失较多，建议调整商品定价或推出优惠活动”）。

### 3.3.4 交互要求

- 筛选交互：多维度筛选条件可组合使用，筛选后统计数据、列表、图表实时更新；商品筛选支持级联筛选（先选类目，再选商品）。

- 列表操作：分页显示（默认每页20条），支持排序（按流失时间、流失率等）；挽回状态切换后，列表实时更新。

- 弹窗交互：查看对话、编辑原因、标记挽回等弹窗，操作便捷，关闭后返回当前列表页面，保留筛选条件。

## 3.4 高频问题统计模块

### 3.4.1 高频问题展示

支持“整体维度”和“商品维度”切换，统计TOP高频问题，具体功能如下：

- 维度切换：顶部提供“整体”“商品”两个切换按钮，默认展示整体维度数据；切换至商品维度时，需选择具体商品（支持商品类目级联筛选）。

- 高频问题列表：展示TOP10（可自定义设置TOP数量，如TOP5、TOP20）高频问题，包含列：排名、问题内容、出现次数、出现频率（出现次数/总会话数）、关联商品（仅商品维度显示）、操作。

- 数据筛选：支持按时间范围（今日/近7天/近30天/自定义）筛选，支持按问题类型（如商品咨询、售后咨询、价格咨询等）筛选。

### 3.4.2 高频问题详情

- 查看原始对话：点击列表“查看对话”按钮，弹出弹窗，展示包含该高频问题的所有原始对话，支持分页查看，高亮显示问题关键词。

- 关联标准话术：点击“关联话术”按钮，弹出话术选择弹窗，可选择系统话术库中的标准回复，关联后，客服可在对话中快速调用该话术，提升响应效率；支持新增话术并关联。

- 问题分类：支持为高频问题添加分类标签（如“商品咨询-规格”“售后咨询-退款”），便于分类管理和统计。

### 3.4.3 统计分析

- 问题趋势图：折线图，展示所选时间范围内TOP5高频问题的每日出现次数变化趋势，支持hover查看具体数值。

- 问题类型分布：饼图，展示所选时间范围内各类高频问题的占比，辅助分析用户核心需求。

- 导出功能：支持Excel/PDF导出高频问题列表及统计图表，可导出筛选后的数据或全部数据。

### 3.4.4 交互要求

- 维度切换：切换维度后，列表、图表实时更新；商品维度下，选择商品后立即加载该商品的高频问题数据。

- 列表操作：支持自定义TOP数量（下拉选择5/10/20），分页显示关联的原始对话，支持关键词搜索对话内容。

- 话术关联：关联话术时，支持搜索话术关键词，快速定位所需话术；关联后，可在列表中查看关联的话术内容。

## 3.5 智能质检模块（AI + Qdrant）

### 3.5.1 质检架构说明

基于AI大模型与Qdrant向量库结合，实现对话的全自动质检，核心架构流程如下，确保质检精准度和效率：

1. 数据输入：系统自动采集客服对话文本（语音对话需先进行语音转文本处理）、询单信息、商品信息，作为质检输入数据。

2. 文本预处理：对对话文本进行清洗（去除无效字符、空格、换行）、分词、去停用词，生成标准化文本。

3. 向量化处理：通过嵌入模型（如BERT、Sentence-BERT）将标准化文本转换为向量，同时将系统预设的标准合规话术、违规话术、历史违规样本转换为向量，存入Qdrant向量库。

4. 向量检索：Qdrant向量库对对话向量与标准向量进行相似度匹配，得出相似度得分（0-100分），相似度越高，说明对话越符合（或违规）标准。

5. AI质检打分：AI大模型结合Qdrant相似度得分、语义理解（如客服态度、响应时效、是否解答用户需求），对对话进行综合打分（0-100分），设定合格分数线（可配置，默认80分），得分≥80分为合格，＜80分为不合格。

6. 标签生成：AI大模型基于对话内容，自动生成对应的质检标签（如“违规-敏感词命中”“态度-语气生硬”“响应-时效达标”“需求-未解答”等），推送至人工确认。

7. 结果输出：生成质检报告，包含质检得分、合格状态、违规项、生成的标签、相似度得分，推送至质检人员进行人工复核。

补充说明：Qdrant向量库主要用于存储标准话术、违规样本的向量，实现高效的语义相似度匹配，解决纯规则质检无法应对复杂语义的问题；AI大模型主要用于语义理解、标签生成、综合打分，提升质检的智能化程度。

### 3.5.2 质检配置

支持质检规则、参数的自定义配置，仅管理员可操作，具体功能如下：

- 合格分数线配置：可设置质检合格分数线（0-100分），默认80分，修改后立即生效，影响后续所有质检结果。

- Qdrant向量库配置：支持配置向量库的相似度阈值（0-100），相似度≥阈值判定为匹配成功，可根据实际质检效果调整；支持手动更新Qdrant向量库（同步最新的标准话术、违规样本）。

- 质检维度配置：可勾选需要质检的维度（如敏感词命中、客服态度、响应时效、需求解答、话术规范），未勾选的维度不参与质检打分。

- 标签生成规则配置：可配置AI自动生成标签的类型、范围，如仅生成违规标签、情绪标签，或生成全量标签；可设置标签生成的精准度阈值，提升标签准确性。

### 3.5.3 自动质检结果管理

展示所有自动质检的会话列表，支持筛选、查看、人工复核，具体功能如下：

- 质检列表：包含列：会话ID、客服姓名、班组、商品名称、质检时间、质检得分、合格状态（合格/不合格/待复核）、违规项（若有）、生成标签（待确认/已确认/已驳回）、操作。

- 筛选功能：支持按时间范围、质检得分范围、合格状态、客服/班组、商品、标签状态筛选，支持关键词搜索（会话ID、客服姓名、违规项）。

- 操作功能：
        

    - 查看详情：点击“查看详情”按钮，弹出弹窗，展示会话完整对话、质检得分明细（各维度得分）、Qdrant相似度匹配结果、AI生成的标签、违规项说明。

    - 人工复核：点击“复核”按钮，弹出复核弹窗，可修改质检得分、合格状态、违规项，确认/驳回AI生成的标签，填写复核意见，提交后更新质检结果，操作记录存入操作日志；支持批量复核（勾选多条会话，点击“批量复核”按钮）。

    - 重新质检：点击“重新质检”按钮，系统重新对该会话进行AI+Qdrant质检，覆盖原有质检结果，操作记录存入操作日志。

    - 导出：支持Excel/PDF导出质检列表，可导出筛选后的数据或全部数据，包含质检得分、复核结果等详细信息。

### 3.5.4 质检报表统计

- 核心报表：展示所选时间范围内的质检统计数据，包含：总质检会话数、合格数、不合格数、待复核数、合格率、平均质检得分，支持按客服、班组、商品维度分组统计。

- 报表图表：

    - 质检合格率趋势图：折线图，展示每日质检合格率变化趋势。

    - 各班组质检得分对比图：柱状图，展示各班组的平均质检得分、合格率对比。

    - 违规项分布饼图：展示各类违规项（如敏感词命中、态度不佳、未解答需求）的占比。

- 报表导出：支持Excel/PDF导出质检报表，可导出筛选后的数据或全部数据，支持按维度分组导出。

### 3.5.5 交互要求

- 质检列表：分页显示（默认每页20条），支持排序（按质检时间、质检得分、合格率等）；待复核会话优先展示，可单独筛选待复核会话。

- 复核交互：复核弹窗中，标签确认/驳回支持批量操作（勾选多个标签，一键确认/驳回）；修改质检得分时，需校验得分范围（0-100分），提交后实时更新列表数据。

- 配置交互：修改质检配置后，需二次确认，提交后生效，操作记录存入操作日志；Qdrant向量库更新时，提供加载提示，更新完成后提示成功。

## 3.6 标签管理模块

### 3.6.1 标签生成与确认

支持AI自动生成标签、人工确认/驳回，确保标签准确性，具体功能如下：

- 标签来源：
        

    - AI自动生成：智能质检过程中，AI大模型基于对话内容自动生成标签，标签状态为“待确认”。

    - 人工新增：管理员、质检人员可手动新增标签，用于补充AI未生成的标签，标签状态为“已确认”。

- 标签类型：预设标签类型，支持自定义新增标签类型，具体如下：
        

    - 违规标签：如“敏感词-红色”“态度-生硬”“响应-超时”等。

    - 问题标签：如“咨询-价格”“咨询-规格”“售后-退款”等。

    - 商品标签：如“商品-电子产品”“商品-服装”等。

    - 情绪标签：如“用户-不满”“用户-满意”“客服-急躁”等。

    - 流失标签：如“流失-价格过高”“流失-商品无货”等。

- 标签确认/驳回：
        

    - 待确认标签列表：展示所有AI自动生成、未确认的标签，包含列：标签名称、标签类型、关联会话ID、生成时间、操作。

    - 确认操作：点击“确认”按钮，标签状态变为“已确认”，存入标签库，可用于后续筛选、统计；支持批量确认（勾选多个标签，点击“批量确认”按钮）。

    - 驳回操作：点击“驳回”按钮，弹出驳回备注弹窗，填写驳回原因（如“标签与对话内容不符”），提交后标签状态变为“已驳回”，不存入标签库；支持批量驳回。

### 3.6.2 标签库管理

对已确认的标签进行统一管理，支持编辑、删除、分类、一键去重等功能，具体如下：

- 标签库列表：展示所有已确认的标签，包含列：标签名称、标签类型、创建时间、创建人、关联会话数、状态（启用/禁用）、操作。

- 核心操作：
        

    - 一键去重：点击“一键去重”按钮，系统自动识别名称相同、类型相同的标签，保留一条（创建时间最早的），删除其余重复标签，操作前弹出二次确认弹窗，操作记录存入操作日志；去重后提示去重结果（如“共去重5条标签，保留10条标签”）。

    - 新增标签：点击“新增标签”按钮，弹出编辑弹窗，填写标签名称、选择标签类型，提交后新增至标签库，创建人自动记录当前登录用户，状态为“启用”。

    - 编辑标签：点击“编辑”按钮，弹出编辑弹窗，可修改标签名称、标签类型，提交后更新，操作记录存入操作日志。

    - 删除标签：点击“删除”按钮，弹出二次确认弹窗，确认后删除标签，同时解除该标签与所有会话的关联，操作记录存入操作日志；支持批量删除。

    - 启用/禁用：点击“启用/禁用”按钮，切换标签状态，禁用后标签不再用于筛选、统计，也不再被AI生成，操作记录存入操作日志；支持批量启用/禁用。

    - 标签分类：支持按标签类型筛选标签，可自定义标签类型（新增/编辑/删除标签类型），便于管理。

    - 导出：支持Excel/PDF导出标签库列表，可导出筛选后的数据或全部数据。

### 3.6.3 标签应用

- 筛选功能：所有涉及会话、询单、质检的页面，均支持按标签筛选（如筛选带有“敏感词-红色”标签的会话，筛选带有“流失-价格过高”标签的询单）。

- 统计功能：展示各标签的关联会话数、占比，以图表（柱状图、饼图）展示，支持时间范围筛选，辅助分析标签分布情况。

- 标签反馈：人工确认/驳回标签时，可选择反馈AI（如“标签错误，建议AI优化”），反馈信息同步至AI模型，用于优化AI标签生成的精准度，形成闭环。

### 3.6.4 交互要求

- 列表操作：分页显示（默认每页20条），支持排序（按创建时间、关联会话数等），支持关键词搜索（标签名称、标签类型）。

- 一键去重：去重过程中提供加载提示，去重完成后显示结果弹窗，可查看去重详情（删除的标签列表）。

- 标签确认/驳回：待确认标签列表可与已确认、已驳回标签列表切换，操作便捷；批量操作时，支持全选/取消全选。

## 3.7 系统管理模块

### 3.7.1 权限管理

基于RBAC（角色基础访问控制）模型，实现权限的精细化管理，仅管理员可操作，具体功能如下：

- 角色管理：
       

    - 角色列表：展示所有系统角色，包含列：角色名称、角色描述、创建时间、操作（编辑、删除、分配权限）。

    - 新增角色：点击“新增角色”按钮，填写角色名称、角色描述，提交后新增角色，默认无任何权限。

    - 编辑角色：点击“编辑”按钮，可修改角色名称、角色描述，提交后更新。

    - 删除角色：点击“删除”按钮，弹出二次确认弹窗，确认后删除角色（若角色已分配给用户，需先解除用户与该角色的关联，否则无法删除），操作记录存入操作日志。

    - 分配权限：点击“分配权限”按钮，弹出权限分配弹窗，勾选该角色可访问的模块、操作（如“敏感词管理-查看”“标签管理-编辑”），提交后生效，操作记录存入操作日志。

- 用户管理：
        

    - 用户列表：展示所有系统用户，包含列：用户名、姓名、手机号、邮箱、角色、状态（启用/禁用）、创建时间、操作（编辑、删除、分配角色）。

    - 新增用户：点击“新增用户”按钮，填写用户名、姓名、手机号、邮箱、密码（自动生成或手动输入），分配角色，提交后新增用户，状态默认为“启用”。

    - 编辑用户：点击“编辑”按钮，可修改用户姓名、手机号、邮箱、角色，提交后更新，操作记录存入操作日志；不可修改用户名，密码可重置（重置后生成随机密码，通知用户）。

    - 删除用户：点击“删除”按钮，弹出二次确认弹窗，确认后删除用户，操作记录存入操作日志；删除后用户无法登录系统，历史操作日志保留。

    - 启用/禁用：点击“启用/禁用”按钮，切换用户状态，禁用后用户无法登录系统，操作记录存入操作日志。

### 3.7.2 操作日志

记录所有用户的关键操作，用于追溯、审计，具体功能如下：

- 日志列表：包含列：操作人、操作时间、操作模块、操作内容、操作IP、操作结果（成功/失败）。

- 筛选功能：支持按操作人、操作时间范围、操作模块、操作结果筛选，支持关键词搜索（操作内容、操作IP）。

- 日志详情：点击“查看详情”按钮，弹出弹窗，展示操作的详细信息（如操作前数据、操作后数据、失败原因等）。

- 日志导出：支持Excel/PDF导出操作日志，可导出筛选后的数据或全部数据；日志数据保留90天，90天后自动归档（可配置归档周期）。

### 3.7.3 系统配置

支持系统基础参数的配置，仅管理员可操作，具体功能如下：

- 基础配置：设置系统名称、logo、首页默认展示时间范围、分页默认条数等。

- 告警配置：开启/关闭敏感词命中告警、邮件通知，配置邮件接收人（可多个）。

- 数据配置：设置数据保留周期（如对话数据保留1年、操作日志保留90天）、自动备份频率（如每日凌晨备份）。

- AI配置：配置AI模型接口地址、API密钥、标签生成精准度阈值等。

- Qdrant配置：配置Qdrant向量库的地址、端口、相似度阈值等。

### 3.7.4 数据导出与备份

- 数据导出：支持全局数据导出，可选择导出的模块（如敏感词、询单流失、质检结果、标签）、导出格式（Excel/PDF）、导出范围（全部数据/筛选后数据）。

- 数据备份：支持手动备份和自动备份，备份内容包含所有系统数据（对话、敏感词、质检结果、标签、用户、权限等）；备份文件可下载、删除，备份记录可查看（备份时间、备份人、备份大小）。

### 3.7.5 交互要求

- 权限分配：权限分配弹窗采用树形结构展示模块和操作，勾选便捷，支持全选/取消全选；分配后实时生效。

- 操作日志：分页显示（默认每页20条），支持排序（按操作时间倒序）；日志详情弹窗展示完整的操作信息，便于追溯。

- 系统配置：修改配置后需二次确认，提交后生效，操作记录存入操作日志；部分配置（如AI、Qdrant配置）修改后需重启系统生效，给出明确提示。

# 4. 技术需求

## 4.1 技术栈要求

- 前端：React + Ant Design Pro 5.x 及以上版本，使用TypeScript开发，确保代码规范、可维护；图表使用Ant Design Charts；路由使用React Router；状态管理使用Redux/Context API。

- 后端：推荐Java（Spring Boot 2.x及以上）或Python（Django/Flask），确保接口稳定、高效；支持RESTful API设计规范。

- 向量库：Qdrant 1.5.x及以上版本，支持向量检索、相似度匹配，确保检索效率；支持向量库的动态更新、备份。

- AI模型：


    - 嵌入模型：推荐Sentence-BERT、BERT-base，用于文本向量化，确保向量表征的准确性。

    - 大模型：推荐国产大模型（如字节跳动ERNIE、百度文心一言）或开源大模型（如Llama 3），用于语义理解、标签生成、质检打分，支持API调用或本地化部署。

- 数据库：关系型数据库（MySQL 8.0及以上），用于存储用户、权限、敏感词、质检结果、标签等结构化数据；可选Redis用于缓存高频访问数据（如敏感词库、标签库），提升响应速度。

- 语音转文本（可选）：若涉及语音对话，需集成语音转文本接口（如阿里云ASR、百度ASR），确保转写准确率≥95%。

## 4.2 接口需求

- 内部接口：系统各模块之间的接口，需遵循RESTful API规范，支持GET（查询）、POST（新增）、PUT（修改）、DELETE（删除）请求，返回格式统一（包含状态码、消息、数据）。

- 外部接口：

    - AI模型接口：与AI大模型、嵌入模型对接，实现文本向量化、标签生成、质检打分等功能，接口响应时间≤1秒。

    - Qdrant接口：与Qdrant向量库对接，实现向量的存入、检索、更新、删除，接口响应时间≤500ms。

    - 语音转文本接口（可选）：与语音转文本服务对接，实现语音对话转文本，接口响应时间≤3秒。

    - 邮件接口：用于发送告警通知、密码重置邮件等，支持批量发送。

- 接口安全：所有接口需进行身份验证（如Token验证），防止非法访问；敏感接口（如权限修改、数据删除）需进行二次验证；接口请求频率限制（如每秒最多10次请求），防止恶意攻击。

## 4.3 数据交互要求

- 数据传输：所有核心数据（对话、敏感词、质检结果等）传输过程中需加密（如HTTPS），防止数据泄露、篡改。

- 数据同步：Qdrant向量库需与系统数据库同步，当标准话术、违规样本更新时，需及时同步至Qdrant向量库；AI标签生成后，需及时同步至标签管理模块。

- 数据格式：文本数据统一编码为UTF-8；向量数据格式需符合Qdrant要求；接口返回数据格式为JSON，字段命名规范（小写蛇形命名）。

# 5. 测试需求

## 5.1 测试总则

本章节明确智能客服质检&数据分析平台的测试标准、测试原则、测试环境及测试流程，确保测试工作有序开展，全面验证平台功能、性能、安全等各项需求是否满足设计要求，保障平台上线后稳定、高效运行。

测试核心原则：以需求文档为依据，覆盖所有功能模块及非功能需求，确保测试的全面性、准确性、可重复性；优先测试核心功能（AI质检、敏感词管理、标签管理等），兼顾边缘场景及异常情况；测试过程全程记录，确保问题可追溯、可复现、可闭环。

## 5.2 测试环境

### 5.2.1 硬件环境

- 测试服务器：CPU≥8核，内存≥16GB，硬盘≥500GB（SSD），确保服务器运行稳定，满足大数据量测试需求。

- 测试终端：PC端（分辨率≥1366×768），配置与目标用户日常使用终端一致，确保测试场景贴合实际使用场景。

- 网络环境：带宽≥100Mbps，支持有线、无线两种网络模式，模拟日常办公网络环境，同时支持弱网环境（带宽≤10Mbps）测试。

### 5.2.2 软件环境

- 操作系统：服务器端（Windows Server 2019及以上、CentOS 8及以上），终端端（Windows 10/11、macOS 12及以上）。

- 浏览器：Chrome（最新版、前两个版本）、Edge（最新版、前两个版本）、Firefox（最新版），与兼容性需求保持一致。

- 数据库：MySQL 8.0及以上，Redis（可选，最新稳定版），与技术需求中数据库要求一致。

- 向量库：Qdrant 1.5.x及以上版本，与技术需求中向量库要求一致。

- 测试工具：功能测试（Postman、Selenium）、性能测试（JMeter）、安全测试（OWASP ZAP）、接口测试（Postman）、兼容性测试（BrowserStack），确保测试工具能覆盖各类测试需求。

- 测试数据：模拟真实业务数据（对话数据、敏感词数据、标签数据、用户数据等），数据量覆盖日常场景（万级对话数据、千级标签数据）及大数据量场景（百万级对话数据、十万级标签数据），同时包含异常数据（空数据、非法数据、超长数据）用于异常场景测试。

## 5.3 测试范围

测试范围覆盖平台所有模块及需求，具体包括以下内容：

- 功能测试：覆盖数据概览、敏感词管理、询单流失分析、高频问题统计、智能质检、标签管理、系统管理7大核心模块的所有功能点，验证功能是否符合需求文档描述，操作流程是否顺畅、逻辑是否严谨。

- 性能测试：验证平台响应速度、并发能力、数据存储与检索效率、稳定性等性能指标，是否满足2.3节性能需求。

- 兼容性测试：验证平台在不同浏览器、不同终端、不同网络环境下的运行情况，是否满足2.4节兼容性需求。

- 安全测试：验证平台权限控制、数据安全、操作安全、防攻击能力等，是否满足2.5节安全需求。

- 交互测试：验证平台UI交互、操作反馈、弹窗提示、分页筛选等交互逻辑，是否符合需求文档中的交互要求及Ant Design Pro风格规范。

- 接口测试：验证内部接口、外部接口（AI模型接口、Qdrant接口等）的可用性、响应速度、返回格式、异常处理能力，是否满足4.2节接口需求。

- 异常场景测试：验证平台对异常数据、异常操作、系统故障等场景的处理能力，确保系统不崩溃、数据不丢失，给出清晰的错误提示。

## 5.4 具体测试要求

### 5.4.1 功能测试要求

功能测试需逐模块、逐功能点验证，确保所有功能正常实现，无功能缺失、逻辑错误，具体要求如下：

- 数据概览模块：验证核心指标展示准确性、实时更新功能，图表渲染正确性，时间筛选、图表交互、刷新功能的有效性。

- 敏感词管理模块：验证敏感词库新增、编辑、删除、启用/禁用、导入导出功能，敏感词命中记录的准确性、筛选功能有效性，敏感词统计与告警功能（实时告警、消息通知）的正常性。

- 询单流失分析模块：验证流失数据统计的准确性，流失列表筛选、查看、编辑原因、标记挽回状态功能，流失原因分析图表的正确性，优化建议（可选）的合理性。

- 高频问题统计模块：验证维度切换功能，高频问题列表展示、筛选功能，查看原始对话、关联标准话术、问题分类功能，统计图表的正确性，导出功能的有效性。

- 智能质检模块：验证AI+Qdrant质检流程的完整性，质检配置功能（合格分数线、Qdrant阈值等）的有效性，自动质检结果的准确性，人工复核、重新质检功能，质检报表统计的正确性。

- 标签管理模块：验证AI标签自动生成、人工确认/驳回功能，标签库新增、编辑、删除、一键去重、启用/禁用、导入导出功能，标签筛选、统计、反馈功能的有效性。

- 系统管理模块：验证权限管理（角色管理、用户管理、权限分配）功能，操作日志的完整性、筛选与查看功能，系统配置功能的有效性，数据导出与备份功能的正常性。

- 通用功能：验证分页、排序、搜索、导出（Excel/PDF）、二次确认等通用功能的一致性、有效性，确保不同模块的通用操作逻辑统一。

### 5.4.2 性能测试要求

- 响应速度测试：分别测试页面加载、筛选、排序、查询、接口调用等操作的响应时间，确保页面加载≤1.5秒，筛选/排序/查询≤1秒，大数据量加载≤3秒，接口响应时间符合4.2节要求。

- 并发测试：模拟50人同时在线、20人同时操作（质检审核、标签确认、敏感词编辑等），测试系统是否无卡顿、无数据错乱、无接口超时，并发操作响应时间≤2秒。

- 大数据量测试：导入百万级对话数据、十万级标签数据、万级敏感词数据，测试数据存储稳定性，数据查询、检索、筛选的响应速度，确保无明显延迟，系统运行稳定。

- 稳定性测试：系统连续运行72小时，期间模拟正常业务操作（质检、标签确认、数据查询等），测试系统是否无崩溃、无异常，核心功能（AI质检、标签生成）无中断，数据无丢失。

### 5.4.3 兼容性测试要求

- 浏览器兼容性：在Chrome（最新版、前两个版本）、Edge（最新版、前两个版本）、Firefox（最新版）中，逐一测试所有功能模块，确保页面布局正常、功能正常、交互流畅，无样式错乱、功能失效问题。

- 终端兼容性：在不同配置的PC端（笔记本、台式机）、不同分辨率（1366×768、1920×1080等）下，测试页面布局、功能操作，确保适配正常，无显示异常、操作不便问题。

- 网络兼容性：在正常网络（≥100Mbps）、弱网（≤10Mbps）环境下，测试页面加载、接口调用、数据查询等功能，确保弱网环境下系统不崩溃，给出合理的加载提示，网络恢复后可正常同步数据。

### 5.4.4 安全测试要求

- 权限控制测试：验证不同角色（管理员、质检人员、运营人员、客服人员）的权限范围，确保无越权访问（如运营人员无法访问权限管理功能），权限分配、修改后实时生效。

- 数据安全测试：验证用户密码加密存储，核心数据（对话、敏感词、质检结果）加密传输，测试数据泄露、篡改的可能性，确保数据安全。

- 操作安全测试：验证关键操作（敏感词删除、标签批量删除、权限修改）的二次确认功能，操作日志的完整性、可追溯性，确保操作可审计。

- 防攻击测试：测试系统对SQL注入、XSS攻击、恶意请求的防御能力，确保系统不被恶意攻击导致异常、崩溃。

### 5.4.5 接口测试要求

- 接口可用性：验证所有内部接口、外部接口的可用性，确保接口能正常调用，返回正确的状态码、消息及数据。

- 响应速度：验证接口响应时间，AI模型接口≤1秒，Qdrant接口≤500ms，语音转文本接口（可选）≤3秒，符合技术需求要求。

- 异常处理：测试接口在参数错误、数据为空、接口不可用等异常场景下的处理能力，确保接口不崩溃，返回清晰的错误提示，不泄露系统内部信息。

- 接口安全：验证接口身份验证（Token验证）的有效性，敏感接口二次验证功能，接口请求频率限制功能，防止非法访问、恶意请求。

### 5.4.6 异常场景测试要求

- 数据异常：测试空数据（如空敏感词、空对话）、非法数据（如特殊字符、超长文本）、错误数据（如无效正则表达式、无效标签类型）的处理，确保系统不崩溃，给出明确的错误提示，不影响其他功能正常运行。

- 操作异常：测试重复操作（如重复新增敏感词、重复确认标签）、非法操作（如删除不存在的标签、修改无权限的用户）、中断操作（如导入数据时中断、复核时关闭弹窗）的处理，确保数据不错乱，系统能正常恢复。

- 系统异常：测试服务器中断、网络中断、数据库异常等场景下的系统处理能力，确保系统不崩溃，数据不丢失，网络/服务器恢复后能正常同步数据，给出合理的异常提示。

## 5.5 测试流程

1. 测试准备：明确测试范围、测试用例，搭建测试环境，准备测试数据，配置测试工具，对测试人员进行培训（熟悉需求文档、测试要求）。

2. 测试执行：按照测试用例，逐模块、逐功能点执行测试，记录测试结果（通过/失败），对失败用例进行复现，详细记录问题描述、复现步骤、异常现象。

3. 问题提交与跟踪：将测试中发现的问题提交至缺陷管理工具，明确问题级别（严重、一般、轻微），跟踪问题修复进度，督促开发人员及时修复。

4. 回归测试：开发人员修复问题后，测试人员对修复的问题进行回归测试，验证问题是否已解决，同时检查是否引入新的问题。

5. 测试总结：所有测试用例执行完成，所有严重、一般问题已修复并通过回归测试，编写测试总结报告，明确测试结论（是否满足上线要求）。

## 5.6 测试交付物

测试工作完成后，需提交以下交付物，确保测试过程可追溯、测试结果可验证：

- 测试计划：明确测试目标、测试范围、测试环境、测试流程、测试人员分工、测试时间安排。

- 测试用例：覆盖所有功能点、非功能需求、异常场景，包含用例编号、用例名称、前置条件、操作步骤、预期结果、实际结果。

- 测试报告：包含测试概况、测试范围、测试结果（通过用例数、失败用例数、通过率）、问题统计（按模块、按级别）、测试结论、改进建议。

- 缺陷报告：详细记录所有测试中发现的问题，包含缺陷编号、缺陷描述、复现步骤、异常现象、缺陷级别、修复状态、回归结果。

- 测试日志：记录测试过程中的关键操作、测试时间、测试人员、测试结果，确保测试过程可追溯。

- 需求确认：开发、测试、产品、业务相关人员共同确认需求文档，明确实施范围、技术方案、时间节点，达成一致意见。

- 环境搭建：按照技术需求及测试环境要求，搭建生产环境、测试环境、开发环境，配置数据库、向量库、AI模型接口等，确保环境稳定。

- 开发实现：开发人员按照需求文档、技术需求，分模块进行开发，遵循代码规范，确保代码可维护、可扩展，定期进行代码评审。

- 测试验证：测试人员按照测试需求，执行全面测试，发现问题及时反馈，开发人员修复后进行回归测试，确保所有需求满足。

- 数据迁移（可选）：若有历史客服对话数据、敏感词数据、标签数据等，需进行数据迁移，确保迁移数据准确、完整，不丢失、不错乱。

- 用户培训：对管理员、质检人员、运营人员、客服人员进行系统操作培训，讲解系统功能、操作流程、注意事项，确保用户能熟练使用系统。

- 试运行：系统上线前，进行1-2周的试运行，模拟真实业务场景，监控系统运行状态、性能指标，及时发现并解决试运行中出现的问题。

- 正式上线：试运行无异常后，正式上线系统，切换至生产环境，通知所有用户开始使用，安排专人负责上线后24小时监控。

- 上线前检查：上线前需完成所有测试，所有严重、一般缺陷已修复并通过回归测试；生产环境搭建完成，配置正确；数据迁移（若有）完成，数据准确；用户培训完成，用户能熟练操作。

- 上线时间：选择业务低峰期（如夜间、周末）进行上线，减少对正常业务的影响，上线前需提前通知所有用户，明确上线时间、注意事项。

- 上线监控：上线后24小时安排专人监控系统运行状态，包括响应速度、并发能力、核心功能运行情况、服务器状态等，及时处理上线后出现的异常问题。

- 回滚机制：制定完善的回滚方案，若上线后出现严重问题（如系统崩溃、数据丢失），可快速回滚至上线前状态，减少业务损失，回滚后及时排查问题、修复后重新上线。

## 6.3 后期维护需求

- 日常维护：安排专人负责系统日常维护，监控系统运行状态，处理用户反馈的问题，定期备份数据（每日自动备份，每周手动备份），确保系统稳定运行。

- 版本更新：根据业务需求变化、用户反馈，定期进行系统版本更新，优化功能、修复bug、提升性能，更新前需进行测试，确保更新不影响现有功能。

- 数据维护：定期清理过期数据（按照系统配置的数据保留周期），归档历史数据，优化数据库、向量库性能，确保数据查询、检索效率。

- 技术支持：为用户提供技术支持，解答用户使用过程中的疑问，处理用户遇到的操作问题、系统异常，确保用户正常使用系统。

- 安全维护：定期进行系统安全检测，更新安全补丁，防范安全风险，确保系统数据安全、操作安全。

# 7. 附则

## 7.1 文档修订

本文档根据需求变化、实施过程中的调整，可进行修订，修订后需通知所有相关人员（产品、开发、测试、业务），修订版本需标注修订时间、修订内容、修订人，确保文档的准确性、时效性。

## 7.2 责任分工

- 产品人员：负责需求文档的编写、修订，协调需求确认，跟踪需求落地情况，确保产品符合业务需求。

- 开发人员：按照需求文档、技术需求进行开发，确保代码规范、功能正常，及时修复测试中发现的bug。

- 测试人员：按照测试需求执行测试，记录测试结果，跟踪问题修复，提交测试报告，确保系统满足所有需求。

- 业务人员：提供业务需求，参与需求确认、用户培训、试运行，反馈使用过程中的问题，提出优化建议。

- 管理员：负责系统上线后的日常维护、权限管理、数据备份，处理用户反馈的问题，确保系统稳定运行。

## 7.3 生效日期

本文档自发布之日起生效，作为智能客服质检&数据分析平台产品设计、开发、测试、实施、上线、维护的核心依据。

## 7.4 备注

本文档中所有未明确的细节，由产品人员、开发人员、业务人员共同协商确定，协商结果需补充至本文档，作为文档的补充内容；若本文档与实际业务需求存在冲突，以业务需求为准，同时修订本文档。
> （注：文档部分内容可能由 AI 生成）