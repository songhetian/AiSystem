# 钉钉风格排班系统功能文档（含详细UI设计）

### 2.2.2 颜色规范（重点）

- 鲜艳色系范围：以高饱和度、高亮度颜色为主，避免灰暗、相近色，确保不同班次视觉区分明显（示例色系：#FF4D4F、#FF7A45、#FFC107、#52C41A、#1890FF、#722ED1、#F5222D）；

- 随机颜色规则：系统从预设鲜艳色库中随机抽取，抽取后自动添加50%透明度（可手动修改），避免生成相近色（若随机颜色与已有班次颜色相似度≥80%，自动重新生成）；

- 透明度调节：支持0%-100%调节，默认50%，调节后实时预览颜色效果，确保颜色鲜艳且不刺眼，同时保证文字清晰可见；

- 文字颜色适配：根据班次背景色自动匹配文字颜色（背景色亮度≥60%，文字用黑色；亮度＜60%，文字用白色），确保可读性。

## 2.3 快捷排班模块（印章式）

### 2.3.1 核心功能

- 排班模式切换：支持“进入排班”“退出排班”一键切换，进入后激活印章功能，退出后恢复只读状态，清空当前选中班次；

- 班次选择（印章激活）：左侧班次栏支持通过「班次部门切换」组件筛选班次，点击班次后高亮（添加边框/阴影），视为激活“排班印章”，支持单选、多选班次；仅显示登录用户可见范围内的班次；

- 快捷点选排班：激活印章后，点击日历中任意日期单元格，自动为该员工该日期排选中班次（单选班次直接排班，多选班次弹出班次选择框，选择后排班）；仅可为本部门员工排班，超级管理员可为所有部门员工排班；

- 批量排班：支持框选多个日期、选中单个/多个员工，一键批量排班，支持复制昨日/上周排班、一键粘贴；批量排班时，仅可选择可见范围内的员工及班次；

- 排班编辑：双击已排班单元格，可修改班次、删除排班；单击已排班单元格，可覆盖排班（替换为当前激活的印章班次）；仅可编辑本部门员工的排班记录，超级管理员可编辑所有员工排班；

- 冲突提醒：同一员工同一日期排多个班次、跨天班次时间重叠时，弹出红色警告提示，且无法完成排班（可强制覆盖，需二次确认）。

## 2.4 排班统计模块（新增数据隔离）

### 2.4.1 核心功能

- 日期范围筛选：支持手动选择开始日期、结束日期，默认显示当月日期，支持跨月筛选；

- 员工维度统计：展示指定日期范围内，登录用户可见范围内（本部门/全部部门）每个员工的各班次排班次数、排班时长、休息天数，支持导出Excel；普通用户仅可导出本部门统计数据，超级管理员可导出所有部门数据；

- 班次维度统计：展示指定日期范围内，登录用户可见范围内（本部门/全部部门）各班次的总排班人数、总排班次数、总工时、人均排班次数，支持导出Excel；

- 统计结果筛选：支持按部门、员工、班次筛选统计结果，支持排序（按次数、时长升序/降序）；普通用户仅可筛选本部门相关数据；

- 空排班提醒：统计结果中，未排班的员工/日期标黄提示，支持一键查看未排班明细；仅显示可见范围内的未排班信息。

## 2.5 班次覆盖检查模块（新增数据隔离）

### 2.5.1 核心功能

- 班次选择：支持多选需要检查的班次，可全选、取消全选；仅显示登录用户可见范围内的班次；

- 时间范围设置：支持设置检查的时间范围（精确到小时，如00:00~24:00），可选择单日、多日、整月；

- 覆盖检查：系统自动分析所选班次在指定时间范围内的覆盖情况，输出3种结果：完全覆盖、存在缺口、存在重叠；仅分析可见范围内的排班数据；

- 结果展示：详细显示缺口时间段、重叠时间段、总覆盖时长、缺口时长、重叠时长，支持导出检查报告；普通用户仅可导出本部门检查报告；

- 快速调整：针对缺口/重叠，可直接点击跳转至排班页面，快速补充/修改排班；仅可调整可见范围内的排班数据。

## 2.6 系统设置模块（新增统一设置组件）

- 部门/员工管理：支持添加、编辑、删除部门，导入/导出员工信息，关联员工所属部门；超级管理员可管理所有部门/员工，部门管理员仅可管理本部门员工；

- 休假管理：支持员工请假、休假登记，休假日期自动置灰，不可排班，同步显示在日历中；仅可查看、登记本部门员工休假信息（超级管理员除外）；

- 排班规则设置：可设置排班冲突是否允许强制覆盖、未排班提醒阈值、默认班次颜色透明度；

- 操作日志：记录所有排班修改、班次编辑、统计导出等操作，包含操作人、操作时间、操作内容，支持查询；普通用户仅可查看本人操作日志，超级管理员可查看所有操作日志；

- 导出设置：支持设置导出Excel的字段、格式，可自定义导出文件名；

- 统一设置组件：新增全局统一设置组件，整合所有设置项（班次颜色默认值、透明度默认值、排班规则、数据隔离规则、AI排班参数等），支持集中配置、一键保存，修改后实时生效；组件样式统一、操作便捷，适配PC端、移动端，支持按权限区分设置可见范围（部分高级设置仅超级管理员可见）。

## 2.7 AI智能排班模块（新增）

### 2.7.1 核心功能定位

基于历史排班数据、员工属性、班次规则、业务需求，通过AI算法自动生成合理的排班方案，减少人工排班工作量，降低排班冲突率，同时支持人工调整，适配企业复杂排班场景；数据隔离规则同步生效，AI仅基于登录用户可见范围内的数据生成排班方案。

### 2.7.2 核心功能详情

- 排班参数设置：支持设置AI排班核心参数，通过统一设置组件配置，包括：        
      

- 人员参数：员工岗位、工作时长限制（每日/每周）、休假偏好、技能适配（如部分班次需特定技能员工）、考勤异常规避（如经常迟到员工尽量不排早班）；

- 班次参数：各班次人数需求、班次时间、班次优先级、班次覆盖要求（如高峰时段需多排人）；

- 规则参数：避免连续排班天数（如不可连续排3天早班）、男女员工搭配比例（可选）、部门人员均衡分配、冲突规避规则；

- 算法参数：排班公平性权重（如人均排班次数均衡）、业务匹配度权重（如高峰时段优先安排熟练员工）、历史排班参考权重。

历史数据学习：AI自动学习近3-6个月的历史排班数据，分析排班规律、员工适配性、业务高峰时段，优化排班算法，提升排班合理性；仅学习登录用户可见范围内的历史数据。

智能生成排班方案：        


- 支持按日期范围（单日、整周、整月）生成排班方案，生成后自动检测排班冲突，标注冲突点并给出调整建议；

- 生成多种备选方案（如方案1：优先公平性，方案2：优先业务匹配度），供用户选择；

- 支持针对特定部门、特定员工生成排班方案，适配部门独立排班需求。

人工调整优化：        
      

- AI生成的排班方案可直接编辑，支持单个/批量调整班次、替换员工，调整后AI自动重新检测冲突；

- 支持手动锁定部分排班记录（如核心员工固定班次），AI生成方案时会避开锁定记录；

- 调整后可保存为自定义模板，后续可直接复用。

排班方案预览与导出：生成方案后，支持预览整体排班情况、员工排班明细、班次覆盖情况，支持导出Excel，同步适配数据隔离规则（仅导出可见范围内的方案）。

异常提醒与优化：AI自动识别不合理排班（如员工连续排班超时、高峰时段人员不足），弹出提醒并给出优化建议；支持手动标记不合理项，AI记录并优化后续排班算法。

权限控制：超级管理员可使用AI生成所有部门排班方案，部门管理员仅可生成本部门排班方案，普通用户仅可查看AI生成的本部门排班方案，不可操作生成功能。

# 3. UI详细设计（钉钉风格）

## 3.1 设计原则

- 简洁清晰：复刻钉钉企业级UI风格，布局规整、元素简洁，避免冗余设计，突出核心操作；

- 视觉区分：班次颜色鲜艳且有辨识度，透明度适中，文字与背景对比清晰，确保可读性；

- 易用性：操作流程简洁，按钮位置合理，高频操作（如排班、统计）触手可及；新增的部门切换、统一设置、AI排班组件操作便捷，符合用户使用习惯；

- 一致性：PC端、移动端布局统一，交互逻辑一致，颜色、字体、按钮样式统一；

- 反馈明确：操作后及时给出视觉/文字反馈（如排班成功提示、冲突警告、AI排班生成进度提示），提升用户体验；

- 权限适配：不同权限用户看到的UI组件、数据内容不同，隐藏无权限操作项，避免误操作，同时明确标注当前可见范围（如“当前查看：本部门班次”）。

## 3.2 全局样式规范

### 3.2.1 字体规范

|字体类型|使用场景|字体大小|字体重量|
|---|---|---|---|
|微软雅黑/思源黑体|标题、按钮、正文|标题：16px-20px；正文：12px-14px；按钮：14px|标题：600；正文：400；按钮：500|
### 3.2.2 颜色规范

- 主色调：#1890FF（钉钉蓝），用于按钮、选中状态、标题高亮；

- 辅助色：#FF4D4F（警告红）、#52C41A（成功绿）、#FFC107（提示黄），用于警告、成功、提示反馈；

- 背景色：#FFFFFF（页面背景）、#F5F7FA（卡片/表格背景）、#EEEEEE（分割线/边框）；

- 文字色：#1F2329（主文字）、#666666（次要文字）、#888888（辅助文字）、#FFFFFF（深色背景文字）；

- 班次颜色：预设鲜艳色库（#FF4D4F、#FF7A45、#FFC107、#52C41A、#1890FF、#722ED1、#F5222D、#FF0080、#00B42A、#13C2C2），支持自定义，默认50%透明度；

- 权限区分色：超级管理员专属标识色（#722ED1），部门管理员专属标识色（#13C2C2），用于区分不同权限用户，明确当前可见范围。

### 3.2.3 按钮样式

- 主按钮：背景色#1890FF，文字色#FFFFFF，圆角4px，padding：6px-16px，hover时背景色#096DD9，点击时背景色#005CC5；

- 次要按钮：背景色#FFFFFF，边框1px solid #1890FF，文字色#1890FF，圆角4px，padding：6px-16px，hover时背景色#E6F7FF；

- 警告按钮：背景色#FF4D4F，文字色#FFFFFF，圆角4px，padding：6px-16px，hover时背景色#F5222D；

- 小按钮：字体12px，padding：4px-8px，圆角3px，样式与对应类型大按钮一致（如小主按钮、小次要按钮）；

- 排班模式按钮：进入排班（主按钮）、退出排班（警告按钮），并排显示在顶部操作区中间；

- AI排班按钮：主按钮，图标+文字（“AI智能排班”），位于顶部操作区右侧，与统计、导出按钮并列；

- 部门切换按钮：次要按钮，下拉选择式，显示当前选中部门（如“本部门”“全部部门”），位于左侧班次栏顶部，与班次列表标题并列。

### 3.2.4 卡片/表格样式

- 卡片：背景色#FFFFFF，边框1px solid #EEEEEE，圆角6px，阴影0 2px 8px rgba(0,0,0,0.06)，padding：16px；AI排班方案卡片添加轻微渐变背景，突出区分；

- 表格：边框-collapse，表头背景色#F5F7FA，表头文字色#1F2329，行高36px，奇数行背景色#FFFFFF，偶数行背景色#F9FAFB，hover时行背景色#E6F7FF，边框1px solid #EEEEEE；权限区分列（如部门列）添加轻微背景色，便于识别；

- 分割线：高度1px，背景色#EEEEEE，用于模块、区域分割。

## 3.3 页面详细布局（PC端）

整体布局分为4个固定区域：顶部操作区、左侧班次栏、中间日历排班区、底部统计区，布局比例合理，适配1920*1080及以上分辨率，支持自适应调整；新增部门切换、AI排班、统一设置相关UI组件，确保布局连贯、操作便捷。

### 3.3.1 顶部操作区（高度60px，固定顶部，不随滚动）

- 左侧（占比40%）：       
      

- 部门筛选：下拉选择框，默认显示“全部部门”，支持搜索部门，样式为圆角4px，边框1px solid #EEEEEE，padding：4px-8px；普通用户仅可选择本部门及上级部门；

- 员工筛选：下拉选择框，默认显示“全部员工”，支持搜索员工，可多选，样式与部门筛选一致；仅显示当前部门筛选范围内的员工；

- 月份切换：左右箭头按钮（小按钮）+ 当前月份显示，点击箭头切换月份，点击月份可弹出日历选择器，快速切换至指定月份。

中间（占比20%）：        
      

- 排班模式按钮：“进入排班”（主按钮）、“退出排班”（警告按钮），并排显示，间距8px，按钮宽度100px；

- 当前模式提示：进入排班后，显示“当前处于排班模式，请选择班次并点击日期”（文字色#1890FF，字体12px），退出后隐藏；同时显示当前可见范围（如“当前可见：本部门班次”）。

右侧（占比40%）：        
      

- AI排班按钮：主按钮，图标+文字（“AI智能排班”），点击弹出AI排班参数设置弹窗；

- 统计按钮：次要按钮，图标+文字（“统计”），点击弹出统计面板；

- 导出按钮：次要按钮，图标+文字（“导出”），点击弹出导出选项（导出排班表、导出统计结果）；

- 设置按钮：小按钮，图标（齿轮），点击弹出系统设置菜单，包含“统一设置”选项；

- 班次管理按钮：次要按钮，文字（“班次管理”），点击跳转至班次管理页面。

### 3.3.2 左侧班次栏（宽度200px，固定左侧，不随滚动）

- 顶部：班次部门切换组件（下拉选择按钮）+ 班次列表标题“班次列表”（字体16px，重量600，margin-bottom：12px）；切换按钮显示当前选中项（如“本部门班次”），下拉选项根据用户权限显示（超级管理员显示“全部班次”“指定部门班次”，普通用户显示“本部门班次”）；

- 班次添加按钮：小主按钮，图标+文字（“添加班次”），宽度100%，margin-bottom：12px；普通用户仅可添加本部门班次；

- 班次列表：       
      

- 每个班次为一个卡片（宽度100%，高度50px，margin-bottom：8px，圆角4px，display：flex，align-items：center，padding：8px）；部门班次卡片右上角添加部门标识（小标签，显示部门名称，字体10px）；

- 班次卡片内容：左侧颜色块（宽度20px，高度20px，圆角3px，margin-right：8px，显示班次颜色及透明度）、中间班次信息（班次名称+时间，字体14px，文字色适配背景色）、右侧操作按钮（编辑、删除，小按钮，默认隐藏，hover班次卡片时显示）；

- 选中状态：班次卡片添加2px solid #1890FF边框，背景色添加10%#1890FF透明度，突出显示；

- 多选状态：选中的班次卡片均显示选中样式，且顶部显示“已选中X个班次”提示（字体12px，文字色#1890FF）。

颜色预览：每个班次卡片的颜色块实时显示当前班次的颜色及透明度，编辑班次时，颜色块同步更新。

### 3.3.3 中间日历排班区（占比70%，可滚动）

- 布局：表格布局，行=员工，列=日期（每周一行，列头为星期+日期，如“一 1”）；员工行添加部门标识（字体12px，文字色#888），便于区分不同部门员工；

- 列头样式：高度40px，背景色#F5F7FA，文字色#1F2329，字体14px，text-align：center，边框1px solid #EEEEEE；

- 行头样式：宽度120px，背景色#FFFFFF，文字色#1F2329，字体14px，text-align：left，padding-left：12px，边框1px solid #EEEEEE，固定左侧，不随水平滚动；

- 单元格样式（日期单元格）：        
     

- 基础样式：宽度100px，高度60px，border：1px solid #EEEEEE，text-align：center，vertical-align：middle，cursor：pointer，背景色#FFFFFF；

- 未排班：空白，hover时背景色#F9F9F9；

- 已排班：显示班次名称（字体12px），背景色为班次颜色（带透明度），文字色适配背景色，hover时边框1px solid #1890FF；部门班次添加轻微边框区分；

- 休假/禁用：背景色#F5F5F5，文字色#888888，显示“休假”字样，cursor：not-allowed，不可点击排班；

- 排班模式下：单元格边框变为1px dashed #1890FF，hover时背景色#E6F7FF，突出可操作状态；

- AI排班标识：AI生成的排班单元格右下角添加小图标（机器人图标），区分人工排班与AI排班。

批量操作提示：框选多个单元格时，弹出提示“已选中X个日期，点击确认排班”，确认后为选中单元格统一排当前激活班次。

### 3.3.4 底部统计区（高度200px，固定底部，可折叠）

- 折叠/展开按钮：小按钮，图标+文字（“展开统计”/“收起统计”），位于统计区顶部左侧；

- Tab切换：“员工统计”“班次统计”两个Tab，默认显示“员工统计”，Tab样式为下划线切换（选中Tab文字色#1890FF，下划线为#1890FF，未选中文字色#666666）；

- 日期范围筛选：位于Tab下方，开始日期+结束日期选择框（样式与顶部筛选一致）+“查询”按钮（小主按钮）；

- 统计表格：        
      

- 员工统计表格：列=员工姓名、部门、早班次数、晚班次数、夜班次数、休息天数、总工时，支持排序、筛选；部门列显示员工所属部门，便于区分；

- 班次统计表格：列=班次名称、所属部门、颜色预览、总排班人数、总排班次数、总工时、人均次数，支持排序、筛选；

- 表格滚动：表格内容超出高度时，可垂直滚动，表头固定。

### 3.3.5 班次管理页面布局（新增部门相关UI）

- 顶部：标题“班次管理”（18px，600）+ 新增班次按钮（主按钮）+ 批量删除按钮（警告按钮）+ 部门筛选下拉框（仅超级管理员可见）；

- 中间：班次列表表格，列=班次名称、所属部门、班次时间、颜色预览、透明度、备注、操作（编辑、删除）；所属部门列显示班次关联的部门，支持按部门筛选；

- 颜色预览列：显示班次颜色块（带透明度），点击颜色块可弹出取色器，直接修改颜色；

- 透明度列：滑块组件（0%-100%），拖动滑块可实时调整透明度，同步更新颜色块；

- 编辑弹窗：弹出式卡片（宽度400px），包含表单：班次名称（输入框）、所属部门（下拉选择框，可多选，普通用户仅可选择本部门）、开始时间（时间选择器）、结束时间（时间选择器）、颜色设置（取色器+随机按钮）、透明度（滑块）、备注（文本框），底部“确定”“取消”按钮。

### 3.3.6 班次覆盖检查页面布局（新增数据隔离UI）

- 顶部：标题“班次覆盖检查”（18px，600）+ 部门筛选下拉框（仅超级管理员可见）；

- 筛选区：        
      

- 班次选择：多选框（显示班次名称+颜色块+所属部门），支持全选、取消全选；仅显示当前部门筛选范围内的班次；

- 时间范围：开始日期、结束日期、开始时间、结束时间选择器；

- 检查按钮：主按钮（“开始检查”），点击后执行检查并显示结果。

结果展示区：

- 检查结果汇总：显示“检查范围”“所选班次”“覆盖状态”“总覆盖时长”“缺口时长”“重叠时长”“所属部门”；

- 详细结果：分“缺口明细”“重叠明细”两个Tab，表格展示具体时间段、涉及班次、涉及员工、所属部门；

- 操作按钮：“快速调整”（跳转至排班页面）、“导出报告”（导出检查结果Excel）。

### 3.3.7 统一设置组件布局（新增）

- 打开方式：点击顶部操作区“设置”按钮，选择“统一设置”，弹出设置面板（宽度600px，高度500px，可滚动）；

- 面板布局：分为5个Tab页，分别为“排班规则设置”“班次颜色设置”“数据隔离设置”“AI排班参数设置”“导出设置”，默认显示“排班规则设置”；

- 各Tab内容：        
      

- 排班规则设置：冲突处理规则、未排班提醒阈值、批量排班默认规则等，以开关、输入框、下拉框形式呈现；

- 班次颜色设置：默认颜色、默认透明度、鲜艳色库管理（可添加/删除颜色）；

- 数据隔离设置：权限可见范围规则（如部门用户是否可见上级部门班次）、数据导出权限控制；仅超级管理员可编辑；

- AI排班参数设置：人员参数、班次参数、算法参数，以滑块、下拉框、输入框形式呈现，支持一键重置默认参数；

- 导出设置：导出字段选择、文件名模板、导出格式设置。

操作按钮：面板底部“保存设置”（主按钮）、“重置默认”（次要按钮）、“取消”（次要按钮），保存后实时生效，弹出“设置保存成功”提示。

### 3.3.8 AI智能排班页面/弹窗布局（新增）

- 打开方式：点击顶部“AI智能排班”按钮，弹出AI排班参数设置弹窗（宽度500px，可滚动）；

- 参数设置区：

- 基础设置：日期范围选择（单日/整周/整月）、所属部门选择（仅超级管理员可选择多个部门）；

- 人员参数：员工工作时长限制、技能适配选择、考勤异常规避开关；

- 班次参数：各班次人数需求、班次优先级设置；

- 规则参数：连续排班限制、人员均衡分配开关等；

- 方案设置：生成备选方案数量（1-3个）、优先权重选择（公平性/业务匹配度）。

操作按钮：“生成排班方案”（主按钮）、“重置参数”（次要按钮）、“取消”（次要按钮）；

方案生成进度：点击生成后，显示进度条（0%-100%），弹出“正在生成排班方案，请稍候”提示；

方案预览区：生成完成后，弹窗切换至方案预览页面，显示方案名称、生成时间、备选方案切换Tab，表格展示排班明细，支持“编辑方案”“导出方案”“应用方案”（将方案同步至日历排班区）按钮。

## 3.4 移动端UI设计（适配钉钉移动端风格）

- 布局调整：顶部操作区简化（只保留月份切换、排班模式按钮、AI排班按钮、统计按钮），左侧班次栏改为底部弹出抽屉（点击“班次”按钮弹出），中间日历排班区自适应屏幕宽度，可左右滑动；

- 班次选择：抽屉式班次列表，顶部添加班次部门切换组件（下拉选择），每个班次卡片简化（只显示颜色块+班次名称+部门标识），支持单选、多选，选中后关闭抽屉，激活印章功能；

- 排班操作：点击日期单元格排班，双击删除，长按弹出批量排班选项（连续日期、整周排班）；

- 统计区：改为弹出式面板（点击统计按钮弹出），支持切换员工/班次统计，可滑动查看统计表格；

- 统一设置组件：适配移动端，Tab页改为横向滑动，设置项简化为卡片式，操作按钮放大，确保触摸便捷；

- AI智能排班：弹窗适配移动端，参数设置项分区域展示，进度条清晰可见，方案预览支持上下滑动，操作按钮固定在底部；

- 班次管理/覆盖检查：适配移动端表单，按钮、输入框放大，确保触摸便捷，颜色取色器适配移动端操作。

## 3.5 交互细节设计（新增功能补充）

- 班次颜色设置：点击取色器选择颜色，点击“随机”按钮生成随机鲜艳色，透明度滑块实时预览效果，确认后同步更新所有相关显示（班次列表、日历单元格）；

- 排班反馈：排班成功后，单元格颜色渐变显示，弹出“排班成功”提示（3秒后自动消失）；冲突时弹出红色警告弹窗，提示冲突原因及处理选项；

- 滚动交互：中间日历排班区支持水平、垂直滚动，行头、列头固定，滚动时不偏移；

- 编辑交互：编辑班次时，实时同步预览颜色、时间、所属部门变化；删除班次时，提示“删除后该班次所有排班记录将同步删除，是否确认？”；

- 统计交互：筛选条件变化后，统计表格实时刷新；点击统计表格中的员工/班次，可跳转至对应排班明细页面；

- 部门切换交互：切换班次部门时，班次列表实时刷新，仅显示对应部门班次；切换部门筛选时，员工列表、统计数据同步刷新；

- 统一设置交互：修改设置项后，实时预览效果，保存后弹出成功提示，部分设置项（如数据隔离规则）需重启生效的，明确提示用户；

- AI排班交互：生成方案时显示进度条，生成失败（如参数不合理）弹出提示并给出调整建议；编辑AI方案时，实时检测冲突并提醒；应用方案后，日历排班区同步更新，弹出“方案应用成功”提示；

- 权限交互：无权限操作时（如普通用户查看其他部门班次），按钮置灰并弹出提示“您无此操作权限”，隐藏无权限的设置项、筛选选项。

# 4. 交互流程设计

## 4.1 快捷排班流程（新增部门切换、数据隔离）

1. 用户点击顶部“进入排班”按钮，进入排班模式，页面提示“当前处于排班模式，请选择班次并点击日期”及当前可见范围；

2. 用户在左侧班次栏通过“班次部门切换”组件筛选所需班次（如本部门班次），点击单个/多个班次，选中的班次卡片高亮，激活“排班印章”；

3. 用户点击中间日历排班区的日期单元格：
      

4. 单选班次：直接为该员工该日期排选中班次，单元格显示班次颜色及名称，弹出“排班成功”提示；仅可为本部门员工排班；

5. 多选班次：弹出班次选择框，用户选择其中一个班次，完成排班。

6. 用户可框选多个日期单元格，点击确认，为选中单元格统一排当前激活班次；

7. 排班完成后，用户点击“退出排班”按钮，退出排班模式，清空选中班次，日历单元格恢复只读状态。

## 4.2 班次添加/编辑流程（新增部门选择）

1. 用户点击左侧班次栏“添加班次”按钮，或点击班次卡片的“编辑”按钮，弹出编辑弹窗；

2. 用户填写班次名称、选择所属部门（普通用户仅可选择本部门）、选择起止时间，设置班次颜色：       
      

3. 自定义颜色：点击取色器，选择鲜艳颜色，拖动滑块调整透明度；

4. 随机颜色：点击“随机”按钮，系统自动生成鲜艳色，默认50%透明度，可手动调整。

5. 用户填写备注（可选），点击“确定”按钮，保存班次信息；

6. 系统同步更新班次列表、日历中已排该班次的单元格颜色，弹出“保存成功”提示；仅对应部门用户可看到该班次。

## 4.3 班次覆盖检查流程（新增数据隔离）

1. 用户点击顶部“统计”按钮，选择“班次覆盖检查”，进入检查页面；

2. 超级管理员可选择检查的部门，普通用户默认检查本部门；用户多选需要检查的班次（仅可见范围内），设置检查的日期范围、时间范围；

3. 用户点击“开始检查”按钮，系统自动分析可见范围内的排班数据，生成覆盖检查结果；

4. 系统展示检查结果（完全覆盖/缺口/重叠），详细显示缺口、重叠明细及所属部门；

5. 用户可点击“快速调整”按钮，跳转至排班页面，补充/修改可见范围内的排班；或点击“导出报告”，导出检查结果。

## 4.4 排班统计流程（新增数据隔离）

1. 用户点击顶部“统计”按钮，弹出统计面板（或直接显示底部统计区）；

2. 用户选择统计Tab（员工统计/班次统计），设置统计日期范围，超级管理员可选择统计部门，普通用户默认统计本部门；点击“查询”；

3. 系统加载并显示可见范围内的统计数据，用户可按需求筛选、排序；

4. 用户点击“导出”按钮，选择导出类型（排班表/统计结果），仅可导出可见范围内的数据。

## 4.5 统一设置流程（新增）

1. 用户点击顶部“设置”按钮，选择“统一设置”，弹出统一设置面板；

2. 用户切换Tab页，根据需求修改设置项（如排班规则、AI排班参数、数据隔离规则）；

3. 用户修改完成后，点击“保存设置”按钮，系统保存设置并弹出“设置保存成功”提示；部分设置项需重启生效的，系统给出明确提示；

4. 若用户需要恢复默认设置，点击“重置默认”按钮，确认后恢复所有设置为默认值；

5. 用户点击“取消”按钮，放弃修改，关闭设置面板。

## 4.6 AI智能排班流程（新增）

1. 用户点击顶部“AI智能排班”按钮，弹出AI排班参数设置弹窗；

2. 用户设置基础参数（日期范围、所属部门）、人员参数、班次参数、规则参数及方案设置；

3. 用户点击“生成排班方案”按钮，系统显示进度条，开始生成方案；

4. 方案生成完成后，弹窗切换至方案预览页面，用户可查看不同备选方案的排班明细；

5. 用户可点击“编辑方案”，调整排班细节（替换员工、修改班次），编辑后系统自动检测冲突并提醒；

6. 用户确认方案后，点击“应用方案”按钮，系统将方案同步至日历排班区，弹出“方案应用成功”提示；

7. 用户可点击“导出方案”按钮，导出当前选中的排班方案Excel。

# 5. 异常处理

## 5.1 异常处理原则

遵循“提前预防、及时提醒、清晰指引、数据安全”四大原则，所有异常均需给出明确的文字提示，区分警告、错误、提示三类反馈，适配权限隔离规则，确保异常处理不泄露跨部门数据，同时提供可操作的解决指引，降低用户操作成本，避免数据丢失或误操作。

## 5.2 权限类异常处理

### 5.2.1 核心异常场景

- 无权限查看跨部门数据：普通用户/部门管理员尝试查看、筛选其他部门的班次、排班记录、统计数据时，系统自动隐藏相关内容，弹出警告提示“您无权限查看该部门相关数据，请切换至本部门查看”，同时将部门筛选框锁定为当前用户所属部门，不可切换至其他部门。

- 无权限操作跨部门排班：普通用户尝试为其他部门员工排班、编辑其他部门员工排班记录时，操作按钮置灰，点击后弹出警告提示“您无权限为该部门员工排班，请仅操作本部门员工”，日历单元格不可点击（置灰显示），无法执行排班操作。

- 无权限修改高级设置：普通用户/部门管理员尝试修改统一设置中的高级选项（如数据隔离规则、全部门AI排班参数）时，相关设置项置灰，鼠标悬浮时弹出提示“您无此设置权限，仅超级管理员可编辑”，无法进行修改操作。

- 无权限生成全部门AI方案：部门管理员尝试生成其他部门或全部门的AI排班方案时，所属部门选择框仅显示本部门，无法选择其他部门，同时弹出提示“您仅可生成本部门排班方案”。

- 权限过期/失效：用户权限过期（如部门管理员被撤销权限）或登录状态失效时，系统自动跳转至登录页面，弹出提示“您的权限已失效或登录状态过期，请重新登录并确认权限”，确保数据安全。

### 5.2.2 处理逻辑

所有权限类异常均优先隐藏无权限内容，避免用户看到敏感数据，提示信息简洁明确，同时提供指引（如“切换至本部门”“联系超级管理员获取权限”）；权限校验贯穿操作全流程，从页面加载、筛选、操作到数据导出，均进行权限判断，确保数据隔离规则落地。

## 5.3 操作类异常处理

### 5.3.1 排班相关异常

- 排班冲突异常：同一员工同一日期排多个班次、跨天班次时间重叠，或排班时间与员工休假时间冲突时，弹出红色警告弹窗，提示“排班冲突：该员工该日期已排【XX班次】/处于休假状态，不可重复排班”，提供“取消”“强制覆盖”两个选项，强制覆盖需二次确认（提示“强制覆盖将删除原有排班记录，是否确认？”）。

- 无选中班次排班：用户进入排班模式后，未选中任何班次即点击日历单元格，弹出提示“请先在左侧班次栏选择需排班的班次”，同时左侧班次栏轻微闪烁，引导用户选择班次。

- 批量排班失败：框选单元格时未选中员工/日期、选中的员工不在可见范围内，或选中的班次已被删除，弹出提示“批量排班失败：请确认选中有效员工、日期及可见范围内的班次”，同时高亮显示无效选中项（如灰色单元格、已删除班次标红），引导用户重新选择。

- 覆盖排班误操作：用户单击已排班单元格进行覆盖时，弹出提示“确认覆盖该员工该日期的原有排班记录吗？”，提供“确认”“取消”选项，避免误操作导致原有排班丢失；若用户连续覆盖多个单元格，每3个单元格弹出一次确认提示，兼顾操作效率与误操作预防。

- 跨天班次设置异常：设置跨天班次（如23:00-次日02:00）时，未正确选择起止日期（如仅选择单日），弹出警告提示“跨天班次需选择正确的起止日期，结束日期需晚于开始日期”，自动定位至日期选择器，方便用户调整。
> （注：文档部分内容可能由 AI 生成）