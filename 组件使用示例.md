# 组件使用示例

本文档提供所有自定义组件的使用示例和最佳实践。

---

## 一、基础UI组件

### 1. Card - 卡片组件

```tsx
import { Card } from '@/components/ui';

// 基础用法
<Card title="基础卡片">
  这是卡片内容
</Card>

// 毛玻璃效果
<Card title="毛玻璃卡片" glass shadow="xl">
  这是带毛玻璃效果的卡片
</Card>

// 可悬停卡片
<Card title="可悬停卡片" hoverable onClick={() => console.log('clicked')}>
  鼠标悬停时会有提升效果
</Card>

// 带额外内容的卡片
<Card
  title="带操作的卡片"
  extra={<Button type="link">更多</Button>}
>
  卡片内容
</Card>
```

### 2. Button - 按钮组件

```tsx
import { Button } from '@/components/ui';
import { PlusOutlined } from '@ant-design/icons';

// 不同类型
<Button type="primary">主要按钮</Button>
<Button type="secondary">次要按钮</Button>
<Button type="success">成功按钮</Button>
<Button type="danger">危险按钮</Button>
<Button type="ghost">幽灵按钮</Button>

// 不同尺寸
<Button size="sm">小按钮</Button>
<Button size="base">默认按钮</Button>
<Button size="lg">大按钮</Button>

// 带图标
<Button type="primary" icon={<PlusOutlined />}>
  新增
</Button>

// 加载状态
<Button type="primary" loading>
  加载中
</Button>

// 块级按钮
<Button type="primary" block>
  块级按钮
</Button>
```

### 3. Table - 表格组件

```tsx
import { Table } from '@/components/ui';

const columns = [
  {
    title: '姓名',
    dataIndex: 'name',
    key: 'name',
  },
  {
    title: '年龄',
    dataIndex: 'age',
    key: 'age',
    sorter: true,
  },
  {
    title: '操作',
    key: 'action',
    render: (_, record) => (
      <Button type="link">编辑</Button>
    ),
  },
];

const dataSource = [
  { key: '1', name: '张三', age: 32 },
  { key: '2', name: '李四', age: 28 },
];

// 基础用法
<Table columns={columns} dataSource={dataSource} />

// 毛玻璃效果 + 紧凑模式
<Table
  columns={columns}
  dataSource={dataSource}
  glass
  density="compact"
  striped
/>

// 带分页
<Table
  columns={columns}
  dataSource={dataSource}
  pagination={{
    current: 1,
    pageSize: 10,
    total: 100,
    onChange: (page, pageSize) => console.log(page, pageSize),
  }}
/>
```

### 4. Form - 表单组件

```tsx
import { Form, Button } from '@/components/ui';
import { Input, Select } from 'antd';

const [form] = Form.useForm();

<Form
  form={form}
  layout="vertical"
  initialValues={{ name: '', age: '' }}
  onFinish={(values) => console.log(values)}
>
  <Form.Item name="name" label="姓名" required>
    <Input placeholder="请输入姓名" />
  </Form.Item>

  <Form.Item name="age" label="年龄">
    <Input placeholder="请输入年龄" />
  </Form.Item>

  <Form.Item>
    <Button type="primary" htmlType="submit">
      提交
    </Button>
  </Form.Item>
</Form>
```

### 5. Modal - 模态框组件

```tsx
import { Modal, Button } from '@/components/ui';
import { useState } from 'react';

const [visible, setVisible] = useState(false);

<>
  <Button onClick={() => setVisible(true)}>打开模态框</Button>

  <Modal
    visible={visible}
    title="模态框标题"
    glass
    onCancel={() => setVisible(false)}
    onOk={() => {
      console.log('确认');
      setVisible(false);
    }}
  >
    这是模态框内容
  </Modal>
</>
```

### 6. Drawer - 抽屉组件

```tsx
import { Drawer, Button } from '@/components/ui';
import { useState } from 'react';

const [visible, setVisible] = useState(false);

<>
  <Button onClick={() => setVisible(true)}>打开抽屉</Button>

  <Drawer
    visible={visible}
    title="抽屉标题"
    placement="right"
    glass
    onClose={() => setVisible(false)}
  >
    这是抽屉内容
  </Drawer>
</>
```

---

## 二、布局组件

### 1. PageContainer - 页面容器

```tsx
import { PageContainer } from '@/components/layout';
import { Button } from '@/components/ui';

<PageContainer
  title="用户管理"
  subTitle="管理系统用户信息"
  breadcrumb={{
    items: [
      { title: '首页', path: '/' },
      { title: '系统管理', path: '/system' },
      { title: '用户管理' },
    ],
  }}
  extra={
    <Button type="primary">新增用户</Button>
  }
  tabs={{
    activeKey: 'all',
    items: [
      { key: 'all', label: '全部用户' },
      { key: 'active', label: '启用用户' },
      { key: 'inactive', label: '禁用用户' },
    ],
    onChange: (key) => console.log(key),
  }}
>
  页面内容
</PageContainer>
```

### 2. ContentWrapper - 内容包装器

```tsx
import { ContentWrapper } from '@/components/layout';

<ContentWrapper glass padding="lg" shadow="md">
  内容区域
</ContentWrapper>
```

### 3. SectionCard - 区块卡片

```tsx
import { SectionCard } from '@/components/layout';
import { Button } from '@/components/ui';

<SectionCard
  title="基本信息"
  description="用户的基本信息"
  extra={<Button type="link">编辑</Button>}
  collapsible
>
  区块内容
</SectionCard>
```

---

## 三、业务组件

### 1. PageHeader - 页面头部

```tsx
import { PageHeader } from '@/components/business';
import { Button, Tag } from '@/components/ui';

<PageHeader
  title="用户详情"
  subTitle="查看用户的详细信息"
  tags={<Tag color="success">启用</Tag>}
  extra={
    <>
      <Button>编辑</Button>
      <Button type="primary">保存</Button>
    </>
  }
  showBack
  onBack={() => history.back()}
/>
```

### 2. FilterBar - 筛选栏

```tsx
import { FilterBar } from '@/components/business';

<FilterBar
  items={[
    {
      name: 'name',
      label: '姓名',
      type: 'input',
      placeholder: '请输入姓名',
    },
    {
      name: 'status',
      label: '状态',
      type: 'select',
      options: [
        { label: '启用', value: 1 },
        { label: '禁用', value: 0 },
      ],
    },
    {
      name: 'dateRange',
      label: '日期范围',
      type: 'dateRange',
    },
  ]}
  collapsible
  defaultRows={1}
  onSearch={(values) => console.log('搜索', values)}
  onReset={() => console.log('重置')}
/>
```

### 3. ActionBar - 操作栏

```tsx
import { ActionBar } from '@/components/business';
import { PlusOutlined, DeleteOutlined } from '@ant-design/icons';

<ActionBar
  actions={[
    {
      key: 'add',
      label: '新增',
      icon: <PlusOutlined />,
      type: 'primary',
      onClick: () => console.log('新增'),
    },
    {
      key: 'delete',
      label: '删除',
      icon: <DeleteOutlined />,
      danger: true,
      onClick: () => console.log('删除'),
    },
  ]}
  extra={<span>已选择 3 项</span>}
  align="space-between"
/>
```

### 4. StatusTag - 状态标签

```tsx
import { StatusTag } from '@/components/business';

// 预定义状态
<StatusTag status="success" />
<StatusTag status="processing" />
<StatusTag status="error" />
<StatusTag status="warning" />
<StatusTag status="pending" />
<StatusTag status="approved" />
<StatusTag status="rejected" />
<StatusTag status="active" />
<StatusTag status="inactive" />
<StatusTag status="online" />
<StatusTag status="offline" />

// 自定义文本
<StatusTag status="success" text="已完成" />

// 不同尺寸
<StatusTag status="success" size="small" />
<StatusTag status="success" size="default" />
<StatusTag status="success" size="large" />

// 不显示状态点
<StatusTag status="success" showDot={false} />
```

### 5. MetricsCard - 指标卡片

```tsx
import { MetricsCard } from '@/components/business';
import { UserOutlined } from '@ant-design/icons';

<MetricsCard
  title="总用户数"
  value="1,234"
  unit="人"
  trend="up"
  trendValue={12.5}
  trendText="较上月"
  icon={<UserOutlined />}
  iconColor="primary"
  glass
  clickable
  onClick={() => console.log('点击')}
/>
```

### 6. Timeline - 时间轴

```tsx
import { Timeline } from '@/components/business';

<Timeline
  items={[
    {
      key: '1',
      time: '2026-04-27 10:00',
      title: '创建订单',
      description: '用户创建了订单',
      status: 'success',
    },
    {
      key: '2',
      time: '2026-04-27 10:30',
      title: '支付订单',
      description: '用户完成支付',
      status: 'processing',
    },
    {
      key: '3',
      time: '2026-04-27 11:00',
      title: '发货',
      description: '商家已发货',
      status: 'default',
    },
  ]}
  glass
/>
```

---

## 四、完整页面示例

### 用户管理页面

```tsx
import React, { useState } from 'react';
import { PageContainer, SectionCard } from '@/components/layout';
import { FilterBar, ActionBar, StatusTag } from '@/components/business';
import { Table, Button, Modal } from '@/components/ui';
import { PlusOutlined, EditOutlined, DeleteOutlined } from '@ant-design/icons';

const UserManagementPage: React.FC = () => {
  const [selectedRowKeys, setSelectedRowKeys] = useState<React.Key[]>([]);

  const columns = [
    {
      title: '用户名',
      dataIndex: 'username',
      key: 'username',
    },
    {
      title: '姓名',
      dataIndex: 'name',
      key: 'name',
    },
    {
      title: '状态',
      dataIndex: 'status',
      key: 'status',
      render: (status: number) => (
        <StatusTag status={status === 1 ? 'active' : 'inactive'} />
      ),
    },
    {
      title: '操作',
      key: 'action',
      render: (_, record) => (
        <>
          <Button type="link" icon={<EditOutlined />}>编辑</Button>
          <Button type="link" danger icon={<DeleteOutlined />}>删除</Button>
        </>
      ),
    },
  ];

  const dataSource = [
    { key: '1', username: 'admin', name: '管理员', status: 1 },
    { key: '2', username: 'user1', name: '用户1', status: 1 },
    { key: '3', username: 'user2', name: '用户2', status: 0 },
  ];

  return (
    <PageContainer
      title="用户管理"
      subTitle="管理系统用户信息"
      breadcrumb={{
        items: [
          { title: '首页', path: '/' },
          { title: '系统管理' },
          { title: '用户管理' },
        ],
      }}
    >
      <SectionCard title="筛选条件">
        <FilterBar
          items={[
            {
              name: 'username',
              label: '用户名',
              type: 'input',
            },
            {
              name: 'name',
              label: '姓名',
              type: 'input',
            },
            {
              name: 'status',
              label: '状态',
              type: 'select',
              options: [
                { label: '启用', value: 1 },
                { label: '禁用', value: 0 },
              ],
            },
          ]}
          onSearch={(values) => console.log('搜索', values)}
          onReset={() => console.log('重置')}
        />
      </SectionCard>

      <SectionCard>
        <ActionBar
          actions={[
            {
              key: 'add',
              label: '新增用户',
              icon: <PlusOutlined />,
              type: 'primary',
              onClick: () => console.log('新增'),
            },
            {
              key: 'delete',
              label: '批量删除',
              icon: <DeleteOutlined />,
              danger: true,
              disabled: selectedRowKeys.length === 0,
              onClick: () => console.log('删除'),
            },
          ]}
          extra={
            selectedRowKeys.length > 0 && (
              <span>已选择 {selectedRowKeys.length} 项</span>
            )
          }
          align="space-between"
        />

        <Table
          columns={columns}
          dataSource={dataSource}
          glass
          density="compact"
          striped
          rowSelection={{
            selectedRowKeys,
            onChange: (keys) => setSelectedRowKeys(keys),
          }}
          pagination={{
            current: 1,
            pageSize: 10,
            total: 100,
          }}
        />
      </SectionCard>
    </PageContainer>
  );
};

export default UserManagementPage;
```

---

## 五、最佳实践

### 1. 组件组合

优先使用组件组合而不是创建新组件：

```tsx
// ✅ 好的做法
<PageContainer title="用户管理">
  <SectionCard title="筛选条件">
    <FilterBar items={filterItems} />
  </SectionCard>

  <SectionCard>
    <ActionBar actions={actions} />
    <Table columns={columns} dataSource={data} />
  </SectionCard>
</PageContainer>

// ❌ 不好的做法 - 创建过多的自定义组件
<CustomUserManagementPage />
```

### 2. 样式定制

使用className和style进行样式定制：

```tsx
// ✅ 好的做法
<Card
  className="custom-card"
  style={{ marginBottom: 24 }}
>
  内容
</Card>

// ❌ 不好的做法 - 直接修改组件源码
```

### 3. 性能优化

对于大数据量的表格，使用虚拟滚动：

```tsx
<Table
  columns={columns}
  dataSource={largeDataSource}
  scroll={{ y: 500 }}
  virtual
/>
```

### 4. 响应式设计

使用响应式布局：

```tsx
<FilterBar
  items={filterItems}
  collapsible
  defaultRows={1}  // 默认只显示1行，其余折叠
/>
```

---

# 十五、你必须记住的 **3 个 “统一样式” 标签**

只要你的页面**套上这 3 层**，**自动和系统一模一样**，不用写 CSS！

```tsx
// 1. 最外层：页面容器（带标题、面包屑、 margins 间距）
<PageContainer>

  // 2. 第二层：卡片（带阴影、内边距、背景）
  <Card>

    // 3. 第三层：你的内容（表格、表单、按钮）
    <Table />
    <Form />

  </Card>
</PageContainer>
```

### 只要这样写 → 页面**100% 和系统统一**！

不写 → 页面就会**难看、贴边、没间距、风格混乱**！

------

# 十六、这三个标签是干嘛的？（你必须知道）

## 1. `<PageContainer>`

- 带**页面标题**
- 带**面包屑导航**
- 带**标准外边距**
- 保持所有页面**上下左右间距一致**

## 2. `<Card>`

- 带**白色背景**
- 带**柔和阴影**
- 带**内边距（内间距）**
- 让内容不贴边，看起来很专业

## 3. 里面放官方组件：Table / Form / Button / Input

- **官方自带样式**
- 颜色、字体、大小全部统一

------

# 十七、你不写会怎样？

- 内容贴到窗口边缘
- 没有背景、没有阴影
- 字体、间距乱七八糟
- **就是你之前觉得 “写出来很难看” 的原因！**

------

# 十八、我给你一个**复制就能用的标准页面**

你以后**所有页面都按这个结构写**，风格永远统一！

```tsx
import { PageContainer } from '@ant-design/pro-layout';
import { Card, Table, Button } from 'antd';
import { PlusOutlined } from '@ant-design/icons';

export default function YourPage() {
  return (
    // 必须写 ↓↓↓
    <PageContainer>
      <Card>
        <Button icon={<PlusOutlined />} type="primary" style={{ marginBottom: 16 }}>
          新增
        </Button>
        <Table columns={[]} dataSource={[]} />
      </Card>
    </PageContainer>
  );
}
```

------

# 十九、最终结论（最重要）

## **想保持页面一致性 = 必须用这两个标签：**

### `<PageContainer>` + `<Card>`

## 里面内容 = 直接用 Ant Design 官方组件

## **不用写任何 CSS！**