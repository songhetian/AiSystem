# 测试工具文档

本文档包含所有系统优化和测试相关的命令和工具使用说明。

---

## 📦 安装依赖

### 安装测试框架依赖

```bash
cd backend
npm install --save-dev jest @nestjs/testing ts-jest @types/jest glob
```

---

## 🔧 测试命令

### 基础测试命令

```bash
# 运行所有测试
npm test

# 监听模式（开发时使用）
npm run test:watch

# 生成覆盖率报告
npm run test:cov

# 调试测试
npm run test:debug

# 运行特定测试文件
npm test -- personnel-departments.service.spec.ts

# 只运行失败的测试
npm test -- --onlyFailures

# 清除缓存
npm test -- --clearCache

# 详细输出
npm test -- --verbose
```

### 查看覆盖率报告

```bash
# 生成覆盖率报告
npm run test:cov

# 在浏览器中查看（macOS）
open coverage/lcov-report/index.html

# 在浏览器中查看（Linux）
xdg-open coverage/lcov-report/index.html

# 在浏览器中查看（Windows）
start coverage/lcov-report/index.html
```

---

## 🔍 权限验证工具

### 运行权限验证

```bash
cd backend

# 运行权限验证工具
npm run verify:permissions

# 查看生成的报告
cat PERMISSION_VERIFICATION_REPORT.md

# 或在编辑器中打开
code PERMISSION_VERIFICATION_REPORT.md
```

### 权限验证报告内容

报告包含：

- 总端点数统计
- 已配置权限的端点数量和百分比
- 缺失权限的端点数量和百分比
- 缺失权限的端点详细列表（文件、行号、方法、路径）
- 已配置权限的端点详细列表（包含权限代码）

### 修复权限配置后重新验证

```bash
# 修复代码后，再次运行验证
npm run verify:permissions

# 确认所有端点都有权限配置
```

---

## 📊 大文件分析工具

### 运行大文件分析

```bash
cd backend

# 运行大文件分析工具
npm run analyze:large-files

# 查看生成的报告
cat LARGE_FILES_ANALYSIS_REPORT.md

# 或在编辑器中打开
code LARGE_FILES_ANALYSIS_REPORT.md
```

### 大文件分析报告内容

报告包含：

- 超过 500 行的文件统计
- 最大文件行数
- 平均行数
- 文件列表（行数、函数数、类数、复杂度）
- 每个文件的详细分析和拆分建议

### 拆分文件后重新分析

```bash
# 拆分文件后，再次运行分析
npm run analyze:large-files

# 确认所有文件都小于 300 行
```

---

## 📈 性能监控

### 集成性能监控

#### 步骤 1: 修改 app.module.ts

```typescript
// backend/src/app.module.ts
import { Module } from "@nestjs/common";
import { APP_INTERCEPTOR } from "@nestjs/core";
import { PerformanceMonitorInterceptor } from "./common/interceptors/performance-monitor.interceptor";

@Module({
  imports: [
    // ... 其他导入
  ],
  providers: [
    {
      provide: APP_INTERCEPTOR,
      useClass: PerformanceMonitorInterceptor,
    },
  ],
})
export class AppModule {}
```

#### 步骤 2: 修改 common.module.ts

```typescript
// backend/src/common/common.module.ts
import { Module } from "@nestjs/common";
import { PerformanceController } from "./controllers/performance.controller";
import { PerformanceMonitorInterceptor } from "./interceptors/performance-monitor.interceptor";

@Module({
  controllers: [PerformanceController],
  providers: [PerformanceMonitorInterceptor],
  exports: [PerformanceMonitorInterceptor],
})
export class CommonModule {}
```

### 启动应用

```bash
cd backend

# 启动开发服务器
npm run start:dev

# 或构建后启动
npm run build
npm start
```

### 性能监控 API

#### 获取整体性能统计

```bash
# 基础查询
curl http://localhost:3000/performance/statistics

# 带时间范围查询
curl "http://localhost:3000/performance/statistics?startTime=2026-04-16T00:00:00Z&endTime=2026-04-16T23:59:59Z"
```

**返回数据示例**:

```json
{
  "success": true,
  "data": {
    "totalRequests": 1000,
    "slowRequests": 50,
    "slowRequestRate": 5.0,
    "avgDuration": 250,
    "minDuration": 10,
    "maxDuration": 5000,
    "p50": 200,
    "p95": 800,
    "p99": 2000,
    "topSlowRequests": [
      {
        "method": "GET",
        "url": "/api/exam/results",
        "duration": 5000,
        "timestamp": "2026-04-16T10:30:00Z"
      }
    ]
  }
}
```

#### 获取各端点性能统计

```bash
curl http://localhost:3000/performance/endpoints
```

**返回数据示例**:

```json
{
  "success": true,
  "data": [
    {
      "endpoint": "GET /api/personnel/employees",
      "totalRequests": 500,
      "avgDuration": 300,
      "minDuration": 50,
      "maxDuration": 2000,
      "p50": 250,
      "p95": 800,
      "p99": 1500
    }
  ]
}
```

#### 获取系统资源使用情况

```bash
curl http://localhost:3000/performance/system
```

**返回数据示例**:

```json
{
  "success": true,
  "data": {
    "memory": {
      "rss": 150,
      "heapTotal": 100,
      "heapUsed": 80,
      "external": 10,
      "heapUsagePercent": 80
    },
    "cpu": {
      "user": 5000,
      "system": 2000
    },
    "uptime": 3600,
    "nodeVersion": "v20.0.0",
    "platform": "darwin",
    "pid": 12345
  }
}
```

### 使用 Postman 或其他工具测试

1. 导入以下端点到 Postman：
   - `GET http://localhost:3000/performance/statistics`
   - `GET http://localhost:3000/performance/endpoints`
   - `GET http://localhost:3000/performance/system`

2. 添加认证 Token（如果需要）

3. 发送请求查看性能数据

---

## 🎯 完整工作流程

### 1. 初始设置（首次运行）

```bash
# 1. 安装依赖
cd backend
npm install --save-dev jest @nestjs/testing ts-jest @types/jest glob

# 2. 验证安装
npm test -- --version
```

### 2. 运行所有检查工具

```bash
# 2.1 权限验证
npm run verify:permissions

# 2.2 大文件分析
npm run analyze:large-files

# 2.3 运行测试
npm test

# 2.4 生成覆盖率报告
npm run test:cov
```

### 3. 查看所有报告

```bash
# 查看权限验证报告
cat PERMISSION_VERIFICATION_REPORT.md

# 查看大文件分析报告
cat LARGE_FILES_ANALYSIS_REPORT.md

# 查看测试覆盖率报告
open coverage/lcov-report/index.html
```

### 4. 集成性能监控

```bash
# 4.1 修改配置文件（参考上面的步骤）

# 4.2 启动应用
npm run start:dev

# 4.3 测试性能监控 API
curl http://localhost:3000/performance/statistics
curl http://localhost:3000/performance/endpoints
curl http://localhost:3000/performance/system
```

### 5. 持续优化

```bash
# 5.1 修复权限配置
# 编辑 Controller 文件，添加 @Permission 装饰器

# 5.2 拆分大文件
# 根据分析报告拆分文件

# 5.3 编写测试
# 为每个服务创建 .spec.ts 文件

# 5.4 重新运行检查
npm run verify:permissions
npm run analyze:large-files
npm run test:cov
```

---

## 📋 检查清单

### 权限配置检查

- [ ] 运行 `npm run verify:permissions`
- [ ] 查看 `PERMISSION_VERIFICATION_REPORT.md`
- [ ] 修复所有缺失权限的端点
- [ ] 再次运行验证，确保 100% 覆盖

### 大文件检查

- [ ] 运行 `npm run analyze:large-files`
- [ ] 查看 `LARGE_FILES_ANALYSIS_REPORT.md`
- [ ] 优先拆分 >1000 行的文件
- [ ] 确保所有文件 <300 行

### 测试覆盖率检查

- [ ] 运行 `npm test`
- [ ] 运行 `npm run test:cov`
- [ ] 查看覆盖率报告
- [ ] 为未覆盖的代码编写测试
- [ ] 确保覆盖率 ≥80%

### 性能监控检查

- [ ] 集成性能监控拦截器
- [ ] 注册性能监控 Controller
- [ ] 启动应用
- [ ] 测试性能监控 API
- [ ] 查看性能数据

---

## 🔧 故障排查

### 测试相关问题

#### 问题：测试依赖安装失败

```bash
# 解决方案：清除缓存后重新安装
npm cache clean --force
npm install --save-dev jest @nestjs/testing ts-jest @types/jest
```

#### 问题：测试运行失败

```bash
# 解决方案：清除 Jest 缓存
npm test -- --clearCache

# 重新运行测试
npm test
```

#### 问题：覆盖率报告无法生成

```bash
# 解决方案：检查 jest.config.js 配置
cat backend/jest.config.js

# 确保 collectCoverageFrom 配置正确
# 重新生成覆盖率
npm run test:cov
```

### 权限验证问题

#### 问题：权限验证工具报错

```bash
# 解决方案：确保安装了 glob 包
npm install --save-dev glob

# 重新运行
npm run verify:permissions
```

#### 问题：找不到 Controller 文件

```bash
# 解决方案：检查文件路径
# 确保在 backend 目录下运行
cd backend
npm run verify:permissions
```

### 大文件分析问题

#### 问题：分析工具报错

```bash
# 解决方案：确保安装了 glob 包
npm install --save-dev glob

# 重新运行
npm run analyze:large-files
```

### 性能监控问题

#### 问题：性能监控 API 返回 404

```bash
# 解决方案：
# 1. 确保已在 common.module.ts 中注册 PerformanceController
# 2. 确保 CommonModule 已导入到 AppModule
# 3. 重启应用
npm run start:dev
```

#### 问题：性能监控无数据

```bash
# 解决方案：
# 1. 确保已集成 PerformanceMonitorInterceptor
# 2. 发送一些请求到应用
# 3. 再次查询性能 API
curl http://localhost:3000/performance/statistics
```

---

## 📊 性能指标说明

### 响应时间指标

- **avgDuration**: 平均响应时间（毫秒）
- **minDuration**: 最小响应时间（毫秒）
- **maxDuration**: 最大响应时间（毫秒）
- **P50**: 50% 的请求响应时间低于此值
- **P95**: 95% 的请求响应时间低于此值
- **P99**: 99% 的请求响应时间低于此值

### 慢请求定义

- **慢请求**: 响应时间 >3000ms 的请求
- **慢查询**: 执行时间 >1000ms 的数据库查询

### 内存指标

- **rss**: 常驻内存大小（MB）
- **heapTotal**: 堆内存总大小（MB）
- **heapUsed**: 已使用的堆内存（MB）
- **heapUsagePercent**: 堆内存使用率（%）

---

## 🎯 优化目标

| 指标           | 当前 | 目标 | 状态      |
| -------------- | ---- | ---- | --------- |
| 测试覆盖率     | 0%   | ≥80% | 🔴 待提升 |
| 权限配置覆盖率 | ?    | 100% | 🟡 待验证 |
| 大文件数量     | 11   | 0    | 🔴 待优化 |
| 最大文件行数   | 1182 | <300 | 🔴 待拆分 |
| 慢请求率       | ?    | <5%  | 🟡 待监控 |
| 慢查询率       | ?    | <5%  | 🟡 待监控 |

---

## 📚 相关文档

- `OPTIMIZATION_README.md` - 优化工具包总览
- `OPTIMIZATION_SUMMARY.md` - 详细优化总结
- `OPTIMIZATION_QUICKSTART.md` - 快速开始指南
- `OPTIMIZATION_COMPLETED.md` - 完成报告

---

## 💡 最佳实践

### 测试编写

1. 每个服务类都应有对应的 `.spec.ts` 文件
2. 测试文件应与源文件在同一目录
3. 使用 `describe` 和 `it` 组织测试用例
4. Mock 所有外部依赖（数据库、外部服务）
5. 测试正常流程和异常流程
6. 使用有意义的测试描述

### 权限配置

1. 所有 API 端点都应有 `@Permission` 装饰器
2. 权限代码格式：`module:resource:action`
3. 例如：`personnel:employee:list`, `approval:request:create`
4. 定期运行权限验证工具
5. 保持前后端权限配置同步

### 代码质量

1. 单个文件不超过 300 行
2. 单个函数不超过 50 行
3. 函数复杂度不超过 10
4. 遵循单一职责原则
5. 避免重复代码

### 性能优化

1. 定期查看性能监控数据
2. 优化响应时间 >3000ms 的 API
3. 优化执行时间 >1000ms 的查询
4. 使用缓存减少数据库访问
5. 添加数据库索引

---

**最后更新**: 2026-04-16
**维护者**: 开发团队
