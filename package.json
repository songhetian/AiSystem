{
  "name": "aisystem-root",
  "private": true,
  "version": "1.0.0",
  "description": "雷犀 AI 客服系统 - 根目录管理脚本",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../dev-tools && npm install && cd ..",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "install:mock": "cd dev-tools && npm install",
    "dev": "docker compose --profile dev up -d",
    "dev:base": "docker compose up -d mysql redis minio qdrant",
    "dev:mock": "docker compose --profile dev up -d mock-service",
    "prod": "docker compose --profile prod up -d",
    "stop": "docker compose down",
    "restart": "docker compose --profile dev restart",
    "logs": "docker compose logs -f",
    "logs:backend": "docker compose logs -f backend",
    "logs:frontend": "docker compose logs -f frontend",
    "logs:mock": "docker compose logs -f mock-service",
    "ps": "docker compose ps",
    "db:migrate": "docker exec backend-service npx prisma migrate deploy",
    "db:seed": "docker exec backend-service npx ts-node prisma/seed.ts",
    "db:studio": "cd backend && npx prisma studio"
  },

  "keywords": [
    "aisystem",
    "nestjs",
    "react",
    "docker",
    "mysql",
    "redis",
    "minio",
    "qdrant"
  ],
  "author": "雷犀科技",
  "license": "UNLICENSED"
}
