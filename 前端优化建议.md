# 前端页面优化建议

基于代码分析，以下是针对操作便捷性和用户体验的优化建议。

---

## 🎯 高优先级优化（立即可实施）

### 1. 全局加载状态优化

**问题**: 多个页面使用 `useQuery` 但没有统一的加载状态处理

**建议**: 创建全局加载组件

```typescript
// frontend/src/components/common/GlobalLoading.tsx
import { Spin } from 'antd';
import { LoadingOutlined } from '@ant-design/icons';

export const GlobalLoading = ({ loading, children }: { loading: boolean; children: React.ReactNode }) => {
  if (loading) {
    return (
      <div style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        minHeight: '400px'
      }}>
        <Spin indicator={<LoadingOutlined style={{ fontSize: 24 }} spin />} />
      </div>
    );
  }
  return <>{children}</>;
};
```

**使用方式**:

```typescript
<GlobalLoading loading={isLoading}>
  <YourContent />
</GlobalLoading>
```

---

### 2. 搜索防抖优化

**问题**: 员工页面的搜索可能没有防抖，每次输入都会触发查询

**建议**: 添加搜索防抖 Hook

```typescript
// frontend/src/hooks/useDebounce.ts
import { useEffect, useState } from "react";

export function useDebounce<T>(value: T, delay: number = 500): T {
  const [debouncedValue, setDebouncedValue] = useState<T>(value);

  useEffect(() => {
    const handler = setTimeout(() => {
      setDebouncedValue(value);
    }, delay);

    return () => {
      clearTimeout(handler);
    };
  }, [value, delay]);

  return debouncedValue;
}
```

**使用方式**:

```typescript
const [searchText, setSearchText] = useState("");
const debouncedSearchText = useDebounce(searchText, 500);

const { data } = useQuery({
  queryKey: ["employees", debouncedSearchText],
  queryFn: () => fetchEmployees(debouncedSearchText),
});
```

---

### 3. 表格列配置持久化优化

**问题**: 员工页面已有列配置，但可能缺少重置功能

**建议**: 添加列配置重置按钮

```typescript
// 在表格工具栏添加
<Button
  icon={<ReloadOutlined />}
  onClick={() => {
    setColumns(defaultColumnConfig);
    saveColumnConfig("employee-table-columns", defaultColumnConfig);
    message.success("已重置为默认列配置");
  }}
>
  重置列配置
</Button>
```

---

### 4. 批量操作优化

**问题**: 批量操作可能没有确认提示

**建议**: 添加批量操作确认

```typescript
// 批量删除示例
const handleBatchDelete = () => {
  if (selectedIds.length === 0) {
    message.warning("请先选择要删除的记录");
    return;
  }

  Modal.confirm({
    title: "确认批量删除",
    content: `确定要删除选中的 ${selectedIds.length} 条记录吗？此操作不可恢复。`,
    okText: "确认删除",
    okType: "danger",
    cancelText: "取消",
    onOk: async () => {
      await batchDeleteMutation.mutateAsync(selectedIds);
      setSelectedIds([]);
    },
  });
};
```

---

### 5. 表单自动保存草稿

**问题**: 长表单填写时，如果误关闭会丢失数据

**建议**: 添加表单草稿自动保存

```typescript
// frontend/src/hooks/useFormDraft.ts
import { useEffect } from "react";
import { FormInstance } from "antd";

export function useFormDraft(
  form: FormInstance,
  key: string,
  interval: number = 30000,
) {
  useEffect(() => {
    // 加载草稿
    const draft = localStorage.getItem(`form-draft-${key}`);
    if (draft) {
      try {
        form.setFieldsValue(JSON.parse(draft));
      } catch (e) {
        console.error("Failed to load draft", e);
      }
    }

    // 自动保存
    const timer = setInterval(() => {
      const values = form.getFieldsValue();
      localStorage.setItem(`form-draft-${key}`, JSON.stringify(values));
    }, interval);

    return () => {
      clearInterval(timer);
    };
  }, [form, key, interval]);

  const clearDraft = () => {
    localStorage.removeItem(`form-draft-${key}`);
  };

  return { clearDraft };
}
```

**使用方式**:

```typescript
const [form] = Form.useForm();
const { clearDraft } = useFormDraft(form, "employee-form");

const handleSubmit = async (values) => {
  await createMutation.mutateAsync(values);
  clearDraft(); // 提交成功后清除草稿
};
```

---

## 🚀 中优先级优化（短期实施）

### 6. 快捷键支持

**建议**: 添加常用操作的快捷键

```typescript
// frontend/src/hooks/useKeyboardShortcuts.ts
import { useEffect } from "react";

export function useKeyboardShortcuts(shortcuts: Record<string, () => void>) {
  useEffect(() => {
    const handleKeyDown = (e: KeyboardEvent) => {
      const key = `${e.ctrlKey ? "Ctrl+" : ""}${e.shiftKey ? "Shift+" : ""}${e.key}`;
      const handler = shortcuts[key];
      if (handler) {
        e.preventDefault();
        handler();
      }
    };

    window.addEventListener("keydown", handleKeyDown);
    return () => window.removeEventListener("keydown", handleKeyDown);
  }, [shortcuts]);
}
```

**使用示例**:

```typescript
// 在列表页面
useKeyboardShortcuts({
  "Ctrl+n": () => setOpen(true), // 新建
  "Ctrl+f": () => searchInputRef.current?.focus(), // 聚焦搜索
  "Ctrl+r": () => queryClient.invalidateQueries(), // 刷新
  Escape: () => setOpen(false), // 关闭弹窗
});
```

---

### 7. 表格行内编辑

**建议**: 为简单字段添加行内编辑功能

```typescript
// 示例：状态快速切换
{
  title: '状态',
  dataIndex: 'status',
  render: (status, record) => (
    <Switch
      checked={status === 1}
      onChange={async (checked) => {
        await updateStatusMutation.mutateAsync({
          id: record.id,
          status: checked ? 1 : 0,
        });
      }}
    />
  ),
}
```

---

### 8. 智能搜索建议

**建议**: 添加搜索历史和建议

```typescript
// frontend/src/components/common/SmartSearch.tsx
import { AutoComplete, Input } from 'antd';
import { SearchOutlined } from '@ant-design/icons';
import { useState, useEffect } from 'react';

export const SmartSearch = ({ onSearch, storageKey }: {
  onSearch: (value: string) => void;
  storageKey: string;
}) => {
  const [options, setOptions] = useState<{ value: string }[]>([]);

  useEffect(() => {
    const history = JSON.parse(localStorage.getItem(storageKey) || '[]');
    setOptions(history.map((h: string) => ({ value: h })));
  }, [storageKey]);

  const handleSearch = (value: string) => {
    if (value) {
      const history = JSON.parse(localStorage.getItem(storageKey) || '[]');
      const newHistory = [value, ...history.filter((h: string) => h !== value)].slice(0, 10);
      localStorage.setItem(storageKey, JSON.stringify(newHistory));
      setOptions(newHistory.map(h => ({ value: h })));
    }
    onSearch(value);
  };

  return (
    <AutoComplete
      options={options}
      onSelect={handleSearch}
      style={{ width: 300 }}
    >
      <Input.Search
        placeholder="搜索..."
        prefix={<SearchOutlined />}
        onSearch={handleSearch}
        allowClear
      />
    </AutoComplete>
  );
};
```

---

### 9. 数据导出进度提示

**建议**: 大数据导出时显示进度

```typescript
const handleExport = async () => {
  const hide = message.loading("正在导出数据...", 0);
  try {
    await exportMutation.mutateAsync();
    hide();
    message.success("导出成功");
  } catch (error) {
    hide();
    message.error("导出失败");
  }
};
```

---

### 10. 表格数据缓存优化

**建议**: 使用 React Query 的缓存策略

```typescript
const { data, isLoading } = useQuery({
  queryKey: ["employees", filters],
  queryFn: () => fetchEmployees(filters),
  staleTime: 5 * 60 * 1000, // 5分钟内数据视为新鲜
  cacheTime: 10 * 60 * 1000, // 缓存10分钟
  refetchOnWindowFocus: false, // 窗口聚焦时不自动刷新
});
```

---

## 💡 低优先级优化（长期改进）

### 11. 虚拟滚动优化

**问题**: 大数据量表格可能卡顿

**建议**: 使用虚拟滚动

```typescript
import { ProTable } from '@ant-design/pro-components';

<ProTable
  virtual
  scroll={{ y: 600 }}
  // ... 其他配置
/>
```

---

### 12. 离线支持

**建议**: 添加 Service Worker 支持离线访问

```typescript
// frontend/src/serviceWorker.ts
// 使用 Workbox 或自定义 Service Worker
```

---

### 13. 主题切换

**建议**: 添加深色模式支持

```typescript
// frontend/src/hooks/useTheme.ts
import { useState, useEffect } from "react";
import { theme } from "antd";

export function useTheme() {
  const [isDark, setIsDark] = useState(false);

  useEffect(() => {
    const saved = localStorage.getItem("theme");
    setIsDark(saved === "dark");
  }, []);

  const toggleTheme = () => {
    const newTheme = !isDark;
    setIsDark(newTheme);
    localStorage.setItem("theme", newTheme ? "dark" : "light");
  };

  return {
    isDark,
    toggleTheme,
    algorithm: isDark ? theme.darkAlgorithm : theme.defaultAlgorithm,
  };
}
```

---

### 14. 数据预加载

**建议**: 鼠标悬停时预加载详情数据

```typescript
const handleRowHover = (record: any) => {
  queryClient.prefetchQuery({
    queryKey: ['employee-detail', record.id],
    queryFn: () => fetchEmployeeDetail(record.id),
  });
};

<Table
  onRow={(record) => ({
    onMouseEnter: () => handleRowHover(record),
  })}
/>
```

---

### 15. 错误边界

**建议**: 添加错误边界组件

```typescript
// frontend/src/components/common/ErrorBoundary.tsx
import React from 'react';
import { Result, Button } from 'antd';

export class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  { hasError: boolean }
> {
  constructor(props: any) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError() {
    return { hasError: true };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        <Result
          status="error"
          title="页面出错了"
          subTitle="抱歉，页面遇到了一些问题"
          extra={
            <Button type="primary" onClick={() => window.location.reload()}>
              刷新页面
            </Button>
          }
        />
      );
    }

    return this.props.children;
  }
}
```

---

## 📊 性能监控建议

### 16. 添加前端性能监控

```typescript
// frontend/src/utils/performance-monitor.ts
export const reportWebVitals = (metric: any) => {
  // 上报到后端性能监控 API
  fetch("/api/performance/web-vitals", {
    method: "POST",
    body: JSON.stringify(metric),
    headers: { "Content-Type": "application/json" },
  });
};

// 在 app.tsx 中使用
import { reportWebVitals } from "./utils/performance-monitor";
reportWebVitals(onPerfEntry);
```

---

## 🎨 UI/UX 优化建议

### 17. 空状态优化

**建议**: 为空列表添加友好的空状态

```typescript
<Empty
  image={Empty.PRESENTED_IMAGE_SIMPLE}
  description="暂无数据"
  style={{ marginTop: 100 }}
>
  <Button type="primary" icon={<PlusOutlined />} onClick={() => setOpen(true)}>
    创建第一条记录
  </Button>
</Empty>
```

---

### 18. 操作反馈优化

**建议**: 所有操作都应有明确反馈

```typescript
// 成功反馈
message.success("操作成功", 2);

// 错误反馈
message.error("操作失败，请重试", 3);

// 警告反馈
message.warning("请先选择记录", 2);

// 加载反馈
const hide = message.loading("处理中...", 0);
// 操作完成后
hide();
```

---

### 19. 表单验证优化

**建议**: 添加实时验证和友好的错误提示

```typescript
<Form.Item
  name="email"
  label="邮箱"
  rules={[
    { required: true, message: '请输入邮箱' },
    { type: 'email', message: '请输入有效的邮箱地址' },
    {
      validator: async (_, value) => {
        if (value) {
          const exists = await checkEmailExists(value);
          if (exists) {
            throw new Error('该邮箱已被使用');
          }
        }
      },
    },
  ]}
  validateTrigger="onBlur"
>
  <Input placeholder="请输入邮箱" />
</Form.Item>
```

---

### 20. 响应式设计优化

**建议**: 确保移动端友好

```typescript
import { Grid } from 'antd';

const { useBreakpoint } = Grid;

export default function ResponsivePage() {
  const screens = useBreakpoint();

  return (
    <ProTable
      scroll={{ x: screens.xs ? 800 : undefined }}
      pagination={{
        pageSize: screens.xs ? 5 : 10,
      }}
      // ... 其他配置
    />
  );
}
```

---

## 🔧 实施优先级

### 立即实施（1-2 天）

1. ✅ 全局加载状态优化
2. ✅ 搜索防抖优化
3. ✅ 批量操作确认
4. ✅ 操作反馈优化

### 短期实施（1 周）

5. ✅ 快捷键支持
6. ✅ 表单草稿保存
7. ✅ 智能搜索建议
8. ✅ 数据导出进度

### 中期实施（2-4 周）

9. ✅ 表格行内编辑
10. ✅ 虚拟滚动
11. ✅ 错误边界
12. ✅ 性能监控

### 长期实施（1-2 月）

13. ✅ 主题切换
14. ✅ 离线支持
15. ✅ 数据预加载

---

## 📝 总结

这些优化建议主要关注：

1. **性能优化**: 防抖、缓存、虚拟滚动
2. **用户体验**: 加载状态、操作反馈、空状态
3. **便捷性**: 快捷键、草稿保存、智能搜索
4. **可靠性**: 错误边界、确认提示、验证优化

建议按优先级逐步实施，每次实施后进行用户测试和反馈收集。

---

**创建时间**: 2026-04-16
**维护者**: 开发团队
