# 问题修复总结

> **更新时间**: 2026-04-25  
> **状态**: ✅ 已完成

## 修复的问题

### 1. Docker镜像构建失败 ✅

**问题**: 
```
ERROR: failed to resolve source metadata for docker.io/library/node:20-alpine
```

**原因**: Docker镜像源连接失败

**解决方案**:
- 在 `frontend/Dockerfile` 和 `backend/Dockerfile` 中添加国内npm镜像源配置
- 使用 `npm config set registry https://registry.npmmirror.com`
- 采用多阶段构建优化镜像大小

**修改文件**:
- `frontend/Dockerfile`
- `backend/Dockerfile`
- `frontend/.dockerignore`
- `backend/.dockerignore`

---

### 2. 后端TypeScript编译错误 ✅

**问题**:
```
error TS2307: Cannot find module '../../../../test/helpers/test-utils'
```

**原因**: 测试文件引用了不存在的测试工具模块，且测试文件被包含在构建中

**解决方案**:
1. 在 `tsconfig.json` 中添加 `exclude` 配置，排除测试文件
2. 在 `nest-cli.json` 中添加 `exclude` 配置
3. 创建缺失的测试工具文件 `test/helpers/test-utils.ts`

**修改文件**:
- `backend/tsconfig.json` - 添加exclude配置
- `backend/nest-cli.json` - 添加exclude配置
- `backend/test/helpers/test-utils.ts` - 新建测试工具文件

---

### 3. API地址配置问题 ✅

**问题**: 
- 前端请求发送到错误的地址（http://localhost:8000 而不是 http://localhost:3000）
- 修改环境变量需要重新构建镜像

**原因**: Vite的环境变量在构建时固定，无法在运行时修改

**解决方案**: 实现运行时配置方案
1. 创建配置模板文件 `frontend/public/config.js`
2. 创建启动脚本 `frontend/entrypoint.sh`，在容器启动时从环境变量生成配置
3. 修改 `frontend/src/utils/request.ts`，从 `window.APP_CONFIG` 读取配置
4. 在 `frontend/.umirc.ts` 中引入配置文件

**配置优先级**:
```
window.APP_CONFIG.apiBaseUrl (运行时，最高优先级)
  ↓
process.env.VITE_API_BASE_URL (构建时)
  ↓
/api/v1 (默认值)
```

**修改文件**:
- `frontend/public/config.js` - 配置模板
- `frontend/entrypoint.sh` - 启动脚本
- `frontend/src/utils/request.ts` - 请求工具
- `frontend/.umirc.ts` - Umi配置
- `frontend/Dockerfile` - 添加entrypoint
- `docker-compose.yml` - 环境变量配置

**优势**:
- ✅ 修改API地址只需重启容器，无需重新构建镜像
- ✅ 支持不同环境使用不同配置
- ✅ 配置灵活，易于维护

---

### 4. 前端热更新支持 ✅

**问题**: 修改前端代码需要重新构建镜像

**解决方案**: 在 `docker-compose.yml` 中添加volume挂载

**挂载目录**:
```yaml
volumes:
  - ./frontend/src:/app/src
  - ./frontend/.umirc.ts:/app/.umirc.ts
  - ./frontend/tailwind.config.js:/app/tailwind.config.js
  - ./frontend/tsconfig.json:/app/tsconfig.json
  - ./frontend/public/config.js:/app/public/config.js
```

**效果**: 修改代码后自动热更新，无需重启容器

---

### 5. Tailwind CSS配置问题 ✅

**问题**: Tailwind CSS编译超时，导致容器频繁重启

**解决方案**:
1. 禁用Umi内置的Tailwind插件：`tailwindcss: false`
2. 使用 `extraPostCSSPlugins` 手动配置
3. 禁用MFSU：`mfsu: false`
4. 创建 `frontend/src/global.css` 并在 `app.tsx` 中导入

**修改文件**:
- `frontend/.umirc.ts`
- `frontend/src/global.css`
- `frontend/src/app.tsx`

---

## 当前系统架构

### 目录结构

```
.
├── backend/                    # 后端服务（NestJS）
│   ├── src/                   # 源代码
│   ├── prisma/                # 数据库Schema和迁移
│   ├── test/                  # 测试文件
│   ├── Dockerfile             # Docker配置
│   ├── tsconfig.json          # TypeScript配置
│   └── nest-cli.json          # NestJS CLI配置
├── frontend/                   # 前端服务（Umi/React）
│   ├── src/                   # 源代码
│   ├── public/                # 静态资源
│   │   └── config.js          # 运行时配置模板
│   ├── Dockerfile             # Docker配置
│   ├── entrypoint.sh          # 容器启动脚本
│   ├── .umirc.ts              # Umi配置
│   └── tailwind.config.js     # Tailwind配置
├── dev-tools/                  # Mock服务
├── infra/                      # 基础设施配置
│   ├── mysql/                 # MySQL配置
│   ├── redis/                 # Redis配置
│   └── nginx/                 # Nginx配置
├── docker-compose.yml          # Docker Compose配置
├── .env                        # 环境变量（从.env.development复制）
├── .env.development            # 开发环境配置
├── .env.production             # 生产环境配置
├── package.json                # 根目录npm脚本
└── 使用手册.md                 # 使用文档
```

### 服务架构

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器                              │
└─────────────────────────────────────────────────────────┘
                          │
                          ↓
┌─────────────────────────────────────────────────────────┐
│  开发环境: http://localhost:8000 (直接访问前端)            │
│  生产环境: http://localhost (通过Nginx)                   │
└─────────────────────────────────────────────────────────┘
                          │
        ┌─────────────────┴─────────────────┐
        ↓                                   ↓
┌──────────────────┐              ┌──────────────────┐
│  Frontend (8000) │              │  Nginx (80)      │
│  - Umi/React     │              │  - 反向代理       │
│  - 运行时配置     │              │  - 负载均衡       │
└──────────────────┘              └──────────────────┘
        │                                   │
        └─────────────────┬─────────────────┘
                          ↓
                ┌──────────────────┐
                │  Backend (3000)  │
                │  - NestJS        │
                │  - RESTful API   │
                └──────────────────┘
                          │
        ┌─────────────────┼─────────────────┬─────────────┐
        ↓                 ↓                 ↓             ↓
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ MySQL (3306) │  │ Redis (6379) │  │ MinIO (9000) │  │Qdrant (6333) │
│ - 数据存储    │  │ - 缓存       │  │ - 对象存储    │  │ - 向量数据库  │
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘

开发环境额外服务:
┌──────────────────┐
│  Mock (3888)     │
│  - 电商API模拟    │
└──────────────────┘
```

### 配置流程

```
1. 环境变量配置
   .env.development → .env
   
2. Docker构建
   Dockerfile → Docker镜像
   
3. 容器启动
   docker-compose.yml → 容器实例
   
4. 运行时配置生成
   entrypoint.sh → config.js
   
5. 应用启动
   前端: serve dist
   后端: node dist/main
```

---

## 使用流程

### 首次启动

```bash
# 1. 复制环境配置
cp .env.development .env

# 2. 构建镜像
npm run rebuild:all

# 3. 启动服务
npm run dev

# 4. 初始化数据库
npm run db:sync
npm run db:seed

# 5. 访问系统
# http://localhost:8000
```

### 日常开发

```bash
# 启动
npm run dev

# 查看日志
npm run logs

# 停止
npm run stop
```

### 修改配置

```bash
# 1. 编辑 .env
vim .env

# 2. 重启服务
npm run restart

# 或只重启前端（如果只修改了前端配置）
docker compose restart frontend
```

---

## 技术栈

### 后端
- **框架**: NestJS 10.x
- **语言**: TypeScript 5.x
- **ORM**: Prisma 6.x
- **数据库**: MySQL 8.0
- **缓存**: Redis 7.2
- **对象存储**: MinIO
- **向量数据库**: Qdrant 1.7

### 前端
- **框架**: Umi 4.x (基于React 18)
- **语言**: TypeScript 5.x
- **UI库**: Ant Design 5.x
- **样式**: Tailwind CSS 3.x
- **HTTP客户端**: Axios
- **状态管理**: React Query

### 基础设施
- **容器化**: Docker + Docker Compose
- **反向代理**: Nginx 1.25
- **开发工具**: Mock服务（Express）

---

## 环境变量说明

### 核心配置

| 变量名 | 说明 | 开发环境 | 生产环境 |
|--------|------|----------|----------|
| `NODE_ENV` | 运行环境 | development | production |
| `PORT` | 后端端口 | 3000 | 3000 |
| `FRONTEND_PORT` | 前端端口 | 8000 | 8000 |
| `VITE_API_BASE_URL` | 前端API地址 | http://localhost:3000/api/v1 | /api/v1 |

### 数据库配置

| 变量名 | 说明 | 开发环境 | 生产环境 |
|--------|------|----------|----------|
| `MYSQL_HOST` | MySQL地址 | mysql-service | mysql-service |
| `MYSQL_PORT` | MySQL端口 | 3306 | 3306 |
| `MYSQL_DATABASE` | 数据库名 | aisystem | aisystem |
| `MYSQL_USER` | 数据库用户 | aisystem | aisystem |
| `MYSQL_PASSWORD` | 数据库密码 | aisystem123 | **强密码** |
| `DATABASE_URL` | Prisma连接串 | 自动生成 | 自动生成 |

### 缓存配置

| 变量名 | 说明 | 开发环境 | 生产环境 |
|--------|------|----------|----------|
| `REDIS_HOST` | Redis地址 | redis-service | redis-service |
| `REDIS_PORT` | Redis端口 | 6379 | 6379 |
| `REDIS_PASSWORD` | Redis密码 | redis123 | **强密码** |

### 安全配置

| 变量名 | 说明 | 开发环境 | 生产环境 |
|--------|------|----------|----------|
| `JWT_SECRET` | JWT密钥 | 固定值 | **随机生成** |
| `JWT_EXPIRES_IN` | JWT过期时间 | 2h | 2h |

---

## 下一步计划

### 已完成 ✅
- [x] Docker镜像构建问题
- [x] TypeScript编译错误
- [x] API地址配置
- [x] 前端热更新
- [x] Tailwind CSS配置
- [x] 运行时配置方案
- [x] 使用文档

### 待优化 📋
- [ ] 前端页面显示问题（如果存在）
- [ ] 性能优化
- [ ] 监控和日志
- [ ] 自动化测试
- [ ] CI/CD流程

---

## 相关文档

- **使用手册**: `使用手册.md`
- **环境配置**: `.env.development` / `.env.production`
- **API文档**: http://localhost:3000/api-docs
- **Docker配置**: `docker-compose.yml`
- **前端配置**: `frontend/.umirc.ts`
- **后端配置**: `backend/src/app.module.ts`

---

**文档版本**: 1.0  
**更新时间**: 2026-04-25  
**维护者**: 雷犀科技开发团队
