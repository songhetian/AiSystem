# 企业级后台开发需求功能文档（完整版）

# 三、全局统一规范（强制）

## 3.1 组件统一（禁止自定义）

所有页面必须使用指定统一组件，禁止自定义同类组件（特殊场景需产品经理书面审批，审批通过后同步至开发、测试全团队），组件使用要求如下：

- BaseTable：统一表格（支持分页、排序、筛选、批量操作，默认配置统一分页参数：每页10条，最大支持50条/页）

- BaseForm：统一表单（支持表单验证、联动、禁用状态控制，表单布局统一为上下布局，标签宽度120px）

- BaseModal：统一弹窗（弹窗标题、按钮（确认/取消）样式统一，弹窗大小按内容自适应，最小宽度400px）

- BaseDrag：统一拖拽（拖拽样式、触发反馈、动画效果统一，拖拽过程中显示临时占位符）

- BaseUpload：统一上传（支持单文件/多文件上传、拖拽上传、文件格式/大小校验，默认限制单个文件不超过100MB）

- ActionGroup：操作组（查看/编辑/删除，按钮顺序固定为“查看→编辑→删除”，禁用状态统一灰化，hover效果一致）

- Permission：按钮/区域权限控制（无权限时，按钮隐藏、区域置灰且不可点击，禁止仅隐藏不做权限拦截）

- StatusTag：状态标签（状态颜色、文案统一，如：启用=绿色、禁用=灰色、异常=红色，标签圆角统一为4px）

## 3.2 权限统一（RBAC 五表）

权限体系基于RBAC五表设计，五表字段、关联关系需严格遵循以下规范，禁止随意新增/修改字段：

1. 用户表：包含id、create_time、update_time、is_deleted、username、password（加密存储）、name、phone、email、status（启用/禁用）、last_login_time

2. 角色表：包含id、create_time、update_time、is_deleted、role_name、role_code（唯一）、description、status

3. 菜单表：包含id、create_time、update_time、is_deleted、menu_name、menu_code、parent_id、icon、route、sort、type（目录/菜单/按钮）、status

4. 按钮表：包含id、create_time、update_time、is_deleted、button_name、button_code、menu_id、status

5. 接口权限表：包含id、create_time、update_time、is_deleted、api_path、request_method、api_name、role_ids（关联角色）、status、platform_id（关联平台）、dept_id（关联部门）、shop_id（关联店铺）

**控制级别（强制落地，无例外）**：

- 菜单是否可见：基于角色关联的菜单权限控制，无权限菜单不显示在侧边栏，且路由拦截禁止直接访问

- 按钮是否可点击：基于Permission组件，无权限按钮隐藏，禁止出现“可见不可点”的无效状态

- 接口是否可调用：接口层添加权限守卫，结合角色、平台、部门、店铺权限校验，无权限调用时返回403，禁止前端仅隐藏按钮不做接口拦截

- 数据是否可查看（数据权限）：基于用户所属角色、部门、关联平台及店铺，控制数据可见范围（如：部门管理员仅可见本部门、本关联平台及店铺的数据）

## 3.3 数据库统一规范

- 所有表必须包含基础字段：`id`（主键，自增/雪花ID，统一使用雪花ID）、`create_time`（创建时间，格式：YYYY-MM-DD HH:MM:SS）、`update_time`（更新时间，自动更新）、`is_deleted`（逻辑删除标识）

- **统一逻辑删除**：`is_deleted = 1`（删除）、`is_deleted = 0`（未删除），禁止修改逻辑删除标识值

- 不允许物理删除：所有删除操作均为逻辑删除，禁止使用DELETE语句，仅允许更新is_deleted字段

- 统一使用 **Prisma** 操作：所有数据库CRUD操作必须通过Prisma实现，禁止直接写SQL语句；Prisma模型需与数据库表一一对应，字段注释完整

- 字段规范：字段命名统一使用下划线命名法（如：user_name），禁止使用驼峰命名；字段类型统一，如：时间字段用DateTime，状态字段用Int（0/1/2），禁止使用Boolean类型（避免跨环境兼容问题）

## 3.4 拖拽统一规范

- 统一使用：`@dnd-kit` 插件，版本固定（需与前端技术栈版本匹配，由前端负责人确认后同步至文档）

- 所有拖拽功能必须基于 `BaseDrag` 组件扩展：禁止直接使用@dnd-kit原生API开发，需复用BaseDrag组件的统一样式和逻辑

- 拖拽后自动保存排序：拖拽完成后，需立即调用接口保存排序结果，保存失败时显示错误提示，且恢复拖拽前状态

- 拖拽交互规范：拖拽时显示半透明占位符，拖拽结束有轻微动画反馈；禁止拖拽超出边界，禁止拖拽时触发其他操作（如：点击、选中）

## 3.5 Docker 详细配置规范（新增）

所有服务均需通过Docker容器化部署，统一使用Docker Compose管理容器，配置文件标准化，确保开发、测试、生产环境一致性，具体配置如下：

### 3.5.1 通用配置要求

- Docker版本：统一使用24.0.0及以上版本，Docker Compose版本：v2.20.0及以上

- 容器网络：统一创建自定义网络 `backend-network`，所有服务容器均加入该网络，禁止使用默认网络

- 数据持久化：所有服务数据均通过Docker Volume挂载，Volume命名规范：`服务名-data`（如：mysql-data），避免数据丢失

- 环境变量：统一使用 `.env` 文件管理环境变量，区分开发（.env.dev）、测试（.env.test）、生产（.env.prod）环境，敏感信息（密码、密钥）需加密存储，禁止硬编码

- 日志配置：所有容器日志统一输出至宿主机指定目录 `/var/log/docker/服务名`，日志保留7天，按天分割，禁止日志挂载至容器内部

### 3.5.2 MySQL 容器配置

- 镜像版本：mysql:8.0.36（稳定版），禁止使用其他版本

- 核心配置（docker-compose.yml）：
        `mysql:
  image: mysql:8.0.36
  container_name: mysql-service
  restart: always
  networks:
    - backend-network
  volumes:
    - mysql-data:/var/lib/mysql
    - ./mysql/conf:/etc/mysql/conf.d
    - /var/log/docker/mysql:/var/log/mysql
  environment:
    - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
    - MYSQL_DATABASE=${MYSQL_DATABASE}
    - MYSQL_USER=${MYSQL_USER}
    - MYSQL_PASSWORD=${MYSQL_PASSWORD}
  ports:
    - "3306:3306"
  command: --default-authentication-plugin=mysql_native_password --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci`

- 配置文件（my.cnf）：
        `[mysqld]
datadir=/var/lib/mysql
socket=/var/lib/mysql/mysql.sock
user=mysql
symbolic-links=0
max_connections=1000
wait_timeout=86400
interactive_timeout=86400
innodb_buffer_pool_size=1G
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

[mysqld_safe]
log-error=/var/log/mysql/mysqld.log
pid-file=/var/run/mysqld/mysqld.pid`

- 初始化：容器启动时自动执行 `./mysql/init.sql` 脚本，完成数据库初始化、用户授权、表结构创建

### 3.5.3 Redis 容器配置

- 镜像版本：redis:7.2.4（稳定版），启用持久化配置

- 核心配置（docker-compose.yml）：
        `redis:
  image: redis:7.2.4
  container_name: redis-service
  restart: always
  networks:
    - backend-network
  volumes:
    - redis-data:/data
    - ./redis/conf/redis.conf:/etc/redis/redis.conf
    - /var/log/docker/redis:/var/log/redis
  ports:
    - "6379:6379"
  command: redis-server /etc/redis/redis.conf --requirepass ${REDIS_PASSWORD}`

- 核心配置（redis.conf）：`bind 0.0.0.0
protected-mode no
port 6379
requirepass ${REDIS_PASSWORD}
daemonize no
pidfile /var/run/redis.pid
logfile /var/log/redis/redis.log
dbfilename dump.rdb
dir /data
save 900 1
save 300 10
save 60 10000
maxmemory 512mb
maxmemory-policy allkeys-lru`

- 注意：生产环境需开启密码认证，禁止暴露公网，仅允许内网访问

### 3.5.4 MinIO 容器配置（文件存储）

- 镜像版本：minio/minio:RELEASE.2024-05-01T00-08-08Z

- 核心配置（docker-compose.yml）：
        `minio:
  image: minio/minio:RELEASE.2024-05-01T00-08-08Z
  container_name: minio-service
  restart: always
  networks:
    - backend-network
  volumes:
    - minio-data:/data
    - /var/log/docker/minio:/var/log/minio
  ports:
    - "9000:9000"  # API端口
    - "9001:9001"  # 控制台端口
  environment:
    - MINIO_ROOT_USER=${MINIO_ROOT_USER}
    - MINIO_ROOT_PASSWORD=${MINIO_ROOT_PASSWORD}
  command: server /data --console-address ":9001"`

- 初始化：容器启动后，通过控制台创建存储桶（如：user-upload、shop-upload），配置桶的访问权限，绑定后台服务的访问密钥

- 用途：用于存储员工身份证正反面、商品图片、报销附件等所有上传文件，统一管理文件生命周期

### 3.5.5 Qdrant 容器配置（向量数据库）

- 镜像版本：qdrant/qdrant:v1.7.3

- 核心配置（docker-compose.yml）：
        `qdrant:
  image: qdrant/qdrant:v1.7.3
  container_name: qdrant-service
  restart: always
  networks:
    - backend-network
  volumes:
    - qdrant-data:/qdrant/storage
    - /var/log/docker/qdrant:/var/log/qdrant
  ports:
    - "6333:6333"  # API端口
    - "6334:6334"  # 控制台端口
  environment:
    - QDRANT__SERVICE__API_KEY=${QDRANT_API_KEY}
  command: --api-key ${QDRANT_API_KEY}`

- 用途：用于知识库文章向量检索、商品相似性推荐等场景，初始化时创建对应集合（如：knowledge-collection、product-collection）

### 3.5.6 Nginx 容器配置（反向代理、静态资源）

- 镜像版本：nginx:1.25.3

- 核心配置（docker-compose.yml）：
        `nginx:
  image: nginx:1.25.3
  container_name: nginx-service
  restart: always
  networks:
    - backend-network
  volumes:
    - ./nginx/conf:/etc/nginx/conf.d
    - ./nginx/html:/usr/share/nginx/html
    - ./nginx/logs:/var/log/nginx
    - minio-data:/usr/share/nginx/minio  # 挂载MinIO存储的静态资源
  ports:
    - "80:80"
    - "443:443"
  depends_on:
    - backend  # 依赖后台服务容器
    - minio`

- 核心配置（nginx.conf 片段）：
        `server {
  listen 80;
  server_name localhost;
  root /usr/share/nginx/html;
  index index.html;

  # 反向代理后台API
  location /api {
    proxy_pass http://backend:8080;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  # 静态资源访问（MinIO文件）
  location /minio {
    alias /usr/share/nginx/minio;
    expires 1d;
  }

  # 日志配置
  access_log /var/log/nginx/access.log;
  error_log /var/log/nginx/error.log;
}

# HTTPS配置（生产环境启用）
server {
  listen 443 ssl;
  server_name localhost;
  ssl_certificate /etc/nginx/conf.d/cert.pem;
  ssl_certificate_key /etc/nginx/conf.d/key.pem;
  # 其他SSL配置省略...
}`

- 用途：反向代理后台API请求、提供静态资源访问、处理HTTPS证书、负载均衡（多后台实例时启用）

### 3.5.7 容器部署与运维规范

- 部署命令：统一使用 `docker-compose -f docker-compose.yml --env-file .env.prod up -d` 启动服务，禁止单独启动单个容器

- 停止命令：`docker-compose down`，禁止使用 `docker rm -f` 强制删除容器（避免数据异常）

- 更新策略：先拉取最新镜像，再执行 `docker-compose up -d` 滚动更新，更新前需备份数据Volume

- 监控：配置Prometheus + Grafana监控所有容器CPU、内存、磁盘使用率，设置告警阈值

---

# 四、10 大核心模块功能清单（完整版）

## 模块 1：系统管理（system）【权限基座】

### 功能清单

1. 用户管理        
      

2. 基础操作：新增 / 编辑 / 禁用 / 重置密码 / 逻辑删除（禁用后用户无法登录，删除为逻辑删除）

3. 批量操作：批量启用 / 禁用、批量重置密码（批量操作需二次确认，避免误操作）

4. 角色分配：为用户分配单个/多个角色，支持批量分配角色；角色变更后即时生效（无需用户重新登录）

5. 辅助功能：用户搜索（支持用户名、手机号、邮箱模糊搜索）、用户导出（导出Excel，包含用户所有基础信息及角色）、用户详情查看

6. 角色管理        
      

7. 基础操作：新增 / 编辑 / 删除（删除前需校验该角色是否关联用户，关联用户则禁止删除，需先解除关联）

8. **菜单权限拖拽配置**：支持树形菜单拖拽，配置角色可访问的菜单；拖拽后自动保存，支持勾选“全选/反选”

9. **按钮权限勾选**：基于选中的菜单，勾选该菜单下的可操作按钮；权限配置后支持预览，确认无误后保存

10. 辅助功能：角色搜索（支持角色名称、角色编码模糊搜索）、角色详情查看、角色复制（快速创建相似角色）

11. 菜单管理        
      

12. 基础操作：新增 / 编辑 / 删除（删除前需校验该菜单是否关联角色、按钮，关联则禁止删除）

13. **菜单排序拖拽**：支持树形菜单拖拽排序，拖拽后自动保存排序结果，刷新页面后排序生效

14. 基础配置：图标（统一使用系统内置图标库）、路由（路由地址唯一，禁止重复）、层级配置（最多支持3级菜单）、菜单类型（目录/菜单/按钮）

15. 辅助功能：菜单搜索（支持菜单名称、路由模糊搜索）、菜单状态切换（启用/禁用）

16. 按钮管理        


17. 关联配置：按菜单绑定按钮，一个菜单可关联多个按钮，按钮不可跨菜单绑定

18. 基础配置：按钮标识（button_code，唯一）、按钮名称、权限码（与接口权限码对应）、按钮图标

19. 辅助操作：按钮新增/编辑/删除、按钮搜索（支持按钮名称、标识模糊搜索）

20. 接口管理

21. 基础配置：接口路径（唯一）、请求方式（GET/POST/PUT/DELETE，统一大写）、接口名称、接口描述、platform_id（关联平台，可多选）、dept_id（关联部门，可多选）、shop_id（关联店铺，可多选）

22. 权限绑定：关联角色，配置哪些角色可调用该接口；支持批量绑定角色，同时关联平台、部门、店铺，实现接口权限按平台+部门+店铺隔离

23. 辅助操作：接口新增/编辑/禁用、接口搜索（支持接口路径、接口名称、平台、部门、店铺模糊搜索）、接口列表导出

24. 权限控制：用户仅能调用自己所属平台、部门、店铺关联的接口，后台可按平台、部门、店铺筛选查看接口列表及调用记录

25. 操作日志、登录日志        
      

26. 操作日志：全记录（用户操作时间、操作人、操作模块、操作内容、操作IP、操作结果、关联平台ID、关联部门ID、关联店铺ID），不可删除、不可编辑

27. 登录日志：全记录（登录人、登录时间、登录IP、登录设备、登录结果（成功/失败）、关联平台ID），不可删除、不可编辑

28. 辅助功能：日志搜索（支持多条件组合搜索，含平台、部门、店铺筛选）、日志导出、日志分页查看

### 拖拽功能

- ✅ 角色权限菜单 **拖拽配置**：树形结构拖拽，支持层级调整、顺序调整，拖拽后即时预览权限范围

- ✅ 后台菜单 **拖拽排序**：树形结构拖拽，支持同级菜单排序、跨层级调整，拖拽后自动保存

---

## 模块 2：人事组织（user）

### 功能清单

1. 部门管理        
      

2. 基础操作：新增 / 编辑 / 排序 / 删除（删除前需校验该部门是否关联员工、岗位，关联则禁止删除，需先转移员工/岗位）

3. 结构展示：树形结构展示，支持展开/折叠，显示部门层级、部门人数

4. 辅助配置：部门负责人（关联员工）、部门描述、部门状态（启用/禁用）、关联平台ID（支持关联多个平台，与电商平台绑定）

5. 辅助操作：部门搜索（支持部门名称、关联平台模糊搜索）、部门详情查看、部门导出

6. 员工管理        
      

7. **基础信息维护（参考钉钉员工字段）**：姓名、性别、年龄、手机号、邮箱、入职时间、离职状态、所属部门（支持跨部门关联，最多关联2个部门）、岗位、工号（唯一）、员工编号、入职部门、转正时间、劳动合同到期时间、职级、岗位序列、汇报人（关联员工）、办公地点、工牌照片、身份证正面照片、身份证反面照片、紧急联系人姓名、紧急联系人电话、户籍所在地、政治面貌、学历、毕业院校、专业、社保公积金缴纳基数、社保缴纳地

8. **身份证正反面上传**：支持通过BaseUpload组件拖拽/点击上传，格式限制为JPG/PNG，单个文件不超过10MB，上传后可预览、替换、删除，文件存储至MinIO容器，关联员工ID，仅管理员及员工本人可查看

9. 关联配置：所属部门（支持跨部门关联，最多关联2个部门）、角色（支持分配多个角色）、关联平台ID（关联员工所属平台，与电商平台、部门绑定）

10. 状态管理：在职/离职切换（离职后标记离职时间，不可删除员工信息）、员工禁用/启用（禁用后无法登录系统）、试用期/转正状态切换

11. 辅助操作：员工搜索（多条件搜索，支持按平台、部门、职级、入职时间等筛选）、员工详情查看、员工信息导出、批量导入/导出员工、员工信息打印（含身份证正反面缩略图）

12. 岗位管理        
      

13. 基础操作：增删改查（删除前需校验该岗位是否关联员工，关联则禁止删除）

14. 基础配置：岗位名称、岗位编码（唯一）、岗位描述、所属部门、岗位等级、岗位序列、关联平台ID（与部门、电商平台绑定）

15. 辅助操作：岗位搜索（支持按岗位名称、所属部门、关联平台搜索）、岗位详情查看、岗位导出、批量调整岗位所属部门

### 拖拽功能

- ✅ 部门树形 **拖拽排序**：树形结构拖拽，支持同级部门排序、跨层级调整（如：子部门调整为父部门），拖拽后自动保存

- ✅ 岗位排序 **拖拽**：按部门分组，拖拽调整同部门内岗位顺序，拖拽后自动保存

---

## 模块 3：考勤排班（attendance）

### 功能清单

1. 考勤规则配置        
      

2. 基础规则：上班时间、下班时间、迟到/早退判定标准（如：迟到15分钟内不记罚）、旷工判定标准

3. 弹性考勤：支持配置弹性上班/下班时间（如：上班时间8:30-9:30，下班时间17:30-18:30）

4. 假期配置：关联法定节假日、公司自定义假期（如：年假、婚假），假期期间无需考勤

5. 规则管理：新增/编辑/删除规则、规则启用/禁用（同一时间仅生效一套主规则）、关联平台ID、部门ID（支持按平台+部门配置不同考勤规则）

6. 排班表管理        
      

7. 排班配置：按平台、部门、按人员、按日期排班，支持固定排班、循环排班（如：周一至周五上班，周末休息），排班信息关联平台+部门，仅可见本平台、本部门排班

8. 排班调整：支持单个/批量调整排班，调整后通知对应员工

9. 排班查看：按日/周/月查看排班表，支持按平台、部门筛选，支持导出排班表

10. 考勤记录 / 异常记录        
      

11. 考勤记录：自动同步打卡数据（上下班打卡），显示打卡时间、打卡地点、打卡状态（正常/迟到/早退/旷工）、关联平台ID、部门ID

12. 异常记录：记录迟到、早退、旷工、漏打卡等异常，显示异常原因、异常时长

13. 异常处理：支持员工提交异常申诉，管理员审核申诉（通过/驳回），审核结果同步至员工

14. 加班 / 请假审批联动       
      

15. 加班管理：员工提交加班申请（加班时间、加班原因），管理员审批；加班时长自动计入考勤统计，申请关联平台ID、部门ID

16. 请假管理：员工提交请假申请（请假类型、请假时长、请假原因），管理员审批；请假期间考勤标记为正常

17. 联动规则：请假/加班审批通过后，自动更新考勤记录、排班表，无需手动调整

### 拖拽功能

- ✅ 排班表 **人员拖拽排班**：在排班表中，拖拽人员至对应日期、对应班次，完成排班，拖拽后自动保存

- ✅ 排班日期段 **拖拽选择**：拖拽选择日期范围，批量为选中日期配置排班规则（如：批量选择1-7号，设置为正常班次）

---

## 模块 4：审批流程（approval）

### 功能清单

1. 审批模板管理       
      

2. 模板配置：新增审批模板（如：请假审批、报销审批、采购审批），配置模板名称、审批类型、表单字段（自定义表单）、关联平台ID、部门ID（支持按平台+部门配置专属模板）

3. 模板管理：编辑/删除模板、模板启用/禁用（禁用后无法使用该模板发起审批）

4. 模板复制：支持复制现有模板，快速创建相似模板，减少重复配置

5. 审批流程配置        
      

6. 流程节点：配置审批节点（如：申请人→部门负责人→HR→总经理），支持多节点、分支节点（如：金额>1000元需总经理审批，否则HR审批）

7. 节点配置：为每个节点分配审批人（单个/多个，多个审批人支持“或签”“会签”）、审批时限（超时提醒）

8. 流程预览：配置完成后，可预览审批流程，确认无误后保存

9. 我的审批 / 待我审批 / 已审批        
      

10. 我的审批：显示自己发起的所有审批，查看审批状态、审批进度、审批意见，支持按平台、部门筛选

11. 待我审批：显示需要自己审批的所有申请，支持同意、驳回、转审操作，可添加审批意见

12. 已审批：显示自己已处理的所有审批，查看审批记录、审批意见、最终结果

13. 审批驳回 / 同意 / 转审        
      

14. 同意：审批通过，自动流转至下一个节点；若为最后一个节点，审批完成

15. 驳回：审批驳回，返回至申请人，申请人可修改后重新提交，需填写驳回原因

16. 转审：将审批任务转交给其他人员审批，需选择转审人、填写转审原因，转审后自己不再参与该审批

17. 辅助功能       
      

18. 审批提醒：待审批任务有消息提醒（系统内消息+短信提醒，可配置提醒频率）

19. 审批搜索：多条件搜索审批（申请人、审批类型、审批状态、日期范围、平台、部门）

20. 审批导出：导出审批记录，包含审批详情、审批意见、审批流程

### 拖拽功能

- ✅ 审批流程节点 **拖拽编排**：拖拽节点调整审批顺序，拖拽分支节点配置审批条件，拖拽后自动保存流程结构

- ✅ 审批人 / 抄送人 **拖拽选择**：在节点配置中，拖拽人员至审批人/抄送人列表，完成分配，支持批量拖拽

---

## 模块 5：行政财务（finance）

### 功能清单

1. 费用类型管理        
      

2. 类型配置：新增费用类型（如：差旅费、办公费、业务招待费），配置类型名称、类型编码、描述、关联平台ID、部门ID（支持按平台+部门配置专属费用类型）

3. 类型管理：编辑/删除类型（删除前需校验该类型是否关联报销记录，关联则禁止删除）、类型启用/禁用

4. 报销申请 / 审批        
      

5. 报销申请：员工提交报销申请，选择费用类型、填写报销金额、报销事由、上传报销附件，关联加班/出差记录（可选）、关联平台ID、部门ID、店铺ID（电商相关报销需关联店铺）

6. 报销审批：关联审批流程，按流程节点审批，审批人可查看报销附件、报销详情，添加审批意见

7. 报销管理：报销通过后，标记为“待打款”；打款完成后，标记为“已打款”；支持报销撤回（未审批前）、报销修改

8. 采购申请        
      

9. 采购申请：员工提交采购申请，填写采购物品、数量、单价、采购原因、预计金额，上传采购需求附件，关联平台ID、部门ID、店铺ID（电商店铺采购需关联对应店铺）

10. 采购审批：关联审批流程，审批通过后，标记为“待采购”；采购完成后，更新采购状态为“已完成”，填写实际采购金额

11. 采购跟踪：查看采购申请进度、采购状态，支持采购申请撤回、修改

12. 收支记录        
      

13. 收入记录：记录公司收入（如：营收、回款），填写收入金额、收入来源、收入日期、备注、关联平台ID、部门ID、店铺ID（电商店铺收入需关联对应店铺）

14. 支出记录：记录公司支出（如：报销打款、采购付款），关联报销/采购记录，自动同步金额、日期、平台、部门、店铺信息

15. 记录管理：新增/编辑/删除收支记录（删除需管理员权限），支持收支记录搜索（可按平台、部门、店铺筛选）、导出

16. 数据统计        
      

17. 报销统计：按费用类型、部门、时间段、平台、店铺统计报销金额、报销笔数，生成统计图表（柱状图、饼图）

18. 采购统计：按采购物品、部门、时间段、平台、店铺统计采购金额、采购数量，生成统计图表

19. 收支统计：按时间段、收入/支出类型、平台、部门、店铺统计收支总额，生成收支对比图表，支持统计数据导出

### 拖拽功能

- ✅ 报销附件 **拖拽上传**：支持多文件拖拽上传，上传后可预览、删除，附件大小不超过100MB/个

- ✅ 表格列展示项 **拖拽自定义**：在收支记录、报销记录等表格中，拖拽调整列的显示顺序、显示/隐藏列，保存个人自定义配置

---

## 模块 6：电商平台（shop）【优化：与平台、部门店铺绑定】

### 基础关联规则（新增）

电商平台核心关联逻辑：平台 → 部门 → 店铺，三者层级绑定，所有电商相关数据（商品、订单、活动等）均关联平台ID、部门ID、店铺ID，实现数据隔离；用户仅能查看/操作自己所属平台、部门及关联店铺的内容，管理员可按平台、部门、店铺筛选管理所有内容。

- 平台管理：新增平台配置（如：主平台、子平台），每个平台可关联多个部门，每个部门可关联多个店铺，禁止跨平台、跨部门关联店铺

- 店铺管理：店铺必须关联所属部门及平台，店铺信息与部门信息联动（如：部门名称变更，店铺所属部门名称同步变更）

- 数据隔离：商品、订单、活动等数据均绑定平台+部门+店铺，用户登录后仅可见本平台、本部门、本关联店铺的数据，接口调用需校验平台、部门、店铺权限

### 功能清单（优化后）

1. 平台管理（新增）
        

    - 基础配置：平台名称、平台编码（唯一）、平台描述、平台状态（启用/禁用）、平台负责人（关联员工）

    - 关联配置：关联部门（支持关联多个部门，每个部门可关联多个店铺），支持批量关联/解除关联部门

    - 操作管理：新增/编辑/删除平台（删除前需校验平台是否关联部门、店铺，关联则禁止删除）、平台搜索、平台详情查看

2. 店铺管理（新增，与部门、平台绑定）
        

    - 基础配置：店铺名称、店铺编码（唯一）、店铺类型（线上/线下）、店铺地址、联系方式、店铺头像、店铺描述

    - 关联配置：必须关联所属平台ID、所属部门ID（部门需先关联平台）、店铺负责人（关联部门内员工），禁止跨部门、跨平台关联店铺

    - 状态管理：店铺启用/禁用（禁用后无法上架商品、接收订单）、店铺装修（支持基础装修配置）

    - 操作管理：新增/编辑/删除店铺（删除前需校验店铺是否关联商品、订单，关联则禁止删除）、店铺搜索（支持按平台、部门、店铺名称搜索）、店铺详情查看

3. 商品管理（优化：与平台、部门、店铺绑定）
        

    - 基础信息：商品名称、商品编码（唯一，按平台+店铺编码前缀生成）、商品分类、商品描述、商品图片（多图上传，存储至MinIO）

    - 关联配置：必须关联平台ID、部门ID、店铺ID（店铺需关联对应平台和部门），一个商品可关联多个同平台、同部门的店铺，禁止跨平台、跨部门关联商品

    - 规格配置：商品SKU规格（如：颜色、尺寸），配置SKU价格、库存、库存预警值，库存按店铺单独管理（不同店铺可设置不同库存、价格）

    - 状态管理：商品上架/下架、热销/普通标记，下架商品不可在对应店铺前端展示；商品状态按店铺单独控制（同一商品可在部分关联店铺上架，部分下架）

    - 操作管理：新增/编辑/删除商品（删除前需校验商品是否关联订单，关联则禁止删除）、商品搜索（支持按平台、部门、店铺、商品名称筛选）、商品导出（可按平台、部门、店铺导出）

4. 订单管理（优化：与平台、部门、店铺绑定）
        

    - 订单查看：查看所有关联自己所属平台、部门、店铺的订单，显示订单号、商品信息、订单金额、支付状态、发货状态、收货信息、所属平台、所属部门、所属店铺

    - 订单关联：订单自动关联下单店铺的平台ID、部门ID，不可手动修改；管理员可按平台、部门、店铺筛选所有订单，普通用户仅可见本关联店铺的订单

    - 订单操作：订单发货（填写物流信息，关联对应店铺）、订单取消（未支付订单可取消）、订单退款（支持退款申请审核，退款记录关联平台、部门、店铺）

    - 订单筛选：按订单状态、支付方式、时间段、商品名称、平台、部门、店铺等多条件筛选订单

    - 订单统计：按时间段、平台、部门、店铺统计订单数量、订单总额、退款金额，生成统计图表，支持按平台、部门、店铺单独统计

5. 商品分类管理（优化：与平台、部门、店铺绑定）


    - 分类配置：新增商品分类（支持多级分类，最多3级），配置分类名称、分类图标、排序，必须关联平台ID、部门ID（分类仅对同平台、同部门的店铺可见）

    - 分类关联：分类可关联同平台、同部门的多个店铺，不同平台、部门可创建相同名称的分类，互不影响

    - 分类管理：编辑/删除分类（删除前需校验分类是否关联商品，关联则禁止删除）、分类启用/禁用（禁用后对应店铺不可使用该分类）

6. 活动管理（优化：与平台、部门、店铺绑定）
        

    - 活动配置：新增活动（如：满减、折扣），配置活动名称、活动时间、活动规则，必须关联平台ID、部门ID，选择参与活动的店铺（仅可选择同平台、同部门的店铺）

    - 活动关联：活动仅对关联的平台、部门、店铺生效，不同平台、部门可创建相同活动，互不影响；活动商品仅可选择关联店铺内的商品

    - 活动管理：编辑/删除活动、活动启用/禁用（禁用后活动不生效），查看活动参与数据（按平台、部门、店铺统计参与人数、订单金额）

### 拖拽功能

- ✅ 商品排序 **拖拽**：在商品列表中，按平台、部门、店铺筛选后，拖拽商品调整对应店铺的展示顺序，拖拽后自动保存，前端按排序展示

- ✅ 商品 SKU 规格 **拖拽排序**：在商品规格配置中，拖拽SKU调整显示顺序，拖拽后自动保存，按店铺单独生效

- ✅ 活动规则配置 **拖拽模块**：拖拽活动模块（如：满减规则、折扣规则），配置活动逻辑，拖拽后自动保存活动配置，关联对应平台、部门、店铺

- ✅ 店铺排序 **拖拽**：按平台、部门筛选后，拖拽店铺调整展示顺序，拖拽后自动保存，前端按排序展示

---

## 模块 7：客服质检（service）【核心风控模块】

### 功能清单

1. 客服账号管理        
  

2. 基础信息：客服姓名、工号（唯一）、手机号、邮箱、所属坐席组、关联平台ID、部门ID、店铺ID（电商客服需关联对应店铺）

3. 账号配置：登录账号（与工号绑定，唯一）、初始密码（自动生成，需强制首次登录修改）、账号状态（启用/禁用，禁用后无法登录客服系统）

4. 关联配置：坐席组分配（支持单个/批量分配）、关联平台+部门+店铺（仅可关联同平台、同部门的店铺，客服仅处理关联店铺的咨询）

5. 操作管理：新增/编辑/禁用客服账号、重置密码、客服账号搜索（支持按平台、部门、店铺、工号、姓名筛选）、账号详情查看

6. 坐席组管理        
  

7. 基础配置：坐席组名称、组编码（唯一）、组描述、关联平台ID、部门ID（坐席组仅对同平台、同部门可见）

8. 成员管理：添加/移除客服成员，支持批量添加，显示组内客服人数、在线状态

9. 权限配置：配置坐席组可处理的业务类型（如：商品咨询、订单售后、投诉处理）、可查看的店铺范围（仅关联平台+部门下的店铺）

10. 操作管理：新增/编辑/删除坐席组（删除前需转移组内客服成员）、坐席组搜索（支持按平台、部门、组名称筛选）

11. 会话管理        


12. 会话查看：按平台、部门、店铺、客服、会话状态（未结束/已结束/转接中）筛选查看所有客服会话，显示会话ID、客服姓名、客户昵称、咨询店铺、会话开始/结束时间、会话时长

13. 会话操作：会话转接（支持转接至同平台、同部门的其他坐席组/客服）、会话结束（客服主动结束或系统超时自动结束）、会话备注（添加会话关键信息，关联平台+部门+店铺）

14. 会话记录：完整保存会话内容（文字、图片、文件），支持按关键词搜索会话，会话记录永久留存，不可删除，关联对应平台ID、部门ID、店铺ID

15. 质检规则配置        
  

16. 规则配置：新增质检规则（如：响应时长、服务态度、业务熟练度、违规话术），配置规则名称、规则类型、扣分标准、达标阈值，关联平台ID、部门ID（支持按平台+部门配置不同质检规则）

17. 违规话术管理：添加/编辑/删除违规话术（如：辱骂客户、推诿责任），支持批量导入/导出，违规话术按平台+部门分组管理，互不影响

18. 规则管理：规则启用/禁用（同一平台+部门可同时生效多个规则）、编辑/删除规则（删除前需校验规则是否关联质检记录）

19. 质检操作        
  

20. 质检方式：手动质检（管理员按平台、部门、店铺筛选会话，手动打分、添加质检意见）、自动质检（系统按配置规则自动检测会话，标记违规项、自动打分）

21. 质检结果：生成质检报告，显示客服姓名、坐席组、关联平台/部门/店铺、会话ID、质检分数、违规项、扣分详情、质检人、质检时间

22. 异常处理：对质检不合格的会话，可发起整改通知，客服需提交整改说明，管理员审核整改结果，整改记录关联平台+部门+店铺

23. 数据统计        
 

24. 客服统计：按平台、部门、店铺、坐席组统计客服的在线时长、接会话量、平均响应时长、质检平均分、违规次数，生成统计图表

25. 会话统计：按平台、部门、店铺、时间段统计会话总量、未结束会话数、转接会话数、客户满意度，生成统计图表

26. 质检统计：按平台、部门、店铺、规则类型统计质检合格率、违规项分布、整改完成率，支持统计数据导出，按平台+部门+店铺单独筛选统计

27. 客户满意度管理        
  

28. 满意度收集：会话结束后，自动向客户推送满意度评价（如：非常满意、满意、一般、不满意），关联对应平台、部门、店铺及会话信息

29. 评价管理：查看所有满意度评价，按平台、部门、店铺、客服、评价等级筛选，显示评价内容、评价时间、关联会话ID

30. 差评处理：对不满意评价，自动提醒对应客服及管理员，客服需提交差评处理说明，管理员审核处理结果，处理记录关联平台+部门+店铺

### 拖拽功能

- ✅ 坐席组成员 **拖拽调整**：在坐席组管理页面，拖拽客服成员调整组内排序，拖拽后自动保存，支持批量拖拽转移成员至其他坐席组（仅同平台、同部门坐席组可转移）

- ✅ 质检规则 **拖拽排序**：按平台、部门筛选后，拖拽质检规则调整生效优先级，拖拽后自动保存，优先级高的规则先执行质检

- ✅ 会话列表列 **拖拽自定义**：在会话列表页面，拖拽调整列的显示顺序、显示/隐藏列（如：关联店铺、会话时长），保存个人自定义配置

---

## 模块 8：知识库（knowledge）【支持向量检索】

### 功能清单

1. 知识库分类管理        
  

2. 分类配置：新增知识库分类（支持多级分类，最多3级），配置分类名称、分类编码（唯一）、分类描述、关联平台ID、部门ID（分类仅对同平台、同部门可见）

3. 分类关联：分类可关联同平台、同部门的多个店铺（电商相关分类需关联店铺，仅店铺相关人员可见），不同平台、部门可创建相同名称的分类，互不影响

4. 分类管理：编辑/删除分类（删除前需校验分类是否关联知识库文章，关联则禁止删除）、分类启用/禁用（禁用后对应平台+部门+店铺不可查看该分类下的文章）、分类排序（支持拖拽排序）

5. 知识库文章管理

6. 基础配置：文章标题、文章内容（支持富文本编辑，插入图片、表格、链接）、作者（自动关联登录用户）、发布时间、更新时间、关联平台ID、部门ID、店铺ID（可选，关联店铺后仅该店铺人员可见）

7. 向量检索配置：文章发布后，自动同步至Qdrant向量数据库，生成文章向量，支持按关键词、语义进行相似性检索，检索结果按关联平台、部门、店铺权限过滤

8. 状态管理：草稿/已发布/下架，草稿仅作者可见，已发布文章对同平台、同部门及关联店铺人员可见，下架后不可查看

9. 操作管理：新增/编辑/删除/下架文章（删除前需校验文章是否被引用）、文章搜索（支持关键词检索、语义检索，可按平台、部门、店铺、分类筛选）、文章导出、文章收藏

10. 文章检索功能        
  

11. 基础检索：支持关键词模糊检索，检索结果按匹配度排序，显示文章标题、分类、关联平台/部门/店铺、发布时间

12. 向量检索：支持语义检索（输入自然语言，检索相似内容的文章），检索结果标注匹配度，可筛选关联平台、部门、店铺范围

文章检索功能        
  

- 基础检索：支持关键词模糊检索，检索结果按匹配度排序，显示文章标题、分类、关联平台/部门/店铺、发布时间

- 向量检索：支持语义检索（输入自然语言，检索相似内容的文章），检索结果标注匹配度，可筛选关联平台、部门、店铺范围

- 检索辅助：支持检索历史记录、检索结果收藏、检索关键词推荐（基于历史检索记录）

文章权限管理        
  

- 查看权限：按平台+部门+店铺控制，普通用户仅可见自己所属平台、部门及关联店铺的文章，管理员可查看所有平台、部门、店铺的文章

- 编辑权限：仅文章作者、管理员可编辑文章，编辑后自动更新更新时间，保留编辑记录

- 引用权限：文章可被其他模块（如：客服质检、审批流程）引用，引用记录关联平台、部门、店铺，可查看文章被引用情况

数据统计        
  

- 文章统计：按平台、部门、店铺、分类统计文章数量、发布数量、下架数量、检索次数、收藏次数，生成统计图表

- 检索统计：按平台、部门、店铺统计检索次数、检索关键词、检索命中率，生成统计图表，支持数据导出

接口监控数据功能（新增，优化版）        
  

- 监控核心配置：支持配置需监控的接口列表（关联接口管理模块的接口，需绑定平台ID、部门ID、店铺ID，仅可选择用户有权限的接口），配置监控数据字段（如：接口响应时间、调用成功率、返回数据量、错误码分布），每个接口可自定义监控字段优先级

- 自助获取监控数据（手动触发）：支持用户按平台、部门、店铺筛选监控接口，选择监控时间范围（今日/昨日/近7天/近30天/自定义时间段），手动触发数据获取，获取后即时展示监控数据详情及趋势图表；支持单接口、多接口批量获取，可导出监控数据（Excel格式，包含接口信息、监控字段、时间节点、关联平台/部门/店铺）

- 定时获取监控数据（自动触发）：支持用户自定义定时规则，配置定时周期（每小时/每天/每周/每月，可精确到具体时间点，如：每天00:00获取昨日数据），设置监控数据保留时长（如：保留3个月，过期自动清理）；定时获取完成后，自动生成监控报告，同步发送系统消息通知配置人，支持按平台、部门、店铺分组设置不同定时规则

- 监控数据展示：按接口分组展示监控数据，支持折线图、柱状图、饼图切换，直观展示接口响应时间变化、调用成功率趋势、错误码分布；支持数据钻取，点击监控数据可查看具体时间节点的接口调用详情、异常数据明细，关联接口调用日志

- 异常监控与提醒：配置监控阈值（如：接口响应时间超过500ms、调用成功率低于95%），超过阈值自动标记异常，触发系统消息提醒（支持系统内消息、短信通知，可配置提醒接收人）；保存异常监控记录，显示异常时间、异常接口、异常字段、关联平台/部门/店铺、异常详情，支持异常处理标记（未处理/已处理）

- 监控规则管理：新增/编辑/删除监控规则（删除前需校验规则是否关联监控数据，关联则禁止删除）；支持监控规则启用/禁用，同一接口可配置多个监控规则（按优先级生效）；支持按平台、部门、店铺筛选监控规则，批量调整规则状态

### 拖拽功能

- ✅ 知识库分类 **拖拽排序**：按平台、部门筛选后，拖拽分类调整显示顺序，支持多级分类拖拽调整层级，拖拽后自动保存

- ✅ 文章排序 **拖拽**：在分类详情页面，拖拽文章调整展示顺序，拖拽后自动保存，前端按排序展示，检索结果排序不受影响

- ✅ 富文本编辑 **拖拽排版**：编辑文章时，拖拽富文本模块（图片、表格、段落）调整排版顺序，拖拽后自动保存排版格式

- ✅ 监控接口 **拖拽排序**：在监控规则配置页面，拖拽已选择的监控接口，调整接口展示顺序及监控优先级，拖拽后自动保存

- ✅ 监控字段 **拖拽自定义**：在监控数据展示页面，拖拽调整监控字段的显示顺序、显示/隐藏，保存个人自定义配置，适配不同用户查看需求

---

## 模块 9：数据大屏（dashboard）【可视化统计】

### 功能清单

1. 大屏模板管理        
 

2. 模板配置：新增大屏模板（如：全局总览、电商运营、人事管理、客服质检、接口监控），配置模板名称、模板描述、关联平台ID、部门ID（支持按平台+部门配置专属模板）

3. 模板组件：模板包含固定统计组件（如：数据总览卡片、柱状图、饼图、折线图），新增接口监控组件（展示核心接口监控数据、异常提醒），支持自定义组件布局，组件数据关联平台+部门+店铺

4. 模板管理：编辑/删除模板、模板启用/禁用（禁用后无法查看该模板大屏）、模板复制（快速创建相似模板）、模板权限控制（按角色分配模板查看权限）

5. 数据可视化展示

6. 全局总览大屏：展示全平台核心数据（员工总数、部门总数、店铺总数、订单总额、报销总额、会话总量、核心接口调用成功率），支持按平台、部门筛选查看细分数据

7. 电商运营大屏：按平台、部门、店铺展示电商核心数据（商品总数、上架商品数、订单数量、订单总额、退款金额、客户满意度），生成趋势图、对比图，支持按时间段筛选（日/周/月/年）

8. 人事管理大屏：按平台、部门展示人事核心数据（在职员工数、新入职员工数、离职员工数、考勤正常率、请假/加班次数），生成统计图表，支持按时间段筛选

9. 客服质检大屏：按平台、部门、店铺展示客服核心数据（在线客服数、接会话量、平均响应时长、质检合格率、客户满意度），实时更新数据，支持异常预警（如：质检合格率低于阈值提醒）

10. 接口监控大屏（新增）：集中展示所有监控接口的核心数据（平均响应时间、调用成功率、异常次数），按平台、部门、店铺分组展示，支持异常接口高亮提醒，点击可钻取查看详细监控数据及趋势

11. 数据筛选与钻取        
  

12. 多条件筛选：支持按平台、部门、店铺、时间段（自定义时间段）筛选大屏数据，筛选后实时更新所有组件数据，接口监控组件支持按接口名称、监控字段筛选

13. 数据钻取：点击大屏组件（如：订单总额柱状图、接口响应时间折线图），可钻取查看细分数据（如：各店铺订单明细、接口不同时间节点的监控数据），支持多级钻取，钻取数据关联平台+部门+店铺权限

14. 大屏配置与分享        
  

15. 自定义配置：支持调整大屏组件大小、位置（拖拽调整），修改组件颜色、字体，保存个人自定义配置，不同用户可拥有不同配置；接口监控组件支持自定义监控字段展示、预警阈值设置

16. 大屏分享：支持生成大屏分享链接（链接有效期可配置），分享链接仅对拥有对应平台、部门、店铺权限的用户可见，禁止分享给无权限用户

17. 定时刷新：支持配置大屏数据刷新频率（如：1分钟/5分钟/10分钟），实时同步最新数据（含接口监控数据），刷新失败时显示错误提示

18. 异常预警        
  

19. 预警配置：设置核心数据预警阈值（如：订单退款率超过10%、质检合格率低于80%、接口响应时间超过500ms），关联平台ID、部门ID、店铺ID，支持按平台+部门+店铺设置不同阈值

20. 预警提醒：数据达到预警阈值时，大屏对应组件高亮显示，同时发送系统消息提醒管理员，提醒信息关联平台、部门、店铺及预警数据（接口监控异常需标注接口名称、异常字段）

21. 预警记录：保存所有预警记录，显示预警时间、预警类型、预警数据、关联平台/部门/店铺、处理状态（未处理/已处理），支持预警记录搜索、导出，可按预警类型（接口监控/订单/质检等）筛选

### 拖拽功能

- ✅ 大屏组件 **拖拽布局**：在大屏配置页面，拖拽组件调整位置、大小，支持组件对齐、均匀分布，拖拽后自动保存配置

- ✅ 组件排序 **拖拽**：拖拽大屏组件调整展示层级（上下顺序），拖拽后自动保存，层级高的组件显示在上方

- ✅ 筛选条件面板 **拖拽自定义**：拖拽调整筛选条件的显示顺序、显示/隐藏，保存个人自定义筛选面板配置

- ✅ 接口监控组件 **拖拽配置**：在大屏配置页面，拖拽接口监控组件至指定位置，调整组件大小，配置展示的监控接口及字段，拖拽后自动保存

---

## 模块 10：消息通知（message）【全模块联动】

### 功能清单

1. 通知模板管理

2. 模板配置：新增通知模板（如：审批通知、考勤异常通知、订单通知、客服预警通知、接口监控异常通知），配置模板名称、模板类型、通知内容（支持变量替换，如：${username}、${apiName}、${exceptionMsg}）、关联平台ID、部门ID、店铺ID（可选，关联店铺后仅该店铺人员接收通知）

3. 通知方式：支持系统内消息、短信、邮件三种通知方式，可配置单一方式或多种方式组合，不同模板可配置不同通知方式；接口监控异常通知可单独配置提醒频率（如：每10分钟提醒一次，直至异常解决）

4. 模板管理：编辑/删除模板、模板启用/禁用（禁用后不再发送该模板通知）、模板复制、模板变量管理（添加/编辑模板变量，关联对应模块数据，新增接口监控相关变量：接口名称、异常字段、异常详情、监控时间）

5. 消息发送管理        
  

6. 自动发送：关联各模块事件（如：审批提交、考勤异常、订单生成、客服差评、接口监控异常），触发事件后自动发送对应模板通知，通知接收人按平台、部门、店铺权限及模块关联关系分配（如：接口监控异常通知发送给对应平台+部门的管理员及接口负责人）

7. 手动发送：管理员可手动发送通知，选择通知模板、接收人（支持按平台、部门、店铺筛选选择）、补充通知内容，支持批量发送；可手动发送接口监控数据汇总通知，选择时间段及监控接口，生成通知内容后发送

8. 发送记录：保存所有通知发送记录，显示发送时间、通知模板、发送方式、接收人、发送状态（成功/失败）、关联平台/部门/店铺、关联模块事件（含接口监控异常事件），支持发送记录搜索、导出

9. 我的消息管理        
  

10. 消息查看：按消息类型（系统通知、审批通知、业务通知、接口监控通知）筛选查看个人消息，显示消息标题、消息内容、发送时间、关联平台/部门/店铺、已读/未读状态；接口监控相关消息可直接跳转至接口监控详情页面

11. 消息操作：标记已读/未读（支持批量标记）、删除消息（逻辑删除）、收藏消息、查看消息详情（显示关联模块数据，如：接口监控异常消息可查看接口监控明细）

12. 消息设置：自定义消息接收方式（如：仅接收系统内消息、不接收短信通知），设置消息免打扰时间段，可单独设置接口监控异常通知的接收方式及免打扰规则，保存个人消息设置

13. 消息权限控制        
  

14. 发送权限：仅管理员及拥有对应模块权限的用户可手动发送通知，发送范围受平台、部门、店铺权限限制（如：普通员工仅可发送本部门、本关联平台及店铺的通知）；接口监控通知仅管理员及接口负责人可手动发送

15. 接收权限：用户仅接收自己所属平台、部门、店铺及关联模块的通知，无权限的通知不展示、不推送；接口监控通知仅推送给对应平台+部门的管理员及接口负责人

16. 管理权限：管理员可查看所有平台、部门、店铺的消息发送记录，普通用户仅可查看自己的消息及自己发送的通知记录

17. 消息联动配置        
  

18. 模块联动：与审批流程、考勤排班、电商平台、客服质检、知识库（接口监控）等所有模块联动，触发对应事件自动发送通知（如：审批通过通知申请人、接口监控异常通知管理员及接口负责人）

19. 联动规则：配置事件与通知模板的关联关系，设置通知接收人规则（如：接口监控异常通知接收人为平台管理员、部门管理员、接口负责人），关联平台、部门、店铺，不同平台+部门可配置不同联动规则

### 拖拽功能

- ✅ 通知模板变量 **拖拽排序**：在模板编辑页面，拖拽模板变量调整显示顺序，拖拽后自动保存，变量替换后按排序显示在通知内容中

- ✅ 我的消息列表列 **拖拽自定义**：拖拽调整消息列表列的显示顺序、显示/隐藏列（如：关联店铺、发送方式、接口名称），保存个人自定义配置

- ✅ 联动规则 **拖拽编排**：拖拽模块事件（含接口监控异常事件）与通知模板，建立联动关系，拖拽调整联动规则的优先级，拖拽后自动保存

---

# 五、补充说明（强制落地）

## 5.1 数据权限补充

所有模块数据均严格按“平台→部门→店铺”层级隔离，用户登录后仅能查看/操作自己所属平台、部门及关联店铺的内容，具体规则如下：

- 超级管理员：可查看/操作所有平台、部门、店铺的所有数据，拥有全部操作权限

- 平台管理员：仅可查看/操作对应平台下所有部门、店铺的数据，无跨平台操作权限

- 部门管理员：仅可查看/操作对应部门及关联平台、关联店铺的数据，无跨部门操作权限

- 普通用户：仅可查看/操作自己所属部门、关联平台及绑定店铺的数据，仅拥有对应角色的操作权限；接口监控功能中，普通用户仅可查看自己有权限的接口监控数据，无配置监控规则权限

## 5.2 接口关联补充

所有API接口均与平台、部门、店铺绑定，接口调用时需携带platform_id、dept_id、shop_id（可选，非电商相关接口可省略shop_id），接口层自动校验权限，无对应平台、部门、店铺权限的调用将返回403错误；接口日志、接口统计均按平台、部门、店铺分组，支持按层级筛选查看；接口监控功能仅可监控已配置权限的接口，禁止监控无权限接口。

## 5.3 文件存储补充

所有上传文件（员工身份证正反面、商品图片、报销附件等）均统一存储至MinIO容器，按“平台ID/部门ID/店铺ID/文件类型”分类存储，文件访问需校验平台、部门、店铺权限，仅拥有对应权限的用户可访问、下载文件；文件保留策略可配置，定期清理过期文件，避免存储冗余；接口监控数据及报告自动存储至对应平台+部门目录，保留时长与监控规则配置一致。

## 5.4 Docker部署补充

Docker容器部署需严格遵循3.5节配置规范，开发、测试、生产环境使用不同的.env文件，禁止跨环境混用配置；容器启动后需检查服务连通性（如：MySQL、Redis、MinIO、Qdrant与后台服务的连通性），连通失败时自动重启容器，确保服务正常运行；定期备份容器Volume数据，防止数据丢失；接口监控相关服务需与后台服务、数据库容器同步部署，确保监控数据实时获取、稳定存储。

## 5.5 接口监控补充说明（新增）

接口监控功能需与接口管理模块严格联动，仅可选择已录入系统且用户有权限的接口进行监控；监控数据获取需通过合法接口调用，禁止非法抓取数据；定时获取监控数据时，需避开接口访问高峰时段（可在定时规则中配置避开时段），避免影响接口正常运行；监控数据异常时，系统自动记录异常日志，便于排查问题，异常日志关联平台、部门、店铺及接口信息，不可删除。

1. 检索辅助：支持检索历史记录、检索结果收藏、检索关键词推荐（基于历史检索记录）

2. 文章权限管理        
  

3. 查看权限：按平台+部门+店铺控制，普通用户仅可见自己所属平台、部门及关联店铺的文章，管理员可查看所有平台、部门、店铺的文章

4. 编辑权限：仅文章作者、管理员可编辑文章，编辑后自动更新更新时间，保留编辑记录

5. 引用权限：文章可被其他模块（如：客服质检、审批流程）引用，引用记录关联平台、部门、店铺，可查看文章被引用情况

6. 数据统计        
  

7. 文章统计：按平台、部门、店铺、分类统计文章数量、发布数量、下架数量、检索次数、收藏次数，生成统计图表

8. 检索统计：按平台、部门、店铺统计检索次数、检索关键词、检索命中率，生成统计图表，支持数据导出

### 拖拽功能

- ✅ 知识库分类 **拖拽排序**：按平台、部门筛选后，拖拽分类调整显示顺序，支持多级分类拖拽调整层级，拖拽后自动保存

- ✅ 文章排序 **拖拽**：在分类详情页面，拖拽文章调整展示顺序，拖拽后自动保存，前端按排序展示，检索结果排序不受影响

- ✅ 富文本编辑 **拖拽排版**：编辑文章时，拖拽富文本模块（图片、表格、段落）调整排版顺序，拖拽后自动保存排版格式

---

## 模块 9：数据大屏（dashboard）【可视化统计】

### 功能清单

1. 大屏模板管理        
  

2. 模板配置：新增大屏模板（如：全局总览、电商运营、人事管理、客服质检），配置模板名称、模板描述、关联平台ID、部门ID（支持按平台+部门配置专属模板）

3. 模板组件：模板包含固定统计组件（如：数据总览卡片、柱状图、饼图、折线图），支持自定义组件布局，组件数据关联平台+部门+店铺

4. 模板管理：编辑/删除模板、模板启用/禁用（禁用后无法查看该模板大屏）、模板复制（快速创建相似模板）、模板权限控制（按角色分配模板查看权限）

5. 数据可视化展示        
  

6. 全局总览大屏：展示全平台核心数据（员工总数、部门总数、店铺总数、订单总额、报销总额、会话总量），支持按平台、部门筛选查看细分数据

7. 电商运营大屏：按平台、部门、店铺展示电商核心数据（商品总数、上架商品数、订单数量、订单总额、退款金额、客户满意度），生成趋势图、对比图，支持按时间段筛选（日/周/月/年）

8. 人事管理大屏：按平台、部门展示人事核心数据（在职员工数、新入职员工数、离职员工数、考勤正常率、请假/加班次数），生成统计图表，支持按时间段筛选

9. 客服质检大屏：按平台、部门、店铺展示客服核心数据（在线客服数、接会话量、平均响应时长、质检合格率、客户满意度），实时更新数据，支持异常预警（如：质检合格率低于阈值提醒）

10. 数据筛选与钻取        
  

11. 多条件筛选：支持按平台、部门、店铺、时间段（自定义时间段）筛选大屏数据，筛选后实时更新所有组件数据

12. 数据钻取：点击大屏组件（如：订单总额柱状图），可钻取查看细分数据（如：各店铺订单明细），支持多级钻取，钻取数据关联平台+部门+店铺权限

13. 大屏配置与分享        
  

14. 自定义配置：支持调整大屏组件大小、位置（拖拽调整），修改组件颜色、字体，保存个人自定义配置，不同用户可拥有不同配置

15. 大屏分享：支持生成大屏分享链接（链接有效期可配置），分享链接仅对拥有对应平台、部门、店铺权限的用户可见，禁止分享给无权限用户

16. 定时刷新：支持配置大屏数据刷新频率（如：1分钟/5分钟/10分钟），实时同步最新数据，刷新失败时显示错误提示

17. 异常预警        
  

18. 预警配置：设置核心数据预警阈值（如：订单退款率超过10%、质检合格率低于80%），关联平台ID、部门ID、店铺ID，支持按平台+部门+店铺设置不同阈值

19. 预警提醒：数据达到预警阈值时，大屏对应组件高亮显示，同时发送系统消息提醒管理员，提醒信息关联平台、部门、店铺及预警数据

20. 预警记录：保存所有预警记录，显示预警时间、预警类型、预警数据、关联平台/部门/店铺、处理状态（未处理/已处理），支持预警记录搜索、导出

### 拖拽功能

- ✅ 大屏组件 **拖拽布局**：在大屏配置页面，拖拽组件调整位置、大小，支持组件对齐、均匀分布，拖拽后自动保存配置

- ✅ 组件排序 **拖拽**：拖拽大屏组件调整展示层级（上下顺序），拖拽后自动保存，层级高的组件显示在上方

- ✅ 筛选条件面板 **拖拽自定义**：拖拽调整筛选条件的显示顺序、显示/隐藏，保存个人自定义筛选面板配置

---

## 模块 10：消息通知（message）【全模块联动】

### 功能清单

1. 通知模板管理        
  

2. 模板配置：新增通知模板（如：审批通知、考勤异常通知、订单通知、客服预警通知），配置模板名称、模板类型、通知内容（支持变量替换，如：${username}、${orderNo}）、关联平台ID、部门ID、店铺ID（可选，关联店铺后仅该店铺人员接收通知）

3. 通知方式：支持系统内消息、短信、邮件三种通知方式，可配置单一方式或多种方式组合，不同模板可配置不同通知方式

4. 模板管理：编辑/删除模板、模板启用/禁用（禁用后不再发送该模板通知）、模板复制、模板变量管理（添加/编辑模板变量，关联对应模块数据）

5. 消息发送管理        
  

6. 自动发送：关联各模块事件（如：审批提交、考勤异常、订单生成、客服差评），触发事件后自动发送对应模板通知，通知接收人按平台、部门、店铺权限及模块关联关系分配（如：订单通知发送给对应店铺负责人）

7. 手动发送：管理员可手动发送通知，选择通知模板、接收人（支持按平台、部门、店铺筛选选择）、补充通知内容，支持批量发送

8. 发送记录：保存所有通知发送记录，显示发送时间、通知模板、发送方式、接收人、发送状态（成功/失败）、关联平台/部门/店铺、关联模块事件，支持发送记录搜索、导出

9. 我的消息管理        
  

10. 消息查看：按消息类型（系统通知、审批通知、业务通知）筛选查看个人消息，显示消息标题、消息内容、发送时间、关联平台/部门/店铺、已读/未读状态

11. 消息操作：标记已读/未读（支持批量标记）、删除消息（逻辑删除）、收藏消息、查看消息详情（显示关联模块数据，如：审批消息可直接跳转至审批详情）

12. 消息设置：自定义消息接收方式（如：仅接收系统内消息、不接收短信通知），设置消息免打扰时间段，保存个人消息设置

13. 消息权限控制        
  

14. 发送权限：仅管理员及拥有对应模块权限的用户可手动发送通知，发送范围受平台、部门、店铺权限限制（如：普通员工仅可发送本部门、本关联平台及店铺的通知）

15. 接收权限：用户仅接收自己所属平台、部门、店铺及关联模块的通知，无权限的通知不展示、不推送

16. 管理权限：管理员可查看所有平台、部门、店铺的消息发送记录，普通用户仅可查看自己的消息及自己发送的通知记录

17. 消息联动配置        
  

18. 模块联动：与审批流程、考勤排班、电商平台、客服质检等所有模块联动，触发对应事件自动发送通知（如：审批通过通知申请人、考勤异常通知员工及部门负责人、店铺订单异常通知店铺负责人）

19. 联动规则：配置事件与通知模板的关联关系，设置通知接收人规则（如：审批通知接收人为申请人、审批人），关联平台、部门、店铺，不同平台+部门可配置不同联动规则

### 拖拽功能

- ✅ 通知模板变量 **拖拽排序**：在模板编辑页面，拖拽模板变量调整显示顺序，拖拽后自动保存，变量替换后按排序显示在通知内容中

- ✅ 我的消息列表列 **拖拽自定义**：拖拽调整消息列表列的显示顺序、显示/隐藏列（如：关联店铺、发送方式），保存个人自定义配置

- ✅ 联动规则 **拖拽编排**：拖拽模块事件与通知模板，建立联动关系，拖拽调整联动规则的优先级，拖拽后自动保存

---

# 五、补充说明（强制落地）

## 5.1 数据权限补充

所有模块数据均严格按“平台→部门→店铺”层级隔离，用户登录后仅能查看/操作自己所属平台、部门及关联店铺的内容，具体规则如下：

- 超级管理员：可查看/操作所有平台、部门、店铺的所有数据，拥有全部操作权限

- 平台管理员：仅可查看/操作对应平台下所有部门、店铺的数据，无跨平台操作权限

- 部门管理员：仅可查看/操作对应部门及关联平台、关联店铺的数据，无跨部门操作权限

- 普通用户：仅可查看/操作自己所属部门、关联平台及绑定店铺的数据，仅拥有对应角色的操作权限

## 5.2 接口关联补充

所有API接口均与平台、部门、店铺绑定，接口调用时需携带platform_id、dept_id、shop_id（可选，非电商相关接口可省略shop_id），接口层自动校验权限，无对应平台、部门、店铺权限的调用将返回403错误；接口日志、接口统计均按平台、部门、店铺分组，支持按层级筛选查看。

## 5.3 文件存储补充

所有上传文件（员工身份证正反面、商品图片、报销附件等）均统一存储至MinIO容器，按“平台ID/部门ID/店铺ID/文件类型”分类存储，文件访问需校验平台、部门、店铺权限，仅拥有对应权限的用户可访问、下载文件；文件保留策略可配置，定期清理过期文件，避免存储冗余。

## 5.4 Docker部署补充

Docker容器部署需严格遵循3.5节配置规范，开发、测试、生产环境使用不同的.env文件，禁止跨环境混用配置；容器启动后需检查服务连通性（如：MySQL、Redis、MinIO、Qdrant与后台服务的连通性），连通失败时自动重启容器，确保服务正常运行；定期备份容器Volume数据，防止数据丢失。


> （注：文档部分内容可能由 AI 生成）